# Yuimi Lab Development Notes

## Note Responsibility

- Scope: Yuimi Lab Astro repository and the progressive multi-theme architecture work.
- Authority: canonical recovery note for this repository.
- Language: verified facts and decisions are recorded in Chinese; code identifiers remain unchanged.

## Non-Negotiable Constraints

- Preserve every canonical content URL. If the user explicitly changes which theme owns the root routes, keep the displaced theme independently available under its own `/themes/<id>/` prefix without changing its copy, visuals, or interactions.
- Never modify, delete, stage, or publish the current untracked source materials under the repository root, `XP/`, `kisara/`, or `showcase-output/` without an explicit request.
- Keep shared article content, covers, and article attachments outside theme namespaces.
- A non-default theme must own its layouts, pages, CSS, runtime, and presentation assets; it must not import Fuyukawa Kagari theme internals.
- Cross-theme navigation must use a full document navigation so `ClientRouter`, theme CSS, and global listeners cannot leak across themes.
- Before every new functional edit, verify that the tracked workspace has a clear Git rollback commit. If the previous functional work is still uncommitted, create a scoped safety commit before changing code; never include the untracked user source-material directories in that checkpoint.
- Windows/PowerShell rules, UTF-8 preservation, minimal edits, and non-destructive Git rules from `AGENTS.md` apply.
- 细微视觉效果不得以 Codex 浏览器截图作为最终判断依据。浏览器截图只用于一眼可见的大范围布局、状态或回归检查；链节咬合、微小位移、材质厚度、粒子手感等细节必须交由用户目视验收。需要用户查看但主任务仍可继续时，优先发送 Windows 提醒，不要为了等待视觉判断中断当前任务。
- 子代理按“方向是否明确、写入风险、影响范围、验证成本”而不是单纯按任务大小分配。主线程 `sol max` 的首要职责是指挥、判断、指引和验收：理解需求，确定方案与非目标，划定文件边界和验收标准，保留关键事实与证据，并统一审查、整合、测试和提交；同时最大限度保护主线程上下文，把可压缩的长文件阅读、日志、重复浏览器操作和机械实现交给子代理，只在高风险决策、代理能力边界、紧耦合关键路径或交付质量不足时亲自操刀。不得为了使用子代理而使用子代理：若问题确实只需一两步、修改位置与方案明确、主线程可以快速完成，就直接亲自处理，不要把 `luna`、`terra`、`sol` 全部叫来验证简单事实。
- `luna` 是默认的快速执行层，不再局限于只读搜索。它可以负责文件/路径/行号定位、长文件与日志压缩、参数和资源整理，也可以在 Prompt 已明确目标、注意点、允许写入文件、禁止触碰区域和验收方式时，直接完成低风险单文件或小范围写入，例如文案、数据数组、局部 CSS、沿用既有模式的简单事件/测试、资源转换和坐标明确的图像处理。纯只读定位与压缩可使用较低 reasoning effort；只要任务涉及浏览器工具或任何文件写入，创建 `luna` 时推荐直接使用 `reasoning_effort: max`，避免因工具规划和实现判断不足反复返工。若任务过小且主线程已经知道修改位置，直接本地完成，避免代理协调成本超过收益。
- `terra` 适合中等难度的审查、方案比较、明确 Bug 分析和实现审计；当主线程已经确定方向且写入边界独立时，也可以负责通常限制在 `1-3` 个互不冲突文件的中等复杂度实现，例如响应式适配、生命周期与清理、中等状态机、测试补充和候选实现。它不得自行重新定义产品方向或扩大范围。任务达到高难度、需要架构审查、复杂竞态追踪、高风险方案讨论或独立反方意见时，才创建与主线程同级的 `sol high` 子代理帮助分析；`sol` 子代理只负责只读审查、讨论、证据整理和方案质疑，不写文件、不调用浏览器，也不承担常规搜索或机械实现，因为其工具循环与主线程同样昂贵缓慢，通常没有必要使用 `max`。最终方案、代码写入和验收仍由主线程统一负责，浏览器操作则统一交给 `luna max`。
- 主线程禁止直接调用浏览器工具；无论操作简单还是复杂、一次还是多轮，均交给 `luna max` 执行。主线程提供 URL、视窗、操作步骤、预期状态和停止条件，实时查看 `luna` 返回的关键状态并决定下一步；`luna` 负责点击、悬浮、滚动、刷新、切路由、读取 DOM/计算样式/控制台并保存关键截图。截图和媒体默认写入本地路径，只返回路径、关键数值、实际结果和复现步骤，禁止把大量 Base64、完整 DOM 或连续截图塞回主线程；主线程只查看少量关键截图并完成最终判断。涉及登录态、发布、删除、保存云端配置或其他外部副作用时，`luna` 停在执行前一步，由主线程完成风险判断和确认后再指挥 `luna` 继续，或交由用户亲自操作；主线程仍不接管浏览器点击。
- 默认采用逐级升级：`luna` 定位或实现，证据表明能力不足时将已确认事实和剩余疑点交给 `terra`，再有架构或高风险不确定性时由主 `sol max` 处理；禁止用同一失败 Prompt 反复重跑。对于问题处理或代码写入，子代理首次交付后最多只进行一次由主线程给出明确证据和具体要求的返工或重试；若返工后质量仍差，或主线程判断该代理在当前任务上已经无法做得更好，应立即关闭代理并由主线程亲自解决，不得让子代理连续重试十几轮。创建代理时显式选择模型和 reasoning effort，默认 `fork_context: false`，传入自包含任务、精确输入、输出格式和停止条件；只允许主代理派发一级代理，禁止嵌套。一个文件同一时间只有一个写入者，重型构建/测试不得并行；代理完成后立即收束为结论、证据、文件/行号、风险和建议动作并关闭，最终完整测试与构建只由主线程在合并后运行一次。

## Current Snapshot

- Repository: `C:\Users\a1234\Desktop\个人博客`
- Remote: `https://github.com/Yuimi-chaya/Yuimi-chaya.github.io.git`
- Branch: `main`; on `2026-08-10` the user restored the pre-redesign Home as the active mainline so article publishing and shared/general features can continue without waiting for the new stage.
- Blog punctuation normalization (`2026-08-12`): all nine `src/content/blog/*.md` articles replace Chinese full stops `。` with `.` and Chinese commas `，` with `,` (394 and 708 replacements respectively). The pre-change article diff, including the user's uncommitted manual edits to `codex-app-usage-notes.md`, is preserved at `C:\Users\a1234\Desktop\codex-backups\personal-blog-before-punctuation-20260812-220333.patch`. Existing UTF-8 BOM and LF/CRLF styles were retained per file; `rg` found no remaining target punctuation and `npm run build` completed with 62 pages and a 63-page Pagefind index.
- Blog documentation update (`2026-08-10`): `src/content/blog/codex-app-usage-notes.md` now centers the latest `AGENTS.md` and `codex-development-guidelines` practices on the development-note loop `read -> verify -> work -> write back`, task-complexity escalation, and `luna max` browser execution. The final reader-facing version removes drafting-language and undisclosed rule-number framing, folds environment/Git/media safety into the `work` phase, defines the main thread before explaining subagent routing, and restores the dedicated Base64 section covering compressed image-review copies, multi-image `413 Payload Too Large` risk, cautious legacy cleanup, and `luna` returning only real image paths plus concise findings. `git diff --check`, `npm test` (`22/22`), and `npm run build` (62 pages, 63-page Pagefind index) pass; the article has static build validation, while ordinary human reading acceptance remains separate.
- Mainline split (`2026-08-10`): `main` was moved without `reset --hard` from the unaccepted keyframe-stage head back to `72a7719 docs: refine Kisara redesign direction`. The complete new six-scene/keyframe stage is preserved independently on local branch `codex/kisara-new-stage-20260810`; its implementation checkpoint is `c1b3ac9 fix(kisara): morph transformation letter in place` and its branch-role note is `d6324f2 docs: record new stage side branch`. Continue that visual work only on the side branch. Local branch `codex/kisara-pre-home-redesign-20260806` remains as the original safety alias at `72a7719`. Nothing was pushed, and the existing untracked source/candidate materials remain in place and must not be staged, moved, deleted, or interpreted as tracked old-stage runtime content. After the split, `git diff --check` passed, `npm test` passed `22/22`, and `npm run build` produced 62 pages with a 63-page Pagefind index on the restored mainline.
- Kisara homepage `001` accepted-memory checkpoint: `449232c chore(kisara): checkpoint accepted 001 memory state`. This is the user-confirmed rollback point after recovering the exact right/bottom diffusion trajectory; do not alter its mask geometry, deterministic random sequence, source framing, or `1900ms` reveal timing during later ambient refinements.
- Current `001` ambient refinement (`2026-07-31`): the existing three depth layers use subject-anchored asynchronous breathing, with the sharp subject layer intentionally almost stationary while the blurred background/foreground move at a clearly visible but bounded roughly `1-3%` scale range. The first pass at `e4656a8` was technically active but visually ineffective because most motion stayed below `1%`, pointer travel was about `1px`, and the first capillary pulse arrived too late; do not restore those timid values. Fine-pointer input now listens on the complete `001` section rather than only the image visual, so overlapping copy cannot intercept the response. The accepted ink Canvas performs an earlier intermittent capillary pulse after its original reveal completes; the pulse adds no random calls to `buildOpeningMemoryInk`, pauses offscreen or in background tabs, restores the exact final frame when stopped, and is disabled on mobile/coarse-pointer/reduced-motion devices. Sparse text-shadow afterglow shares the same visible-state pause gate. Fine amplitude and feel remain for user visual acceptance.
- Current `001` hint presentation (`2026-08-05`): opening easter hints now remain in the same copy layer as the scene text but use a numbered `HIDDEN ROUTES` annotation list with short clue phrases, staggered settle-in, a thin signal rail, and the existing Pulse-style layered strike. The hint ids and trigger ledger are unchanged; do not move them into an opaque card or introduce a second image/mask layer over the memory scene.
- Current `001` hint contrast correction (`2026-08-05`): after visual review found the annotation rows still washed out by the pink source lettering, the copy now counter-rotates back toward horizontal, uses darker heavier ink, a faint edge-faded local paper wash, stronger label/rail contrast, and a light text edge for separation. This remains a local text treatment only; the source image, mask, video timing, and hint state machine are unchanged. `npm test` passed 22/22 and `npm run build` produced 62 pages with a 63-page Pagefind index; fine visual acceptance remains with the user.
- Kisara Friends route (`2026-08-05`): profile now links to the new canonical `/friends/` page, with `/themes/kisara/friends/` as the noindex compatibility route. `FriendsPage.astro` and `friends.css` use the Lovebrain final-frame atmosphere, animated pink/blue orbit lines, pointer parallax, four replaceable friend-link slots, and a return-to-profile link. The browser agent was requested for desktop/mobile visual review but both attempts were unavailable (`429`, then `503`); static validation is complete, while fine visual acceptance remains with the user.
- Multi-theme baseline commit: `d4e2a29 Add independent multi-theme architecture`.
- Kisara color-mask prototype rollback commit: `97990c8 Add Kisara scroll mask theme prototype`.
- Kisara automatic-release rollback commit: `0409dbf Stabilize Kisara automatic burst sequence`.
- Kisara organic-beam rollback commit: `9ac9223 Refine Kisara organic beam and release`.
- Kisara beam-detail rollback commit: `fcc9397 Refine Kisara beam detail and performance`.
- Kisara site-wide presentation rollback commit: `fae3b93 Unify Kisara theme presentation`.
- Kisara momentum/screen-impact rollback commit: `835baa4 Refine Kisara beam motion and black hole impact`.
- Kisara seamless-phase/collapse rollback commit: `e19ca2a Deepen Kisara collapse and lensing sequence`.
- Kisara WebGL lens baseline rollback commit: `5864321 Replace Kisara warp slices with WebGL lensing`.
- Kisara continuous-lensing refinement commit: `bd03ae6 Refine Kisara black hole lensing`.
- Kisara subject-aware lensing refinement commit: `5a70d67 Prioritize Kisara subject-aware lensing`.
- Kisara continuous-title-lens checkpoint commit: `5e3949d Checkpoint Kisara continuous lensing`.
- Kisara side-field lensing refinement commit: `139ca19 Constrain Kisara lensing to side fields`.
- Kisara segmented aftermath refinement commit: `3b017b0 Deepen Kisara black hole aftermath`.
- Kisara title-clarity checkpoint commit: `957edab Checkpoint Kisara clarity reconstruction pass`.
- Kisara block-reconstruction checkpoint commit: `d774fcc Extend Kisara block reconstruction field`.
- Kisara sharp blade-gloss refinement commit: `3e0c690 Keep Kisara blade gloss sharp`.
- Kisara ball-lightning/performance refinement commit: `280cd0d Refine Kisara lightning core and performance`.
- Kisara procedural plasma-core refinement commit: `1426bde Refine Kisara blade gloss and plasma core`.
- Kisara morning blade-gloss restoration commit: `f4aeb16 Restore Kisara morning blade gloss`.
- Kisara blade text-mask correction commit: `444d77c Clip Kisara blade gloss to title`.
- Kisara procedural singularity rollback commit before the post-release interaction pass: `7059c87 Integrate Kisara procedural singularity core`.
- Kisara title-erosion/singularity-flow checkpoint commit: `4e61262 Checkpoint Kisara data erosion and singularity flow`.
- Kisara charge-beam transition refinement commit: `139ab81 Refine Kisara charge beam transition`.
- Kisara post-release whip-particle refinement commit: `a20782e Refine Kisara post-release whip particles`.
- Kisara title-data erosion integration rollback commit: `cc15be9 Integrate Kisara title data erosion`.
- Kisara gate-runtime restoration commit: `c016746 Restore Kisara gate interaction runtime`.
- Kisara eye-particle flow refinement commit: `3eae746 Refine Kisara eye particle flow`.
- Kisara segmented charge-flight checkpoint commit: `1725af6 Checkpoint Kisara charge flight continuity`.
- Kisara continuous charge-flight refinement commit: `f754c06 Smooth Kisara charge flight path`.
- Kisara charge-flight notes rollback commit: `e399d3e Update Kisara charge flight notes`.
- Kisara rejected 2D liquid-overlay rollback commit: `ba82ee4 Refine Kisara post-release title effect`.
- Kisara eight-second automatic-release timing commit: `f20ab43 Tighten Kisara autoplay timing to 8 seconds`.
- Kisara full-screen warning experiment rollback commit: `40d1558 Checkpoint full-screen Kisara warning experiment`.
- Kisara full-screen warning refinement checkpoint: `010e9e7 Refine Kisara full-screen warning`.
- Kisara continuous warning-spin rollback commit: `eee26e7 Smooth Kisara warning cross rotation`.
- Kisara shortened warning-handoff rollback commit: `72794e2 Tighten Kisara singularity handoff`.
- Kisara staged singularity entrance rollback commit: `a235044 Stage Kisara singularity entrance`.
- Kisara warning overexposure rollback commit: `0baecad Refine Kisara warning overexposure`.
- Kisara July 20 warning restoration checkpoint: `556a90e Restore Kisara July 20 warning cross`.
- Kisara original-scene exposure refinement commit: `d112f11 Refine Kisara warning exposure`.
- Kisara chain rupture/heart-seal rollback commit before the immediate-crush refinement: `3204f96 Rework Kisara chain rupture and heart seal`.
- Kisara opening-transition rollback commit: `cb1fa6e Refine Kisara opening transition`.
- Kisara compressed singularity/reversible-intro checkpoint: `91356a2 Tighten Kisara singularity sequence`.
- Kisara stable outer-chain motion rollback commit: `2d7853b Stabilize Kisara outer chain motion`.
- Kisara adaptive chain-interlock rollback commit: `d1ffa62 Refine Kisara chain interlock`.
- Latest Kisara inner-page functional checkpoint: `39c76f9 Fix Kisara arcade mobile sizing`.
- Latest Kisara home/Blog presentation checkpoint: `47331c7 Redesign Kisara home and blog surfaces`.
- Kisara Works cooking-game checkpoint: `19b01a7 Build Kisara Works cooking game`. The compact single-viewport workbench, piled draggable pantry, side-by-side blender/trash layout, origin-anchored drag ghost, and shortened result/remix scroll timing are the current accepted follow-up; `09d3f7c` remains the earlier image-led Works rollback.
- Kisara original transformation-scene rollback commit: `603abbc Integrate Kisara transformation scene sequence`.
- Kisara mobile theme-input and homepage boot-lock rollback commit: `2b6a39b Add mobile theme input and Kisara home boot lock`.
- Kisara homepage Event 003 investigation checkpoint: `0d5cbc2 Rebuild Kisara event investigation scene`.
- Kisara Event 003 camera-frame refinement checkpoint: `3393c8b Refine Kisara camera investigation frame`.
- Kisara homepage refrigerator `002` checkpoint: `48e34e5 Build Kisara fridge discovery section`.
- Kisara Event 003 secret-audio/achievement rollback checkpoint: `9e30edd feat(kisara): add secret audio state and achievement`.
- Deployment: Astro static output to GitHub Pages through `.github/workflows/deploy.yml`.
- Local state: the Kisara image transition, seal-chain title progression, post-gloss procedural warning, segmented foreground pink-black-hole release, original-scene `6 -> 7 -> 8` transformation bridge, continuous full-frame WebGL2 lensing, independent title refraction/dissolve/reconstruction, full-screen block reconstruction, lingering particle rain, post-reconstruction highlights/parallax, and site-wide deep-indigo UI are implemented and browser-validated. Blog, Works, Game, and Me now use four distinct page structures rather than variants of one card layout. The title keeps its clear red core and soft tide, while the blade gloss remains the Beijing-time July 19 morning slanted text layer clipped inside the glyphs. The rejected `ba82ee4` 2D overlay remains a rollback reference only; `1725af6` remains the segmented-flight rollback point. Untracked user source materials remain separate and untouched (`102000325_p0.jpg`, `122472458_p0.png`, `gate-background.jpg`, `XP/`, `kisara/`, `showcase-output/`).
- Default theme ID: `kisara`.
- Available theme IDs: `fuyukawa-kagari`, `blank`, `kisara`.
- Route decision: `kisara` owns the canonical root URLs. `fuyukawa-kagari` uses `/themes/fuyukawa-kagari/...`; `blank` uses `/themes/blank/...`; the historical `/themes/kisara/...` pages remain as noindex compatibility aliases. Every prefixed route canonicalizes to its matching root URL.
- Preference key: `yuimi-theme-id-v2` with an allowlist and `kisara` fallback. The key was intentionally versioned instead of inheriting `v1`, because the old gate automatically stored the former Fuyukawa default for ordinary visitors and would otherwise redirect most returning users away from the new default.
- Implementation state: three independent frontends are locally available, with Kisara now serving the canonical Home/Blog/Game/Works/Me/article routes. Fuyukawa remains complete under its explicit prefix, including theme-local navigation, article search results, Home actions, canonical metadata, and `noindex,follow`. Commit `2b6a39b` adds the cross-theme mobile long-press menu, homepage boot lock, and final-stage mobile orientation input. The current homepage runtime recovery patch keeps the large gate script as a cacheable external module, retries a failed module request from a small inline guard, and prevents `astro:page-load` from cleaning up an already-bound gate. The subject-aware lensing checkpoint is `1164fb4`, the latest warning rollback point is `d112f11`, the opening-transition rollback point is `cb1fa6e`, and the compressed-release checkpoint is `91356a2`.
- Current uncommitted stability pass (`2026-07-28`): the persistent Kisara audio control now draws its play/pause glyphs with local CSS geometry instead of Astro icon `<use>` references, preventing an intermittently clickable-but-empty control after document swaps. Me episode-memory scenes no longer combine full-viewport `filter` animation with `clip-path` and transform on the same scene layer; the visual background was moved out of negative stacking and given backface protection to reduce intermittent white compositor flashes. `npm test` passed 3/3 and `npm run build` generated 42 pages with a 43-page Pagefind index. Fine runtime visual confirmation remains pending user testing.
- Current reverse-opening handoff: the gate visual shell remains fixed for the whole handoff and is clipped by the gate section after leaving the first viewport; scene breathing belongs to an inset inner camera. Returning from `001` keeps the live portal open while the section moves down, then resets the blue gate only after the section has fully left the viewport. Do not restore the superseded `is-returning-to-gate` overlay or predictive wheel-delta cleanup.
- Current Me-to-Home secret checkpoint (`2026-07-29`): viewing Profile through Games without entering Pulse, then navigating directly to the Kisara Home route, consumes a 30-second same-tab ticket and replaces the normal gate with the one-shot `found-self` memory interlude. The interlude owns its input/audio lifecycle and exits through the existing Home smooth-scroll path; its ticket is consumed before playback, so returning upward always restores the normal blue gate.

## Kisara Project-Level Design Audit (`2026-08-06`)

### Audit Scope And Evidence

- This is a durable design-review snapshot, not an approved implementation plan. The review was read-only against `main` at `7bf3934`, used the LottieFiles motion-design framework, mblode agent-skills UI/animation/typography guidance, and Impeccable critique methodology without globally installing any Skill. Two browser reviews covered representative desktop/mobile states, and an independent source/media review challenged the product direction. The transient Impeccable detector attempt timed out after 124 seconds, was stopped, and produced no detector result.
- Objective inventory at review time: the Kisara theme contained roughly 145 CSS keyframe declarations, 247 animation declarations, and 142 public image/video/audio assets totaling about 124.47 MB. These are inventory/density indicators, not proof that all effects or assets execute or download simultaneously.

### Durable Conclusions

- Kisara already has a strong, non-template visual identity. Its next quality ceiling is not reached by adding more effects; it requires stronger editorial selection, motion hierarchy, visual rest, and clearer page responsibilities.
- The recurring systemic issue is the absence of a calm post-performance state. Home sections, inner-page entrances, persistent HUD, audio state, particles, breathing, tracks, and text motion frequently remain active together, so content has too few stable reading windows. A future motion budget should usually allow one primary action, one interaction response, and one low-frequency ambient effect per viewport.
- The Home gate remains the flagship interaction and should not be homogenized away, but captured wheel/touch input still violates ordinary scrolling expectations. Input acknowledgement, reliable escape paths, and return-visit behavior must remain explicit.
- Home `001` easter hints still compete with the source scene lettering and the site introduction. Further contrast work should manage the background/source-text hierarchy while hints are active instead of only increasing text opacity or adding an opaque card.
- Blog has one of the strongest visual openings, and its four-character selection should not be removed merely to simplify the page. The real issue is that this interaction has little semantic relationship to the primarily technical articles below it; future refinement should either connect character viewpoints to content grouping/recommendation or clearly treat the stage as a short atmosphere piece.
- Article presentation is visually cleaner than the surrounding pages, but long technical reading still inherits persistent Kisara HUD/audio/route behavior. A protected quiet-reading state should reduce nonessential loops and preserve native text selection, copying, and browser interaction.
- Works uses an appropriate cooking image and should not replace it merely because the current composition has faults. The verified problems are clipped hero copy, unstable contrast/exposure, and the cooking game delaying visible evidence of real projects, responsibilities, and results.
- Home `002` has semantically appropriate refrigerator media and draggable objects, but the interaction goal is weakly communicated. Home `003` has strong investigation media but too many simultaneous metadata, progress, chapter, and instruction layers around the actual camera hotspot.
- Mobile pressure comes mainly from fixed navigation, audio controls, small HUD labels, crop-sensitive character media, and full-screen effects competing for the reduced viewport. Mobile should not merely scale the desktop composition down.
- Friends currently uses an emotionally suitable ED/final-scene atmosphere, but the visual-performance intensity exceeds the information density while its link slots remain placeholders.
- Media governance and runtime concentration are now design risks: public assets mix formal runtime files with QA/candidate/generated variants, while `KisaraLayout.astro`, `home.css`, and large page files own many unrelated visual/state responsibilities. This increases the likelihood that a small visual refinement causes lifecycle, route, or stale-state regressions.

### User-Confirmed Additional Critical Findings

- Although Home sections repeatedly feature the same character and each section is individually well supplied with media and effects, they do not yet form a convincing shared narrative or conceptual progression. `001`, `002`, `003`, `004`, the footer stage, and the gate can feel like strong but unrelated fragments placed next to one another. The solution does not need to force a single-episode narrative like the gate or Me; it does need a readable connective idea, escalation, or chapter relationship so the page feels intentionally authored rather than assembled east-to-west from independent scenes.
- The site relies on damped, smooth, viewport-to-viewport section movement close to ten times. Repeating the same transition grammar creates interaction fatigue and makes otherwise different scenes feel mechanically identical. At the same time, falling back to ordinary notch-by-notch wheel scrolling is also a poor fit for these large visual sections and feels slow, sticky, and imprecise. This scrolling conflict is a first-class product problem, not a minor tuning issue.
- A later redesign should distinguish performance scenes from reading/content sections instead of applying one scroll model everywhere. Candidate direction: reserve section locking/snap for a small number of flagship scenes; use accumulated intent and momentum so one deliberate gesture advances a scene rather than requiring many wheel notches; keep ordinary continuous scrolling for denser reading areas; preserve natural trackpad/touch behavior. These are audit directions only and require a separate interaction design decision before implementation.
- Redesigning the four Home sections does not imply discarding their accepted media and interaction assets. Each asset should be evaluated independently for semantic fit, visual quality, and adaptation cost; suitable material may be reframed, reduced, or migrated into Blog, Works, Me, Games, article discovery, or other page-local roles where it can also resolve an existing content or presentation weakness. Asset reuse must follow page purpose rather than preserving the current Home placement by default.
- Cross-section transitions must be designed only after the adjacent sections have stable compositions, independent themes, interaction ownership, and final entry/exit states. A transition is the shared boundary between a specific outgoing section and a specific incoming section, not a reusable site-wide effect attached afterward. Depending on the actual pair, the correct solution may be foreground carry-through, spatial overlap, material transformation, exposure/focus continuity, an authored dissolve, ordinary continuous movement, or deliberately no visible transition; do not preassign damping, fading, or dissolving as formulas.

### Prioritized Future Review Order

1. Repair verified clarity defects without changing the underlying product direction: Works hero copy, `001` hint hierarchy, mobile Game viewport pressure, `003` action hierarchy, and Friends placeholder presentation.
2. Build an asset-role migration map, define the Home narrative spine, and classify every section as flagship performance, interactive vignette, reading/content, or ambient bridge.
3. Stabilize each redesigned section first, then design its pairwise boundaries together with a mixed scrolling grammar and motion budget; do not tune more damping constants or add generic section transitions beforehand.
4. Establish quiet-reading and native-browser behavior for articles, then reduce persistent inner-page motion and HUD density.
5. Reconcile Blog character semantics, Works project evidence, media governance, and route/runtime ownership.

### Non-Decisions

- Do not interpret this audit as approval to remove the Home gate, Blog character stage, Me episode sequence, semantic mini-games, easter eggs, custom context menu, or the Kisara pink/blue identity. Each needs targeted evaluation rather than blanket deletion.
- Do not solve cross-section inconsistency by forcing every page or section back into one repeated card, HUD, title, or transition template. The goal is shared grammar with distinct page identities.
- No visual or interaction implementation was approved during this audit; user selection and prioritization are still required before code changes.

## Architecture Decision

- Shared layer: content collections, article queries, site metadata, category labels, SEO inputs, and theme registry/path helpers.
- Fuyukawa Kagari layer: existing pages, layouts, theme CSS at `src/themes/fuyukawa-kagari/styles/theme.css`, navigation presentation, footer copy, Sakura, pig scrollbar, tool dock, music player, Live2D, notice, weather/IP signal, Canvas tag rain, and related assets.
- Blank layer: independent document layout, navigation, pages, article presentation, CSS, context menu, and visible theme return control. It does not load Kagari CSS, assets, remote Live2D, music, weather, or notice runtime.
- Kisara layer: independent dark visual-interaction frontend with a reversible blue-to-red diagonal title tide controlled by captured vertical input. It owns its layout, pages, CSS, mobile navigation, context menu, runtime, and theme-owned background slots under `public/themes/kisara/assets/`.
- Switching: map the current canonical pathname into the target theme, preserve query/hash, persist the target ID, then perform a full-document navigation. Explicit user switches use history-preserving `assign`; automatic preference restoration uses `replace`; browser history traversal adopts the restored page's theme.
- SEO: alternate theme routes use `noindex,follow`; sitemap excludes `/themes/` routes.
- 404: the single GitHub Pages root Kisara 404 performs an early client redirect for unknown Blank, Fuyukawa, or legacy Kisara prefixed paths to that theme's `/404/` route, carrying the original URL in `from`.

## Asset Inventory

- Shared content assets: `public/blog-assets/`, `public/blog-covers/`, Markdown content images.
- Fuyukawa Kagari theme assets: `public/themes/fuyukawa-kagari/assets/`, `public/themes/fuyukawa-kagari/music/`, Live2D CDN configuration, homepage background/avatars/brand images, About media, and the 404 visual.
- External Fuyukawa Kagari runtime dependencies: jsDelivr Live2D packages, Cubism SDK, location/weather endpoints.
- Kisara presentation assets: the user-selected cold background is at `public/themes/kisara/assets/gate-background.jpg`; the explicitly selected fight artwork is at `public/themes/kisara/assets/fight.jpg`. Transformation sources `kisara/6.png`, `kisara/7.png`, and `kisara/8.png` remain user-owned and untouched; optimized runtime copies are `transformation-smoke-wide.webp`, `transformation-detail.webp`, and `transformation-silhouette.webp` under `public/themes/kisara/assets/`.
- Kisara gate asset matching: the base names `gate-background` and `fight` accept `.webp`, `.png`, `.jpg`, or `.jpeg`, in that priority order. Keep one custom format per base name when comparing artwork.
- Kisara Event 003 sources `kisara/picture.png` and `kisara/win.png` remain user-owned and untouched. The user explicitly approved their copied runtime assets at `public/themes/kisara/assets/picture.png` and `public/themes/kisara/assets/win.png` for the homepage investigation scene.
- Kisara homepage refrigerator source `kisara/IMG_0042.PNG` remains user-owned and untouched. Runtime assets are `public/themes/kisara/assets/fridge-discovery-002.webp` plus the pre-rendered dark blur veil `fridge-discovery-002-veil.webp`; the veil exists to avoid animating a heavy full-screen CSS blur.
- Historical or unreferenced candidates remain untouched until a later cleanup request; absence of a current reference is not deletion approval.

## Migration Map

| Previous path or role | Current path or role |
|---|---|
| `src/layouts/BaseLayout.astro` | `src/themes/fuyukawa-kagari/layouts/BaseLayout.astro` |
| `src/layouts/ArticleLayout.astro` | `src/themes/fuyukawa-kagari/layouts/ArticleLayout.astro` |
| `src/styles/global.css` | `src/themes/fuyukawa-kagari/styles/theme.css` |
| `src/data/noticeContent.ts` | `src/themes/fuyukawa-kagari/data/noticeContent.ts` |
| Theme-heavy implementations in `src/pages/*.astro` | Theme-owned implementations under `src/themes/*/pages/`; root `src/pages/` files are thin canonical entries currently importing Kisara, while alternate themes have prefixed entries under `src/pages/themes/` |
| `public/assets/` | `public/themes/fuyukawa-kagari/assets/` |
| `public/music/` | `public/themes/fuyukawa-kagari/music/` |
| Blog collection, `public/blog-assets/`, `public/blog-covers/`, `src/lib/site.ts` | Remain shared and outside theme namespaces |

## Adding a Third Theme

1. Add a stable ID, label, and static `routePrefix` to `src/core/themes/registry.ts`.
2. Create `src/themes/<theme-id>/index.ts` as the public entry and export the theme's independent page components.
3. Implement the theme's own layout, pages, styles, runtime, and presentation data without importing another theme's internals.
4. Add thin static route entries under `src/pages/themes/<theme-id>/`, including article `getStaticPaths()` through the shared blog helper.
5. Store fixed URL assets under `public/themes/<theme-id>/`; keep article images and attachments shared.
6. Render `ThemePreferenceGate` with the new ID and expose both a context-menu choice and a touch/keyboard-accessible return control.
7. Keep prefixed theme pages `noindex,follow`, canonicalize to root content URLs, and retain the sitemap `/themes/` exclusion unless the SEO policy changes.
8. Extend registry tests, then verify build, deep links, history, refresh persistence, 404 fallback, desktop/mobile layout, and resource isolation.

## Active Workstreams

### Multi-Theme Foundation

- Goal: keep all three themes independently renderable and switchable while the selected default owns the unchanged canonical content routes.
- Status: implemented and locally validated with Kisara as the default root theme; publication pending.
- Key paths: `src/core/`, `src/themes/`, `src/pages/`, `public/themes/`.
- Validation: production build, root/deep-link rendering, both-direction context-preserving switch, refresh persistence, history navigation, and allowlisted fallback logic passed.

### Fuyukawa Kagari Boundary Migration

- Goal: move existing theme implementation as intact units before any internal refactor.
- Status: implemented and browser-validated; the original frontend now lives at `/themes/fuyukawa-kagari/...` after the explicit default-route migration.
- Explicitly deferred: splitting the large Kagari layout script, homepage runtime, and theme CSS into smaller modules.

### Blank Theme

- Goal: provide a minimal independent theme for all existing major pages and article reading.
- Status: implemented and browser-validated.
- Coverage: home, blog list, article detail, About, Projects, Games, and themed 404 on desktop and 390px mobile.
- Boundary: no import from `src/themes/fuyukawa-kagari/` and no `/themes/fuyukawa-kagari/` asset reference.

### Kisara Theme

- Goal: establish a visual-interaction-first character theme before selecting final artwork.
- Status: image transition, continuous organic charge beam, two-stage overcharge sequence, persistent particle pools, segmented pink-black-hole release, full-frame WebGL refraction, post-reconstruction eye/blade highlights, mouse parallax, and the WebGL liquid-title takeover are implemented and browser-validated on desktop and `375x844` mobile viewports; publication is pending.
- Key behavior: while the homepage is at the top, downward wheel/touch/keyboard input raises a blue-to-red diagonal tide and upward input lowers it. Input changes a target level; `requestAnimationFrame` advances the visible level with damped velocity, so the mask, diffused tide boundary, and background transition continue smoothly between wheel events. The cold background gradually blurs and dims while the fight artwork fades in from blur to clarity. The lower contract display is a Canvas-only tapered energy beam drawn from one continuous envelope; its internal filaments use periodic phase functions without modulo-wrapped packets or differently scaled shell geometry. Wake particles emit from the beam nose and keep visible streak continuity through jet, inertial coast, and ambient-drift phases before a late TTL fade. Normal-charge instability eases toward the current charge instead of jumping at wheel segments, and an explicit initial-state latch makes a second charge cycle restart from a clean pose phase after returning to zero. After the red tide settles at 100%, the title gloss finishes first; a short pre-warning blur then precedes the procedural DOM warning. The warning directly desaturates and raises the contrast of the actual scene, closes a dark elliptical focus field onto the title center, and keeps its black/white core, four unequal tapered rays, broken shards, and sparks above that field. Its rotation follows one uninterrupted wall-clock timeline across build, flash, and collapse instead of restarting at eased phase boundaries; only a restrained pulse-local shake remains near the impact. It then collapses sharply back into the core and hands off to an automatic eight-second segmented release. The sequence now dwells through field lock, deep collapse, terminal-crush hold, rupture, and aftermath rather than sweeping one linear progress curve. The local meter dissolves permanently for that sequence. A foreground pink black hole forms at the warning center while the title and background remain depth-blurred: layered far/near accretion arcs, a feathered absorption core, broken photon arcs, lensing paths, and bounded orbit particles build pressure before an inversion flash. The background WebGL pass uses one bounded radial inverse map to pull scene features toward the event well, adds a narrow compression shell and a low-amplitude asymmetric transverse flow, and keeps one feathered field boundary from the core through the outer fade. It no longer rotates one broad field direction or combines four unrelated corner vortices. The title remains independently refracted through rupture, dissolves into the blast instead of visibly springing back, and reconstructs from blur only during the final aftermath. Three slingshot particle waves, curved bipolar jets, expanding echo rings, residual ribbons, longer-lived fragments, slower gravity-driven rain, and afterglow replace the abrupt particle-only ending. The burst Canvas is a top-level gate layer and is explicitly excluded from the common blur selector, so the black hole remains sharp above the blurred title. The lower state text shows `CONTRACT 000%` through the first charge and switches to continuously changing machine-glitch output instead of replaying a second percentage counter. Document scrolling is released only after both stages settle at 100%; the next section and the return to the blue gate use a time-scaled damped spring rather than a distance-proportional jump.
- Escape paths: reduced-motion users start completed, restored pages below the top are synchronized to completed, and an explicit `SKIP` link bypasses the gate.
- Current energy refinement: the local beam uses one continuously advancing master phase rather than wrapping or visibly resetting at `2π`; return-to-gate resets its timestamp but preserves the oscillator phase. The envelope ripple, front/back depth-separated helix, compression rings, helical fragments, and nose shock therefore remain continuous across long runs and repeated charge cycles. Local and flight filaments now calculate each strand once, then batch segments into four depth buckets; the post-50% exterior coils use the same batched approach. During ordinary charge-only frames, the presentation fast path updates only the meter pose instead of rewriting the full black-hole/title/background variable set. Wake particles retain the low-pass beam-head velocity inheritance and bounded jet/coast/drift lifecycle.
- Current charge prelude: the 0-100% local beam follows a more pronounced softly displaced centerline, and its upper/lower envelope now enters the rounded nose through continuous cubic tangents rather than two straight shoulder ramps. Exterior front/back energy coils and partial halos appear progressively after 50%. When the target reaches full and the visible charge crosses roughly `99.5%`, the state locks to `100%` immediately and begins the bounded 2.7-second intro without waiting for the spring velocity to settle. The full-screen beam first inherits the meter's live translate/rotate/scale pose, then detaches. Its head follows one five-control U-shaped curve sampled by real arc length, rather than restarting easing at a launch/turn boundary. The lagging tail gathers through a matched quintic segment whose end tangent and acceleration agree with the main route, so it leaves the meter without a pinned endpoint or a second impulse when it crosses the old seam. Reversing after the intro still resets the prelude cleanly.
- Current seal-chain refinement: the outer frame feed is derived only from reversible scroll progress, with a separate bounded take-up feed during the automatic crush; wall-clock pose time no longer advances the closed loop while input is idle. Frame records are distributed by equal arc length and forced to an even count so the left closure cannot join two links with the same orientation. Upright links and flat connectors render in four depth passes: upright far arc, connector far arc, upright near arc, connector near arc. Connector width is calculated from the current pair-center span and the upright-link inner aperture instead of one global scale, so each connector remains independent, reaches both neighboring inner edges, and does not form a continuous strip behind the upright links. The user visually accepted the current fine interlock; future micro-adjustments must use user judgment rather than browser screenshots.
- Current title-tide material: the blue-gray source keeps the previously accepted fine repeating memory-striation texture and hollow low-saturation depth. The wine-red tide retains the newer non-repeating energy hot spots and fragments, while the moving boundary remains free of the rejected regularly tiled white dots.
- Current scene-camera refinement: the base scene, five memory scenes, and three transformation scenes share a stronger low-frequency multi-axis breath. Dynamic overscan derives from the live camera displacement and scale so the larger motion remains covered without exposing black edges. The eighth scene's blade handoff now adds up to `10%` extra scale while its blur clears.
- Current ambient/rail refinement: the gate owns fourteen lightweight deterministic pink motes between the scene shell and title rather than extending a Canvas hot loop; mobile keeps the first nine and reduced-motion hides the layer. The right rail preserves the existing gate progress source but has returned to one continuous thin track and fill without handoff ticks, segmented textures, or stage-dependent core shapes. A fixed angular contract needle follows progress; stages communicate through color, glow, warning rays, gravity orbit, and restrained pulse timing rather than swapping among ellipses, rings, blocks, and tapered bars.
- Current opening-bridge handoff: the pixel-continuous fixed gate shell remains active only while the sampled `001` edge is needed. Forward wheel/touch/keyboard input predicts when that edge will leave the viewport and finalizes the deferred gate reset before native scrolling can expose one compositor frame of `fight`. Reverse `001 -> gate` travel now owns an explicit `is-returning-to-gate` state, and bridge activation/deactivation always reconciles the real DOM classes instead of trusting a possibly stale in-memory flag; the generated opening-edge strip is therefore forced hidden while the live blue gate crosses the boundary. `002` and later homepage bands also own a higher isolated stacking context, so a late browser paint cannot place the fixed gate shell above their backgrounds.
- Current black-hole refinement: the release uses keyframed progress across five perceptual stages instead of one linear autoplay. Collapse reaches a nonlinear terminal crush, pauses under maximum pressure, then releases through a separate vacuum shockwave and detonation. The bounded procedural field no longer uses `angle * 3/5/7/11` spiral families, five rotating dark arms, rotating edge arcs, or a continuously changing global angle. Cartesian advected currents animate inside a stationary irregular envelope; only two or three short fixed-anchor fragments travel inward. Absorption clouds pulse radially without orbiting, orbit particles favor radial fall over angular acceleration, and four offset dark contours build the event well without a hard radial-gradient disk. The WebGL background lens now uses a monotonic bounded radial pull, local compression shell, and small asymmetric transverse flow instead of one global rotation plus four corner vortices. It adds no texture lookup and removes the old vortex evaluations; the independent title lens still handles rupture, dissolve, and measured reconstruction.
- Post-reconstruction interaction: activation is strictly `burstComplete && pageMode === "gate"`, not `.is-released`, because the latter is only applied after scrolling into the next section. A transparent scene Canvas maps normalized eye and blade coordinates through the exact `background-size: cover` geometry of `fight.jpg`, then draws screen-blended blooms, tapered anime trails, and bounded particles that detach in screen space. Pointer direction selects one damped travel target rather than restarting on every event; the fight background moves at most `14px` desktop or `8px` mobile inside the existing `24px` bleed, and particles eject opposite that movement. Eye particles retain straight local streaks; their collective path bends upward through eye-only lift and a lifetime-weighted steering target. Do not curve each particle stroke, because that produces the rejected comma-shaped appearance. The eye flame's outer lobes are longer and progressively bend upward while the bright core remains compact. The rejected 2D title-data Canvas is forced hidden in post-release. The existing WebGL title lens remains active after reconstruction and becomes the only visible title source: low idle flow preserves readability, while pointer pressure broadens the deformation across multiple letters and the flow front mirrors the background direction. Broken edge drag is generated by shifted glyph texture samples rather than fixed external lines. Once post-release owns the title, the older burst renderer must not call `drawTitleLens()` or write title-source opacity; this ownership guard prevents the multi-second entry flicker caused by two RAF loops toggling the same layers.
- Site-wide presentation: the homepage continues into full-width channel, current-pulse, project, and article bands instead of repeating the hero effect. Inner pages use a fixed theme header, mobile bottom navigation, cold-blue information lines, rose active states, restrained `fight.jpg` atmosphere, and page-specific list/article controls. Game now opens with a full-viewport `IMG_0116.PNG` night-scene hero before the existing arcade selector; Blog search/filtering, the Works cooking-game workbench, Games metadata/actions, About identity signals, article reading progress/share controls, and a themed 404 are functional rather than decorative-only.
- Boundary: root `kisara/` source images remain untracked and are never imported directly. The user explicitly approved publishing `kisara/IMG_0116.PNG` as the copied runtime asset `public/themes/kisara/assets/game-night-hero.png`; do not stage any other root source material without a separate request.
- Local helper: root `preview-blog.bat` runs `npm run build` first and starts `npm run dev -- --host 127.0.0.1` only after a successful build.

### Kisara Inner-Page Redesign

- Goal: rebuild Blog, Projects/Works, Games, and About/Me as four distinct interaction models inside the existing Kisara visual system, without changing the accepted homepage gate sequence or article layout.
- Status: implemented and locally validated; user visual acceptance is pending. `d1ffa62` is the rollback point before this work, and `f4776b2` is the latest functional checkpoint.
- Blog asset source: user-owned `kisara/engage kiss.png`; the source remains untracked and untouched. A reproducible offline extraction script may read it and publish only generated, compressed theme assets under `public/themes/kisara/assets/blog/`.
- Blog direction: layered city transmission intro with four separately masked visible-character layers. Entry order follows the requested front-to-back depth order, replays on every Blog entry, and degrades to a static composition for reduced motion. The DOM still stacks Sharon, Ayano, Shu, then Kisara from back to front; back-character masks are intentionally allowed to overlap already-visible front layers so occluded clothes and weapons remain intact beneath the correct foreground instead of being destructively partitioned.
- Page boundaries: Blog owns search/archive behavior; Works owns the technology-ingredient cooking game; Games owns a single-focus arcade selector; Me owns profile and interest navigation. New selectors must be page-prefixed so shared homepage and article styles remain unchanged.
- Functional commits: `9c488a4`, `5c11956`, `f066c50`, `f20b1cf`, `67a7a54`, `702ccfb`, and `39c76f9`.
- Generated Blog assets: `public/themes/kisara/assets/blog/engage-kiss-scene.webp`, `kisara-front.webp`, `shu-middle.webp`, `ayano-middle.webp`, and `sharon-back.webp`. The old full-scene `isnet-anime` union plus nearest-seed/Voronoi ownership was removed after it assigned Shu clothing, Ayano's weapon, and Sharon clothing to front layers. Curated source-size masks now live at `scripts/assets/kisara-blog-masks/`; `scripts/extract_kisara_blog_layers.py` validates and resizes those masks deterministically without modifying the source artwork or rerunning an unstable model.
- V2 extraction recovery (`2026-07-22`): the first image-heavy attempt exceeded the upstream response limit and its initial V2 files were rejected. A later bounded Arendt pass rewrote the same `*-v2` outputs and produced `engage-kiss-layers-v2-qa.png`; Blog now references only the four compressed V2 WebP layers. Their source-size PNG files and QA sheet remain local/untracked and must not be staged unless the user explicitly requests the masters.
- Blog mask/framing validation (`2026-07-22`): the cumulative V2 composition loads all four `1440x975` layers, completes its entrance animation, and retains zero horizontal overflow in the local browser. The Kisara foreground master received an additional component-aware cleanup limited to the transparent space above the left wing and long hair: disconnected foreign face/cloth fragments were removed while the connected wing edge and primary hair arcs were preserved. The refreshed WebP loaded at `1440x975`; `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed after the cleanup.
- Kisara favicon: the earlier heart-and-key badge is superseded. The active multi-resolution `.ico` is now a single centered pink heart on a fully transparent canvas, with no key, plate, or background. The layout uses `?v=heart-only` to bypass the previous `keyheart` browser cache.
- Temporary extraction tooling: SAM2 experiments were isolated under `C:\Users\a1234\AppData\Local\Temp\yuimi-sam2-venv` with model `C:\Users\a1234\AppData\Local\Temp\yuimi-sam2-models\sam2.1_s.pt`; they are not project or global Python dependencies. Ultralytics created local settings at `C:\Users\a1234\AppData\Roaming\Ultralytics\settings.json` and `C:\Users\a1234\Desktop\Ultralytics\settings.json`; do not delete them without an explicit cleanup request.

## Known Risks

- `ClientRouter`, `transition:persist`, and global event listeners can retain Kagari runtime state; cross-theme switches therefore require full navigation.
- Existing absolute resource URLs assume the GitHub Pages user-site root. A future project-page deployment needs a separate base-path migration.
- Music filenames contain spaces and non-ASCII characters; URL generation must remain encoded and verified after namespacing.
- Live2D and IP/weather services are network-dependent and remain Kagari-only.
- `npm test` currently covers registry allowlisting and path mapping only. Browser behaviors are manually verified; no browser test runner is configured.
- No `astro check` script or `@astrojs/check` dependency exists. It was not installed because this task does not authorize new tooling installation without confirmation.
- Unknown Blank paths rely on JavaScript to move from the single GitHub Pages 404 to the Blank 404. Normal Blank pages remain readable without JavaScript.
- Kisara's scroll gate intentionally uses global wheel/touch/keyboard listeners on its homepage. Future interaction work must keep browser zoom shortcuts, focused controls, restored scroll positions, reduced motion, and a visible mobile navigation path working.
- Kisara homepage scheduling must check the current gate's `data-bound` / `data-binding` state before calling the previous runtime cleanup. Reversing that order can let the initial zero-delay task finish immediately before `astro:page-load`, after which the second schedule destroys the live runtime and returns on the stale bound flag.

## Immediate Next Actions

1. Obtain user visual acceptance for the redesigned Kisara Blog, Works cooking game, Game, Me, and homepage Event 003 investigation scene, especially fine character-layer timing, the Works hero-to-counter blend, Event 003 image-edge treatment, and page-specific visual density.
2. Continue homepage or puppet-layer refinement only as a separate scoped task so the accepted inner-page baseline stays recoverable.
3. Push the baseline and Kisara commits only when separately requested.

## Test and Validation Record

- Kisara homepage `001` ambient-memory refinement (`2026-07-31`): preserved checkpoint `449232c`, then added subject-anchored depth breathing, damped fine-pointer parallax, a sparse text-ink afterglow, and intermittent capillary motion derived from the existing sorted wash nodes. Runtime inspection after the user's “less than 1%” report confirmed the first pass was attached to the correct three image layers but used nearly invisible values; the corrected pass raises blurred-layer travel to visible multi-pixel/`1-3%` motion, binds pointer input to the full section, starts the first ink after-pulse sooner, and increases only the existing node-edge ripple. Browser-computed matrices reached about `1.7%` background and `3.1%` foreground scale while the focus layer stayed near `0.1%`; a right-copy pointer probe wrote about `4.9px` foreground travel, and dense Canvas hashes changed across four pulse frames before returning exactly to the resting hash. The deterministic wash sequence remains unchanged because ambient phase/strength consume no extra random values. Intersection/visibility gates pause work offscreen, mobile/coarse-pointer/reduced-motion devices do not start it, and cleanup cancels every observer, timeout, and animation frame. `git diff --check`, `npm test` (3/3), and `npm run build` (60 pages, sitemap, 61-page Pagefind index) passed; fine user visual judgment remains pending.
- Kisara Blog/Game/Works compact-layout pass (`2026-07-28`): enlarged the Blog four-character stage by 20% and lowered it on desktop/mobile; replaced the local 2048 embed's obsolete fixed `0.64` transform with viewport-measured fitting for the current smaller arcade screen; and condensed the Works pantry, cutting board, blender, and trash into one desktop workbench viewport. Pantry ingredients now overlap as a draggable pile, the trash sits beside the scaled blender, drag ghosts are positioned at the grabbed source before their first rendered frame, blending delay dropped from `1320ms` to `980ms`, and result/remix scroll duration was shortened. The user accepted the current visual result. `git diff --check`, `npm test` (3/3), and `npm run build` passed; 42 static pages were generated and Pagefind indexed 43 pages.
- `npm test`: 3/3 Node tests passed for all theme IDs, canonical path stripping, and cross-theme article-context mapping.
- `npm run build`: passed after the current Kisara site-wide presentation pass; 36 static pages generated, sitemap created, Pagefind indexed 36 pages.
- Kisara Event 003 hidden-song flow (`2026-07-27`, session rules revised `2026-08-02`): `ココロスペアキー` is a current-document one-shot secret track rather than a twelfth normal playlist entry. Entering it snapshots the previous normal track, time, and play intent; full playback consumes the unlock and restores that state. Reload/new-document navigation clears the qualification, while returning from the lower homepage to the hero resets 003 so the archive can be investigated again. One page-lifecycle `AbortController` owns audio/window listeners so Astro navigation cannot accumulate duplicate `ended` handlers. The solved investigation state reveals a compact achievement badge; active secret playback uses the demon-pink sticker treatment and heart particles.
- Kisara demon-player sticker states (`2026-08-02`): the `kokoro-spare-key` player no longer uses the CSS triangle, pause bars, circular emblem, or mini equalizer. User sources `kisara/musicplayer1.png` and `kisara/musicplayer2.png` had baked checkerboard backgrounds and neighboring sticker fragments; the runtime RGBA assets `public/themes/kisara/assets/music-player-playing.png` and `music-player-paused.png` retain the character and white sticker edge while removing those fragments. The first crying-sticker pass used a subject model and incorrectly removed gray uniform areas while leaving a ragged upper/right edge; that mask is superseded and must not be restored. The accepted correction removes only checker pixels connected to the source canvas border, preserving the complete gray uniform, collar, bow, hair, and original sticker outline. The demon variant maps the happy sticker to `playing` and the crying sticker to paused/loading states through the existing `data-state` flow. Desktop runtime checks confirmed both state swaps and removal of the old pseudo-elements; the `390x844` layout remained `390px` wide with a `224x66px` player and no overflow.
- Kisara homepage Event 003 investigation scene (`2026-07-25`): replaced the old three-row project runway with a full-width gacha-event investigation interface built around the approved `picture.png`. The complete-ratio photo keeps its hotspot aligned to the taped-in pink-haired character; activating it draws a clue path and reveals a second control, which opens a native modal styled as a normal anime-game chapter/item unlock and displays the approved `win.png`. The custom element owns the `idle -> traced -> solved` state, modal scroll lock, Escape/backdrop cleanup, and reconnect behavior after Astro navigation. Desktop `1280x720` and mobile `390x844` checks retained equal `scrollWidth` and `clientWidth`; the mobile modal fit inside `370x757`, the Works/back return reset and rebound the interaction, and page warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 37-page Pagefind index) passed. Fine image-edge and clue placement acceptance remains with the user.
- Kisara homepage refrigerator `002` follow-up (`2026-07-25`, based on checkpoint `48e34e5`): replaced the simultaneous scan/light/text entrance with a text-free black start. One black curtain retreats left while a pre-rendered dark blurred copy dissolves over the slightly softened final image, so brightness and clarity recover without a full-screen animated blur. Replay and return-to-gate reset use an explicit forced reset frame so headings, inventory, shade, image scale, and curtain cannot retain the previous open state. The `001 -> 002` and `002 -> 003` bridges now overlap adjacent sections by `3px`, keep opaque endpoint seals, and delay decorative mixing away from the join; screenshot-row sampling reduced the former lower seam jump from roughly `36` RGB-sum units to `0-5`, while the upper join is generally `3-9` outside real content edges. Desktop and `390x844` checks retained zero horizontal overflow. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 37-page Pagefind index) passed; fine visual timing and final seam acceptance remain with the user.
- Kisara Event 003 camera-focus correction (`2026-07-25`): vertically mirrored the clue path so it leaves the hotspot toward the lower-right and moved the clue control below the hotspot on desktop and mobile. The first camera-frame pass incorrectly stacked the original edge mask, a duplicated blurred photo, horizontal/bottom fog transitions, an optical vignette, and extra rings; the user correctly identified the resulting multi-shell blur, and that approach is superseded. The current scene renders one unmasked source image only, with one restrained in-image vignette, viewfinder corners/HUD, and an image-load-plus-IntersectionObserver focus pull from blur/overscan to the final sharp frame. DOM checks confirmed zero duplicate-photo layers and no panel fog pseudo-element; desktop and `390px` layouts retained equal `scrollWidth` and `clientWidth`, the mobile clue remained below the hotspot, and warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 37-page Pagefind index) passed; fine focus timing remains for user visual acceptance.
- Kisara Game hero entrance (`2026-07-23`): the existing dark `game-night-hero.png` now enters as one full-frame asset without character cutout. A short phone-origin pink pulse leads a diagonal left-to-right image unseal; brightness, saturation, and blur settle with the reveal while the copy enters in restrained staggered timing, then the original long breathing motion resumes. The inline page lifecycle waits for image decode, replays after browser Back through `data-astro-rerun`, completes fail-safe state cleanup after `1.76s`, and bypasses the sequence under reduced motion. Desktop `1280px` and mobile `390x844` runtime checks reached `idle/playing -> complete`, retained equal `scrollWidth` and `clientWidth`, and reported no warning/error logs. `git diff --check` passed, `npm test` passed 3/3, and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index; fine timing and visual feel remain for user acceptance.
- Kisara Game hero dialogue cloud (`2026-07-23`): added the Japanese line `えっ…?! なんでこうなるの…？` in the dark area left of the character. The rejected first pass was a large oval with a triangular tail and must not be restored. The current body uses one continuously scalable cloud silhouette with a dominant central crest, smaller side lobes, and rounded lower lobes; its translucent tones and `backdrop-filter` preserve and blur the artwork behind it instead of presenting an opaque overlay. Two real blurred circles (`14px` and `10px`) form the thought-bubble tail toward the character. The cloud joins the hero entrance after the image is mostly revealed, has a narrower mobile placement, and remains immediately visible under reduced motion. Final runtime checks reached `complete`, cleared all page/copy blur, retained equal `scrollWidth` and `clientWidth`, and reported no warning/error logs. `git diff --check` passed, `npm test` passed 3/3, and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index; fine compositing and placement remain for user visual acceptance.
- Kisara Game dialogue typography (`2026-07-23`): replaced the rejected two perfectly centered subtitle-like rows with five independently positioned phrase/punctuation pieces. `えっ` rises and leans left while `…?!` kicks upward in the opposite direction; `なんで`, `こうなるの`, and `…？` follow a shallow uneven baseline with restrained size and rotation differences. The accessible Japanese reading order remains unchanged, and the cloud/tail geometry is untouched. `git diff --check` passed, `npm test` passed 3/3, and the 36-page build completed with sitemap and Pagefind output; fine comic timing remains for user visual acceptance.
- Kisara Game dialogue scale refinement (`2026-07-23`): enlarged the desktop dialogue cloud from roughly `205px` to roughly `245px` while shifting its left anchor from about `90px` to about `50px`, keeping the right edge near the character's hair rather than pushing into the hero copy. Vertical padding, line gap, and type scale rose proportionally so the cloud reads as a scene element instead of a small annotation. Mobile width increased only from `158px` to `168px` with a smaller left/top adjustment to limit face coverage. `git diff --check` passed, `npm test` passed 3/3, and the 36-page build completed with sitemap and Pagefind output; final size remains for user visual acceptance.
- Kisara Game dialogue composition correction (`2026-07-23`): the first comic-layout pass pushed `…?!` too far upward and `…？` too far downward, making both punctuation groups read as detached stickers; that arrangement is superseded. The current layout keeps each sentence visually continuous: the first line has one restrained left-up lean with tightly rejoined punctuation, while the second line follows a shallow readable arc with only small baseline changes between its three pieces. Type scale and inter-line spacing were raised to use the enlarged cloud more fully. `git diff --check` passed, `npm test` passed 3/3, and the 36-page build completed with sitemap and Pagefind output; fine visual acceptance remains with the user.
- Kisara Game punctuation split (`2026-07-23`): treating `…?!` and `…？` as two indivisible styled blocks still made the punctuation read like pasted badges and is superseded. Each line now separates the ellipsis from the terminal marks while preserving DOM reading order: `…` stays close to the phrase to hold the pause, while `?!` and `？` receive their own smaller position/rotation adjustments. The pieces remain nowrap inside each dialogue line. `git diff --check` passed, `npm test` passed 3/3, and the 36-page build completed with sitemap and Pagefind output; fine typography remains for user visual acceptance.
- Kisara Game oversized punctuation composition (`2026-07-23`): the first line now splits `?` and `!` into independent elements instead of keeping `?!` bound together. The rejected flexible-spacer pass pushed both marks to the far edge and left a meaningless hole across the cloud. The current composition keeps the raised question mark and lower opposing exclamation mark immediately after `えっ…`, while the longer second sentence is enlarged, centered, and allowed to use the cloud's full inner width. Mobile keeps the same composition with restrained scaling. `git diff --check` passed, `npm test` passed 3/3, and the 36-page build completed with sitemap and Pagefind output; final scale and spacing remain for user visual acceptance.
- Kisara route-cache correction (`2026-07-23`): the persistent `ClientRouter` was not dropping the Game CSS; the layout had instead marked every brand/header/footer route as `data-astro-prefetch="load"`, causing the homepage to cache all route documents immediately. A locally open home document could then retain an older Game document after a CSS edit until Game itself was refreshed. Those eager attributes were removed, and later Works testing proved the global `hover` default could still cache an unseen route before an edit completed. The site-wide default is now `tap`, which prefetches immediately before activation while preserving ClientRouter swaps and uninterrupted Kisara audio. Existing already-open tabs require one refresh to discard snapshots cached under the old strategies.
- Kisara Works kitchen redesign (`2026-07-23`): replaced the dark operations-console presentation with a light image-led kitchen/workbench composition built around the user-provided `kisara/work.png` (`2200x1392`). The untouched source remains untracked; the published runtime copy is `public/themes/kisara/assets/work-kitchen-hero.webp` at roughly `201 KB`. The hero uses a transparent page header, warm cream/pink/yellow sampling, right-shifted character framing, reduced lower masking to retain the knife/board area, and a restrained image/copy reveal. The lower page keeps all existing filters, project selection, ARIA state, and ClientRouter cleanup, but presents them as ingredient tabs, a project menu, and one light preparation sheet rather than another dark console. Game dialogue text gained a bounded subpixel blur (`0.52px` desktop, `0.42px` mobile) without changing the accepted cloud or punctuation layout. Desktop and `390x844` runtime checks retained zero horizontal overflow; fine crop, color, and blur acceptance remain with the user.
- Kisara tide/chain/camera/lens refinement (`2026-07-23`): restored only the blue-gray title's accepted old striation texture, clipped the chain leader glow fully behind the foremost link to remove the outward white thread, increased the shared base-plus-eight-scene breathing motion with displacement-derived overscan, and raised the eighth scene's blade reveal scale to `1.052`. The space-lens shader replaced the wide global rotation and four corner vortices with a bounded radial inverse map, compression shell, and low-amplitude asymmetric transverse flow without extra texture samples. `git diff --check` passed, `npm test` passed 3/3, and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index. A fresh local runtime reported the WebGL lens state as `ready`, no warning/error logs, and equal `scrollWidth`/`clientWidth`; fine visual acceptance remains with the user.
- Kisara ambient/rail refinement (`2026-07-23`): raised the eighth scene's blade-driven scale from `1.052` to `1.1` and added fourteen CSS-only pink ambient motes behind the title. The first rail redesign introduced three handoff markers, repeating/segmented fills, and stage-dependent ellipse/ring/block cores; the user rejected that visual language, so it is superseded and must not be restored.
- Kisara continuous rail correction (`2026-07-23`): removed the three handoff marker nodes and all repeating/tick-like rail fills. The rail is again a continuous `1px` track with a `2px` completed line, a fixed angular contract needle, a small continuous endpoint, and one smooth glow pass. Gate stages retain only palette/glow changes and restrained local effects; reconstruction uses a continuous blue-white-pink sweep instead of pixel cells. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed; fine visual acceptance remains with the user.
- Kisara `001 -> 002` bridge-flash correction (`2026-07-23`): the completed `fight` gate shell was still `position: fixed` above the document while its deferred reset waited for a passive `scroll` event, allowing a compositor frame to escape at the first post-opening band. Directional input now finalizes that reset before a forward scroll would carry the `180px` sampled edge past `-24px`; the existing passive-scroll path remains as a fallback for restored/programmatic positions. The signal, project, and latest bands use `z-index: 2` plus `isolation: isolate` as a paint-order guard. Local SKIP replay retained the live bridge at the `001` top, changed the shell from `fixed` to `absolute` before `002` entered, reported no warning/error logs, and kept the signal band at `z-index: 2`. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed; transient visual acceptance remains with the user.
- Kisara Game hero and superseded reverse-bridge attempt (`2026-07-23`): published the explicitly requested `IMG_0116.PNG` copy as `public/themes/kisara/assets/game-night-hero.png` and rebuilt Game as a full-viewport image-led first screen followed by the existing arcade selector. Desktop `1280x720` and mobile `390x844` checks loaded the `1260x709` asset, retained the character face, and reported zero horizontal overflow; this Game work remains current. The same commit's `is-returning-to-gate` reverse-bridge overlay was later disproved by the real viewport handoff and is superseded; do not restore it.
- Kisara reverse-opening handoff stabilization (`2026-07-23`): the visible seam was caused by the live portal closing before the sampled edge had actually left the viewport, the visual shell switching between `fixed` and `absolute`, and the opening section's `-2px` top margin exposing another layer. The scene transform now lives on an inset `.kisara-gate-visual-camera`, while `.kisara-gate-visual-shell` stays fixed and is clipped by the gate. The opening margin is `0`; forward cleanup follows the measured `scroll` position rather than predicted wheel delta; upward entry is captured before the `180px` bridge enters view; and `returnToGate()` keeps the portal active while `001` retraces the SKIP motion, resetting the gate only after it exits. Fresh runtime checks kept the shell at the viewport bounds, kept the portal alive through the connection region, and produced no page warning/error logs. The user reported that the seam appears fixed. `git diff --check` passed, `npm test` passed 3/3, and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.
- Opening-edge seam correction (`2026-07-22`): the gate atmosphere is now represented once as a shared continuation layer instead of being baked independently into every scene texture. Memory-scene soft-light and vignette treatment is calculated against the original full gate coordinate space and baked into sampled pixels, avoiding the dark boundary introduced when those gradients were recomputed inside the `180px` strip. The intended `24px` overscan now wins CSS specificity and is clipped rather than compressed. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed; fine visual acceptance remains with the user.
- Scene-aware opening edge and repeatable warning warmup validation (`2026-07-22`): the opening continuation now owns ten independently sampled scene textures (base, five memory scenes, three transformation scenes, fight), renders them through an idle queue, follows the same per-scene opacity timeline as the gate, and freezes the current scene state during damped entry into the next section. Browser runtime checks at about `42.8%` showed the clash texture dominant over the attack transition; at `100%` transformation intro the detail texture was dominant, with all ten textures ready and no page errors. Warning prewarm now reruns before each charge intro and after visibility recovery, activates the real ring/shard/spark/core filter paths, and defers the two WebGL lens draws on the cross's first visible frame. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Kisara inner-page redesign validation (`2026-07-21`): Blog desktop/mobile layouts retained zero positive horizontal overflow and replayed the four-character entrance in the order Kisara, Shu, Ayano, Sharon, including after browser Back via `pageshow`. Works technology filtering reduced the queue to the expected UE5 record. Game keyboard/button navigation changed the active title and scene without clipping on mobile. Me interest tabs switched content correctly and kept the avatar square at `220x220` desktop and `170x170` mobile. The accepted homepage gate and an article containing seven Expressive Code blocks remained intact, and browser warning/error logs were empty. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.
- Seal-chain motion and interlock validation (`2026-07-21`): the outer frame remains stationary after input settles, reverses along the same equal-arc-length path, and no longer splits into independently rotating sections during upward input. The closed loop uses an even link count, and flat connector width is derived from the neighboring upright-link inner spacing with split front/back depth passes. The user visually accepted the resulting connector/upright interlock. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed. Browser warning/error logs were empty; Statsig network timeouts belonged to browser-control telemetry, not the site.
- WebGL liquid-title validation (`2026-07-20`): desktop replay completed the full charge, enchantment, automatic black-hole release, strong pointer travel in both directions, damped section entry, blue reset, and a second complete release. The CSS title source returned to opacity `1` with the lens cleared on section entry, then the lens retook ownership after the second release. A focused 60-sample transition audit found zero upward source-opacity jumps after `is-post-release`; source opacity moved monotonically `1 -> 0`, and lens activity changed only once from off to on. Mobile `375x844` reported equal document/client widths with no positive horizontal overflow and retained a readable centered title. Page warning/error logs were empty; repeated Statsig timeouts were browser-control telemetry only. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Charge-flight continuity validation (`2026-07-20`): the segmented quadratic/cubic experiment preserved direction but still produced visible impulses because launch acceleration restarted and the lagging tail crossed a separate path seam at the U-turn; that approach is superseded and retained only in rollback commit `1725af6`. The current implementation uses one quintic head route, a 96-sample arc-length lookup, smootherstep launch timing, and a quintic gather segment matched through position, tangent, and acceleration. Desktop `1280x720` frame sampling showed continuous travel through launch, the U-turn low point, and title approach. Mobile `375x844` kept `scrollY` locked at `0`, reported zero positive horizontal overflow, and retained the complete route. Page warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Kisara gate runtime restoration (`2026-07-20`): `cc15be9` extracted title-data drawing into `drawTitleDataCell()` but retained two loop-only `continue` statements, causing `SyntaxError: Illegal continue statement` before the global wheel listener registered. Replacing them with `return` restored the captured gate input without changing the visual parameters. A fresh browser load had no warning/error logs; downward input kept `scrollY` at `0` while advancing `CONTRACT 000% -> 009%`. The full desktop sequence reached enchantment, automatic release, `SIGNAL RELEASED / CONTINUE`, damped scrolling to `scrollY 720`, blue reset on return, and a working second charge cycle. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.
- Post-release eye-flow refinement (`2026-07-20`): the first attempt curved each short particle stroke and was rejected because individual particles read as commas. The final implementation restores straight particle streaks, stores the emitting source kind, and bends only eye-particle velocity toward the upward axis over its lifetime. Desktop browser checks in both pointer directions showed one continuous upward-curving stream, longer outer flame lobes, no warning/error logs, and an intact full gate/release sequence. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.
- Subject-protection and automatic handoff validation (`2026-07-20`): added `fight-distortion-protect.svg`, a feathered local alpha mask aligned with the current `fight.jpg` cover crop. The background WebGL lens now suppresses source and destination distortion, caustic, refraction sheen, and black-hole horizon darkening inside the marked face/body/handgrip region while leaving the independent `Kisara` title lens active. Checkpoint commit: `1164fb4`. The release timeline then changed so the blade-gloss stage can hand off automatically after its fade: the black/white warning cross, collapse, black hole, detonation, and particle rain now run without another wheel event. Browser validation reached `AUTOMATIC RELEASE / STAND BY` mid-sequence and `SIGNAL RELEASED / CONTINUE` after completion with `scrollY` locked at `0`, zero horizontal overflow, and no page warning/error logs. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index. The timing follow-up was committed as `f20ab43`.
- Full-screen warning refinement validation (`2026-07-20`): removed the rejected full-viewport horizontal/vertical shutter gradients that appeared as one large white cross. Scene desaturation, contrast, and brightness now apply directly to the background/title compositing layers, while a variable elliptical aperture closes toward the shared title/black-hole origin. The procedural warning cross moved into the top-level impact layer so it stays sharp without lifting the whole title above the black-hole Canvas. Desktop `1280x720` and mobile `375x844` replay measured title/cross center error below `1px`; mobile reported equal `scrollWidth` and `clientWidth`, the complete release still reached `SIGNAL RELEASED / CONTINUE`, and browser warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Continuous warning-spin validation (`2026-07-20`): replaced the three accumulated build/flash/release rotation terms and phase-driven angular wobble with one autoplay-clock curve spanning the entire visible warning. The first cycle produced 153 measured visible frame steps from about `-7deg` to `185deg`; the post-release return-to-blue second cycle produced 156 measured steps across the same range. Both cycles reported zero stalled steps below the chosen motion threshold and zero reverse steps, while the reduced positional shake remained confined to the flash/release pulse. The second full release still reached `SIGNAL RELEASED / CONTINUE`, retained zero horizontal overflow, and produced no browser warning/error logs. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Post-reconstruction interaction validation (`2026-07-19`): desktop completed the full automatic release twice with an intervening damped transition, blue reset, and second cycle. The post layer activated only at `is-burst-complete`, reset to opacity/offset `0` on section entry, and returned without stale particles. Rightward travel sampled a smooth monotonic `6.04 -> 10.67 -> 12.61 -> 13.42 -> 13.74px`; the mirrored title strip moved from the opposite edge without per-event restarts. Mobile `375x844` used a `360x844` Canvas, capped parallax at `8px`, retained zero positive horizontal overflow, and kept both eye highlights aligned after cover cropping. Page warning/error logs were empty; after the final idle-frame cap, `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Title-erosion/singularity-flow validation (`2026-07-19`): browser-replayed repeated desktop release cycles plus a complete `375x844` mobile release. The post-release title Canvas measured `812.5x254.3` desktop and `342.2px` wide mobile, remained at zero positive horizontal overflow, parked at both directional edges, and visibly retained square cells outside the `Kisara` strokes. The black-hole core no longer exposed fixed arm counts or global rotation; the remaining event well is assembled from offset irregular dark layers and short inward fragments. Page warning/error logs were empty. After the final dark-layer offset adjustment, `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) all passed.
- Sitemap: verified that `/themes/blank/` routes are excluded.
- Resource boundary: verified no legacy `/assets/` or `/music/` runtime references and no Blank import/reference to Kagari internals or assets.
- Desktop browser: verified both theme home pages, Blank article deep link, canonical/noindex, zero horizontal overflow, two-way switching, query/hash preservation, refresh persistence, browser back/forward, and no console warnings/errors.
- Mobile browser at 390x844: verified both themes fit without horizontal overflow and both visible theme controls remain reachable.
- Kisara browser validation: confirmed intermediate downward input raises the mask while `scrollY` remains `0`; normal-charge particles visibly fan across the full gate; the second stage sweeps the title gloss before pressure, expansion, shock rings, and the full-screen burst; and upward input first retracts the burst before lowering the red tide. At both stages settled to `100%`, the next downward wheel input scrolls into the full-width content band. Desktop and `390x844` mobile layouts have no horizontal overflow, and the browser console has no warnings or errors.
- Latest Kisara browser validation: confirmed the wake ribbons and trailing particles are emitted from the beam nose toward the rear, charge-dependent floating motion is continuous, the overcharge beam reforms centrally without extending its length, thickens while shaking, dissolves into particle rain, and then releases damped section scrolling. Returning upward from the next section resets the gate to the blue `CONTRACT 000%` state on desktop and `390x844` mobile.
- Current Kisara warning validation: at `1280x720` and `461x551`, the procedural rays extend from the title center, rotate without forming a fixed X logo, collapse into the core, and hand off to a center shock ring. At `OVERCHARGE 064%`, warning opacity, ray length, and ring opacity were all `0` while the reformed beam had begun at partial opacity, confirming the warning cannot overlap the beam regrowth. Both viewports had no positive horizontal overflow, and the browser warning/error log was empty.
- Latest Kisara refinement validation: at `1280x720` and `461x551`, charge movement stayed continuous during rapid input, the gloss opacity reached `0` before the warning variables became non-zero, performance blur reached about `4.8px` before warning build, the beam tail remained composited from transparent to opaque across its rear third, and detonation reached stronger scale/angle/glow values without positive horizontal overflow. After the first `CONTRACT 100%`, the status changed to continuously varying glitch text; both viewports released damped document scrolling after `100%`, and page console logs remained empty.
- Latest Kisara feedback validation (`2026-07-19`): after a full charge/retract/recharge cycle, the beam pose restarted from a clean initial phase; at the warning stage all four title text layers reported the active performance blur while the procedural cross remained separate. The transformed beam anchor followed the cross center on desktop and `390x844` mobile, and after the 5.2-second automatic release the beam opacity was `0`, the gate had no positive horizontal overflow, and the final canvas frame showed no visible wake residue. Downward release reset to blue before the next section completed, and upward return landed at `CONTRACT 000%` without replaying the red scene. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and Pagefind index. The browser page log remained empty; one Statsig telemetry timeout was tool-side only.
- Persistent-particle/cinematic-release validation (`2026-07-19`): desktop `1265x712` and mobile `375x844` browser passes confirmed that charge packets fade at both wrap boundaries, wake exhaust separates from the nose and becomes bounded ambient drift, the automatic release grows an asymmetric rear-weighted body instead of restoring the local meter, and three event-driven burst waves hand off to slower particle rain and residual motes. Both viewports had no positive horizontal overflow; the performance blur recovered to `0px`, the final cue reached `SIGNAL RELEASED / CONTINUE`, and the desktop viewport was restored after mobile testing. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.
- Pink-black-hole validation (`2026-07-19`): desktop `1265x712` continuous frame sampling confirmed the ordered sequence of sharp accretion/lensing build, dark-core compression, white-pink inversion, curved bipolar jets, elliptical shock rings, slingshot fragments, and lingering particle rain. The title and background remained depth-blurred while the top-level burst Canvas stayed sharp above them. Mobile `375x844` checks confirmed the continuous charge beam and black hole remain inside the gate with no positive horizontal overflow. The final cue reached `SIGNAL RELEASED / CONTINUE`.
- Beam-detail/performance validation (`2026-07-19`): desktop `1280x720` confirmed the charge beam retains one continuous envelope while clipped helical shards circulate inside it and a primed wake immediately opens into a denser, longer rearward plume. The black-hole endpoint now skips transparent detonation drawing, clears inherited Canvas shadow blur before residual particles, fades and releases the old wake with the beam, paints residual rain at about 30 FPS, and stops Canvas/layout work once only the low-frequency glitch label remains. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.
- Site-wide presentation validation (`2026-07-19`): desktop checks covered the homepage continuation, Blog, Projects, Games, About, Article, and 404. Blog keyword search returned two `Hexo` signals and the Projects UE5 filter reduced the list to its matching record. Mobile `375x844` checks covered the homepage continuation and all major page types; Projects filtering remains horizontally scrollable with its native scrollbar hidden, About and 404 have no positive horizontal overflow, and long hand-written article `pre` blocks scroll only inside the code surface. The page log contained Vite debug messages only, with no warnings or errors. `git diff --check` reported no content errors, `npm test` passed 3/3, and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.
- Momentum-wake/screen-impact validation (`2026-07-19`): desktop testing completed a full charge, automatic black-hole release, damped transition to the next section, return to a blue `0%` gate, and a second charge to `60%` without the previous post-release beam twitch. Timed screenshots at about `85%`, `90%`, `93%`, and `96%` confirmed full-gate gravity/vignette/chromatic feedback, sharp black-hole depth, inversion flash, coherent release waves, and recovery into residual particles. Mobile `375x844` completed the release with no positive horizontal overflow, and desktop/mobile page logs contained no warnings or errors. `git diff --check` reported no content errors, `npm test` passed 3/3, and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index. One Statsig request failure came from the browser-control runtime, not the site.
- Seamless-phase/collapse validation (`2026-07-19`): desktop `1280x720` completed repeated gate cycles with the beam driven by one closed master phase. A second-cycle `43%` pose sample after a four-second zero-state idle measured a maximum 55ms step of about `1.95px`; after holding partial charge for another five seconds and nudging to `50%`, the maximum step was about `1.88px`, with no accumulated-time phase jump. Timed automatic-release captures confirmed a distinct inward black-hole compression, a high-opacity radial background-lensing/shockwave stage, restrained sub-pixel screen displacement, final detonation, and complete blur/warp recovery. The gate visual stayed at an identity transform, desktop and mobile `375x844` had zero positive horizontal overflow, and page warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed. A Statsig timeout was emitted by the browser-control runtime only.
- Terminal-crush/refraction validation (`2026-07-19`): desktop `1280x720` timed captures confirmed the black hole retains a deep blue-violet interior before its disk and horizon accelerate into a much smaller white-hot singularity. The peak shockwave used two masked background copies with independent radial scaling and opposite rotation/skew; displaced artwork remained visible above the atmosphere layer, no rectangular filter boundary appeared, and both warp opacities recovered to `0`. Desktop and mobile `375x844` completed the automatic release with no positive horizontal overflow, `--kisara-performance-blur: 0px` at rest, `SIGNAL RELEASED / CONTINUE`, and empty warning/error logs. The abandoned SVG displacement filter was removed after browser testing exposed a rectangular compositor clip at large lens radii.
- Segmented-aftermath validation (`2026-07-19`): desktop `1280x720` timed captures at roughly `86%`, `90.6%`, `95%`, and `98.3%` confirmed a slower five-stage release, a feathered small-core void instead of a hard black disk, visible fold/torsion refraction in both outer thirds, a stable middle background zone, title rupture/dissolve without reverse-morph playback, and a multi-wave aftermath with echo rings, residual ribbons, fragments, and rain. The title source-opacity samples progressed from `0` at `98.7%` through `0.0313`, `0.5709`, and `0.9714` to `1` at completion while blur fell to `0`, confirming reconstruction rather than a single-frame snap. Mobile `375x844` used a reduced side-field scale, retained zero positive horizontal overflow, completed at `SIGNAL RELEASED / CONTINUE`, restored the source title to `1`, and cleared both WebGL canvases. Browser page warning/error logs were empty; Statsig timeouts were emitted by the browser-control runtime only. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Title-clarity/block-reconstruction/gloss validation (`2026-07-19`): the blue title begins at `8.8px` blur, reaches about `2px` when the red tide completes, and returns to full clarity after reconstruction. The final background pass expands the same `9px` block vocabulary used by the title from the rupture center across the viewport, then resolves each cell back into the clear scene. During the blade-gloss stage, desktop and mobile `375x844` computed styles confirmed the tide parent and gloss layer use `filter: none`, the red layer alone retains `blur(2px)`, and both generated layers retain `background-clip: text`; mobile had no positive horizontal overflow. A fresh page produced no console errors. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Ball-lightning/performance validation (`2026-07-19`): desktop captures at the wider blade pass and automatic-release `84%-93%` stages confirmed a broad silver-white metal face, narrow bright core, moving internal lightning branches, a breathing plasma sphere, deeper moving void, orbital foreground depth, and nonlinear collapse into the existing rupture. Mobile `375x844` retained the blade inside the title mask, used a `360x844` burst buffer for a `360x844` CSS surface, had no positive horizontal overflow, and produced no page errors. A complete release, damped transition, return to blue, and second charge reached `34.286%` with `burst=0%` and no stale particles or state. Canvas/WebGL dimensions remained stable under the throttled `ResizeObserver`; adaptive quality now trims existing excess particles and restores at least `0.88` quality on a new cycle. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Plasma-core/morning-gloss validation (`2026-07-19`): desktop `1280x720` and mobile `375x844` captures confirmed the restored independent gloss layer uses the `5b1d2b7` slanted polygon, white-to-pink vertical gradient, screen blend, sharp `filter: none`, explicit `background-clip: text`, and the original `0.01 -> 0.3` sweep/fade timing. The slanted rectangle is no longer visible outside the title strokes. The later black-hole stage retained the procedural plasma field without symbol-like radial branches. Mobile used a `360x844` burst buffer, had no positive horizontal overflow, and completed at `SIGNAL RELEASED / CONTINUE` with no page errors. A CDP requestAnimationFrame sample during the mobile black-hole stage kept the browser's full refresh cadence. Desktop completed the release, damped transition, blue reset, and second charge to `34.286%`. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Opening-transition validation (`2026-07-21`): desktop `1307x882` reproduced the reported lower-edge leak during the red chain stage, then confirmed that moving the sampled continuation to second-section `top: 0` with `overflow: hidden` removes the exposed source-image line. Mobile `390x844` retained zero positive horizontal overflow. A blue-state `SKIP` kept `--kisara-fill` at `0%` and fight opacity at `0` throughout the damped move, while gate reset now occurs only after the first screen is offscreen. A fresh reload added no warning/error entries. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.
- Compressed-singularity/reverse validation (`2026-07-21`): desktop `1280x720` traced the automatic release from blade handoff to completion in `5573ms`; warning began near `71ms`, singularity at `539ms`, collapse at `947ms`, detonation at `2833ms`, and reconstruction at `4603ms`. The pure warning stage held a `16.6ms` p95 frame interval with a `16.7ms` maximum after duplicate presentation work was removed. Mid-enchant upward input reversed the visuals and gate rail continuously from roughly `55%` to `44%`, while the next-section return rail moved from `10.9%` to `0%` without switching to document progress. During strong collapse the base scene carried about `3.57px` blur while the protected character layer remained at `0.98` opacity; WebGL source/destination protection use the same feather and transparent handoff, avoiding an opaque mask rim. Reconstruction wash samples had zero backward opacity steps. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed. Statsig network timeouts came from the browser-control runtime, not the site.
- Accessibility: verified Kagari context menu opens with `Shift+F10`, focuses its first item, closes with Escape, becomes `visibility:hidden`, and returns focus outside the hidden menu.
- Search: Pagefind query returned the canonical root article once and did not expose a duplicate Blank result.
- Production 404: verified `/themes/blank/not-a-real-page/?x=1#lost` redirects to the Blank 404 with the original URL encoded in `from`.

## Independent Review Record

- A resource review: no missing runtime resources, old-path references, Blank/Kagari boundary violations, or accidental use of untracked user materials. Adopted its warning to keep `public/themes/fuyukawa-kagari/` in the eventual commit scope.
- B note review: found stale dirty-state, asset-path, status, test, and next-action entries. All were corrected in this note.
- C logic review: found the missing Blank unknown-path fallback, inaccessible/hidden-focus Kagari theme menu, and broken history semantics from `location.replace`. All three were implemented and browser-verified. Its browser-test-suite suggestion remains deferred because no browser runner exists.
- D final review: found incomplete Blank keyboard focus handling and missing migration/third-theme recovery notes. Both were corrected. A targeted re-review found no remaining blocker or important issue and marked the implementation deliverable. The no-JavaScript 404 limitation remains documented; the root 404 now also exposes a direct Blank link.
- Kisara interaction review: found missing skip behavior, browser zoom interception, focused-control keyboard interception, hidden mobile navigation, narrow-viewport menu clipping, scroll-restore drift, and alternate-theme 404 matching gaps. These were addressed before final validation.

## Engineering History

### 2026-07-18 Kisara interaction prototype

- Registered `kisara` at `/themes/kisara/` with independent routes, layout, pages, CSS, mobile navigation, theme controls, canonical/noindex behavior, and themed 404 handling.
- Implemented a color-only homepage gate: `Kisara` starts blue and fills red diagonally from bottom-left to top-right as downward input accumulates.
- Replaced direct per-wheel percentage steps with a reversible target/visible/velocity state model, a damped frame-time animation loop, a soft red mask edge, and a narrow moving tide crest.
- Kept all root `kisara/` image sources untouched and outside the published theme.
- Created the architecture rollback point `d4e2a29` before starting the prototype.

### 2026-07-18 Kisara image transition and energy meter

- Copied the explicitly selected `kisara/fight.jpg` into the theme-owned public asset slot without modifying the original source directory.
- Replaced the single gate background with independent cold and fight image layers. The existing reversible tide progress now controls opacity, blur, scale, saturation, and brightness continuously in both directions.
- Rebuilt the contract meter as a growing energy beam with a Canvas wave, moving particles, rays, and a glowing front; the animation pauses when the document is hidden and degrades to a static frame for reduced-motion users.
- Removed the gate pseudo-decoration/background arrangement that could expose a rectangular dark frame around the masked title; title mask sizing and alpha behavior are now explicit.

### 2026-07-18 Kisara tapered beam refinement

- Removed the ruler-like track, tick marks, DOM fill bar, and detached front orb after visual review.
- Reworked the Canvas display into a cone-like beam whose width increases continuously from its origin to a pointed energy nose. Particle count, spread, and speed increase with charge, while the front glow remains integrated with the beam rather than clipped at the canvas edge.
- Removed the remaining gate center axis so the background no longer presents the original cross-shaped frame.

### 2026-07-18 Kisara overcharge burst sequence

- Added a second reversible captured-input stage after the red title tide reaches 100%; normal document scrolling remains locked until this stage also settles at 100%.
- Added a diagonal blade-gloss pass across the title, progressively stronger beam shake, bloom, and expansion, followed by late-stage viewport shock rings and flash.
- Added a gate-sized Canvas particle field. Normal charging now sends radial streaks from the beam nose toward the full viewport, while detonation increases particle density, travel distance, speed, and brightness.
- Kept reverse input symmetrical: upward input retracts the detonation and gloss before it can lower the original title tide.
- Created safety rollback commit `d3333c0` before the final full-screen particle and staged-detonation refinement.

### 2026-07-18 Kisara wake and release refinement

- Replaced the remaining radial charge particles with two head-connected wake ribbons and rearward streaks so the diffusion reads as motion generated by the energy nose rather than decoration attached to the meter.
- Added a continuous charge-dependent float before overcharge, then made the post-100% sequence dissolve and reform the beam in the center, tilt it upward, increase thickness and shake, blur the surrounding gate, and end in a full-screen particle rain without extending the beam's length.
- Added damped snapping from the completed gate to `#kisara-opening`, and back to the top with a blue reset when the user scrolls upward from the next section.
- Re-ran `npm test` (3/3) and `npm run build` (36 pages, sitemap, Pagefind) after the final Canvas/CSS adjustments.

### 2026-07-18 Kisara cinematic timing refinement

- Replaced the hard symmetric wake strokes with curved, low-alpha ribbons and age-based turbulent streaks that inherit their origin and rearward flow from the energy nose.
- Added visible idle instability and charge-weighted multi-frequency float/rotation/jitter so the beam reads as increasingly difficult to contain from the first charge.
- Moved the pre-blast cross into the title DOM layer. The gloss, cross build, white-core flash/release, beam reform, pressure, detonation, and post-impact rain now occupy separate ordered phases; the beam cannot reappear before the cross has finished.
- Reused the transformed beam head as the burst anchor, delayed rain until after detonation, and made the surrounding blur recover completely during the rain.
- Replaced distance-proportional scroll stepping with a delta-time damped spring and revalidated `npm test` (3/3), `npm run build` (36 pages, sitemap, Pagefind), desktop interaction, zero horizontal overflow, and an empty warning/error console.

### 2026-07-18 Kisara procedural pre-blast warning

- Replaced the full-rectangle dual-gradient cross and its rectangular shadow with a DOM effect assembled from a black/white core, four unequal tapered rays, broken shards, small sparks, and a release-only shock ring. No SVG or bitmap warning asset is used.
- Changed the warning motion to a fast center-out extension, progress-driven rotation and strobe, late nonlinear collapse, core flash, and radial release pulse before the energy beam reforms.
- Fixed two broad title selectors (`.kisara-title span` and `.kisara-gate.is-complete .kisara-title span`) that were clipping the effect backgrounds to empty text and overriding every child transform; both now target only the direct text layers.
- Revalidated `npm test` (3/3), `npm run build` (36 pages, sitemap, Pagefind), desktop and mobile-sized layouts, phase ordering, zero positive horizontal overflow, and an empty browser warning/error log.

### 2026-07-18 Kisara charge continuity and runaway status refinement

- Smoothed the normal-charge instability through a frame-time state so rapid wheel input no longer chains the strongest per-segment shake into repetitive vertical twitching.
- Moved the procedural warning after the title gloss and added a short background-blur prelude before the warning appears.
- Added a Canvas destination-in tail mask that fades the narrow rear third of the energy beam to transparent without changing its head position.
- Reworked detonation motion with nonlinear reach, faster head-anchored particle/ring travel, stronger beam shake, and larger late-stage thickness/brightness.
- Replaced the second `OVERCHARGE 000%→100%` counter with persistent deterministic glitch output that continues updating after the first contract reaches `100%`.
- Revalidated `npm test` (3/3), `npm run build` (36 pages, sitemap, Pagefind), desktop `1280x720`, mobile `461x551`, zero positive horizontal overflow, release scrolling, and an empty page console warning/error log.

### 2026-07-19 Kisara persistent particle and cinematic release refinement

- Replaced modulo-recomputed wake streaks with bounded stateful particles that move through jet, deceleration, ambient drift, edge response, and TTL fade phases.
- Rebuilt the normal-charge beam from one continuous tapered envelope, braided internal flow, and boundary-faded packets so visible texture no longer snaps back to a first frame or exposes a hard rear splice.
- Replaced the post-warning restored meter with an asymmetric full-screen plasma body, open electric edges, an internal spine, pre-blast compression, directional rush lines, three event-driven burst waves, shock arcs, shards, slower rain, and residual afterglow.
- Extended automatic release from 5.2 to 6.8 seconds and applied width-aware release thickness so the cinematic body remains readable on narrow mobile screens.
- Revalidated `npm test` (3/3), `npm run build` (36 pages, sitemap, Pagefind), desktop `1265x712`, mobile `375x844`, zero positive horizontal overflow, final blur recovery, and restored desktop viewport state.

### 2026-07-19 Kisara pink black-hole release refinement

- Replaced the remaining multi-shell normal-charge beam with one continuous teardrop envelope and phase-continuous internal filaments, removing both visible shell seams and packet-loop snapback.
- Extended wake particles into explicit jet, inertial-coast, and ambient-drift phases with longer TTL, weaker drag, curl-driven separation, and late fade so exhaust can escape and remain in the gate before disappearing.
- Replaced the final restored plasma body with a lightweight Canvas 2D pink black hole inspired by accretion/lensing hierarchy rather than importing the reference project's WebGL/LUT assets. The sequence now uses far/near accretion arcs, an occluding dark core, photon ring, lensing paths, orbit particles, tangential slingshot waves, curved bipolar jets, shock rings, spiral rush lines, and residual rain.
- Moved the burst Canvas out of the negative-z background container, placed it above the title, excluded it from the common performance-blur selector, and kept the title/background blurred during black-hole formation.
- Extended automatic release from 6.8 to 8.2 seconds and revalidated desktop `1265x712` and mobile `375x844` framing with no positive horizontal overflow.

### 2026-07-19 Kisara beam detail and release performance

- Restored small helical shards inside the normal-charge beam with phase-boundary fades, so fragments circulate with the internal flow without adding another beam shell or a visible loop reset.
- Added an initial primed wake burst, higher rearward launch speed, wider side separation, longer jet/coast phases, and a larger bounded pool. Escaped particles still transition into ambient drift, while overcharge now fades and clears the old wake before the black-hole phase.
- Removed the post-impact hot path by skipping the completed transparent detonation layer, resetting inherited `shadowBlur` before burst/rain particles, suppressing invisible ambient work, and throttling completed residual particles to roughly 30 FPS. After all Canvas particles expire, the frame loop only updates the runaway glitch label at low frequency and performs no Canvas or layout repaint.
- Revalidated `git diff --check`, `npm test` (3/3), `npm run build` (36 pages, sitemap, Pagefind), and desktop `1280x720` charge/release behavior.

### 2026-07-19 Kisara site-wide presentation pass

- Replaced the placeholder homepage continuation with full-width contract introduction, channel navigation, current-pulse, project-preview, and latest-article sections that extend the gate palette without repeating its cinematic effect.
- Rebuilt the Kisara layout shell with page-aware navigation states, a compact fixed inner-page header, clearer context-menu actions, footer status/navigation, and a reachable mobile bottom bar.
- Reworked Blog, Projects, Games, About, Article, and 404 into page-specific information systems using cold-blue rules, rose activation, dense editorial type, and restrained fight-art atmosphere instead of generic glass-card stacks.
- Added functional Blog search/category filtering and results count, Projects technology filtering, Games metadata/actions, About identity and live-signal blocks, article reading progress/copy-link behavior, and themed recovery actions on 404.
- Fixed narrow-layout overflow for hand-written article `pre` blocks and hid the native scrollbar on mobile filter tracks while preserving horizontal scrolling. Revalidated `git diff --check`, `npm test` (3/3), `npm run build` (36 pages, sitemap, Pagefind), desktop major pages, and mobile `375x844` Projects/About/404 with no warning or error logs.

### 2026-07-19 Kisara momentum wake and screen-impact refinement

- Replaced the single bright internal flow with back-layer dark helices, front-layer luminous helices, travelling compression rings, speed-responsive fragments, and a beam-nose shock flare while retaining one continuous tapered envelope.
- Added a low-pass beam-head velocity tracker. Wake particles now inherit source motion, then progressively receive rearward exhaust force and lateral separation; primed particles use the same integrated trajectory instead of linear side placement.
- Prevented post-release animation multiplication by guarding one active energy RAF epoch. Reset, visibility, and return-to-gate paths now cancel RAF/timers and restart pose, particle, and beam-head clocks from a clean epoch.
- Extended black-hole influence beyond its Canvas object through differential background pull, full-gate vignette/chromatic response, screen shake, ambient-particle attraction, and broad lensing rings.
- Reorganized the explosion into two coherent major-axis waves and reduced orbit, burst, rain, path, highlight, DPR, and lifetime budgets. Completed release updates now use delayed scheduling rather than a 60 Hz no-op RAF loop.
- Revalidated desktop full release plus second-cycle charge, mobile `375x844` release and overflow, empty page warning/error logs, `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind).

### 2026-07-19 Kisara seamless phase and collapse-shockwave refinement

- Replaced frequency-times-accumulated-clock pose equations with independent integrated motion phases. Charge-dependent frequency changes now preserve oscillator phase after long idle periods and across repeated gate cycles.
- Moved all normal-beam internal motion onto one `2π` master phase with integer harmonics. The front/back helix is blended segment by segment, while rings and fragments fade at their periodic travel boundary, removing the visible loop restart.
- Split the black-hole endpoint into a deeper inward collapse, a particle-free full-screen shockwave, and delayed final detonation. Added inward streaks, compression rings, and a central compression glow without increasing persistent particle limits.
- Added a radial-masked duplicate of the cold/fight backgrounds as a local lensing layer. The layer expands with the shockwave while the main wallpaper container remains fixed, replacing prolonged viewport shake and preventing exposed outer edges.
- Revalidated desktop repeated-cycle/long-idle continuity, desktop release staging, mobile `375x844` full release, zero positive horizontal overflow, full blur/warp recovery, empty page logs, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind).

### 2026-07-19 Kisara WebGL lensing and pure-black horizon refinement

- Replaced the superseded CSS background-copy/radial-mask warp with one full-screen WebGL2 fragment pass that cover-samples the theme backgrounds per pixel. Unsupported WebGL2 and context-loss paths keep the Canvas release effects without restoring the old sliced warp.
- Converged on a capped monotonic primary-image lens with a short smooth influence envelope. Removed the experimental signed secondary-image fold, shader chromatic triple sampling, and oversized lens radius after browser captures showed that combination turning the character face into a circular portal.
- Matched the shader background atmosphere to the page, reduced full-screen performance blur, and kept the `Kisara` title in the same blur choreography while the lens Canvas remains a continuous undivided scene.
- Made the event horizon optically empty through both shader and Canvas masks, reduced the bright photon rim, and reapplied a full-radius solid-black mask after foreground disk highlights so no texture or light streak can cross the center.
- Reduced the lens shader from roughly eight texture reads per fragment to two. Desktop runs at up to CSS resolution/60 FPS; mobile remains capped near 30 FPS with a `0.68` render scale and pixel budget. The lens Canvas clears and becomes transparent after release or while hidden.
- Revalidated `git diff --check`, `npm test` (3/3), `npm run build` (36 pages, sitemap, Pagefind), desktop `1280x720`, mobile layout metrics at `375x844`, zero positive horizontal overflow, WebGL ready/active/clear lifecycle, and no browser warning/error logs. Final visual acceptance remains with the user.

### 2026-07-19 Kisara subject-aware lensing refinement

- Historical/superseded by `139ca19`: the per-feature face/hair/body/wing/weapon regions described below are no longer the active background-warp architecture.
- Added a face-protection region derived from normalized coordinates in the current `fight.jpg`, then converted it through the same cover crop used by the WebGL scene. The protection therefore follows the face from desktop center framing to the right-shifted mobile crop instead of relying on fixed viewport pixels.
- Reweighted the shader so the face interior receives almost no radial pull or shock displacement, while an elliptical subject halo around the hair, bow, scarf, shoulders, wings, and weapon receives stronger lensing. The title rectangle is supplied separately as another priority region.
- Added three clipped `Kisara` title refraction layers for upper, middle, and lower bands. Their shear, drift, scale, opacity, and retained source-text blur follow the black-hole pressure, keeping the word visibly bent but readable instead of smearing it into horizontal blocks.
- Revalidated the full automatic release at desktop `1280x720` and mobile `375x844`: facial proportions remain stable, title/subject-edge distortion is visible, the mobile lens buffer remains `255x574`, horizontal overflow stays zero, and browser warning/error logs remain empty. `npm test` passed 3/3 and `npm run build` generated 36 pages with sitemap and Pagefind.
- The normalized protection coordinates are intentionally tuned to the current `public/themes/kisara/assets/fight.jpg`; replacing that artwork with a differently composed image requires retuning the face region.

### 2026-07-19 Kisara side-field lensing and continuous title refinement

- Replaced the three clipped CSS title replicas with one transparent WebGL title texture rendered from Canvas2D. The source title crossfades into the texture only during the black-hole pressure phase, so the word is sampled as one continuous surface rather than as independently stretched bands.
- Superseded the stronger center-collapse title map after browser captures showed it pulling letters into disconnected fragments. The active title field uses low-frequency vertical bend, horizontal ribbon shear, a restrained orbital tangent, and a small shock displacement; it preserves the full word silhouette while still visibly flexing around the release center.
- Removed the fight-art-specific face, hair, body, wing, and weapon uniforms from the background shader. The active field now uses a horizontal three-zone mask: the middle third returns the original UV exactly, while only the left and right thirds receive radial pull, slow side sweep, and shock displacement through a soft boundary.
- The foreground black-hole Canvas remains sharp and centered above the unchanged character area. The background lens and title lens each clear after release or while hidden; unsupported WebGL2 keeps the original DOM title and Canvas release fallback.
- Revalidated the full automatic release at desktop `1280x720` and mobile `375x844`: the central character region remains geometrically stable, the word stays readable while bending, both lens canvases return inactive after release, performance blur returns to `0px`, and positive horizontal overflow remains zero. Fresh-page warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-19 Kisara segmented black-hole aftermath refinement

- Replaced the single linear automatic-release sweep with a 10.6-second keyframed timeline that dwells through field lock, deep collapse, terminal-crush hold, rupture, and aftermath.
- Rebuilt the event horizon from broad feathered absorption gradients, a smaller deep center, subtle breathing, and broken rim arcs, removing the hard opaque ellipse that read as a pasted black shape.
- Increased outer-third WebGL radius reach and added inward tension, low-frequency folds, torsion, refraction sheen, and shock displacement while preserving the exact original UV across the middle third. Narrow screens use a lower side-field scale because the subject spans more of those outer zones.
- Extended the title WebGL pass with rupture displacement and procedural dissolve. The original title now crossfades into the lens, stays hidden during breakup, and returns through a late blurred reconstruction instead of visibly relaxing like an elastic layer.
- Extended the endpoint with three bounded particle waves, longer-lived fragments, expanding echo rings, residual magnetic ribbons, slower rain, delayed lens recovery, and a quieter residual settle. Existing canvas lifecycle and low-frequency completed-state scheduling remain intact.
- Browser-validated desktop `1280x720` and mobile `375x844`, title reconstruction samples, cleared WebGL lifecycle, zero positive horizontal overflow, and empty page warning/error logs. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-19 Kisara clarity, block reconstruction, and sharp gloss refinement

- Added a progress-linked title clarity curve: the cold-blue word begins deliberately soft, becomes increasingly clear with the red tide, retains a small amount of blur before the black-hole release, and finishes fully sharp after reconstruction.
- Replaced the earlier radial/full-screen reconstruction interpretation with the title shader's square-cell language. A `9px` block field now expands from the rupture center, briefly pixelates the affected scene, and resolves outward into the clear background.
- Split the red tide rendering into two generated text layers under the same diagonal tide mask. The red gradient alone receives `--kisara-title-blur`; the metal blade gloss is composited separately with no filter, so it stays crisp without escaping the `Kisara` glyphs.
- Corrected CSS specificity and avoided a `background` shorthand reset that initially reapplied parent blur and changed `background-clip` back to `border-box`.
- Browser-validated the gloss at desktop and mobile `375x844`, confirmed fresh-page console output is clean, and passed `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind).

### 2026-07-19 Kisara ball-lightning core and adaptive performance refinement

- Added a translucent sharp red title core beneath the existing blurred tide, widened the moving metal blade into a silver-white face with a narrow bright edge, removed residual crest blur, and reduced the title WebGL texture/drop-shadow softness.
- Replaced the broad repeated elliptical void fills with a small offscreen Canvas ball-lightning renderer. Its near-black sphere contains continuously moving pink/blue/white branches, plasma lobes, a breathing contour, a drifting deep void, Fresnel edge light, and short outward tendrils that tighten during collapse.
- Kept the sphere on the existing release anchor and composited it between rear and foreground accretion layers, preserving the established title, cross, lens, collapse, shockwave, detonation, and aftermath timing.
- Removed per-frame Canvas and WebGL layout reads. Canvas dimensions and meter geometry are cached, WebGL renderers resize only when marked dirty, and a throttled `ResizeObserver` covers font/container changes that do not emit `window.resize`.
- Lowered the burst Canvas DPR ceiling and added render-cost-based quality control for particle density, pool limits, shadow blur, and offscreen-core resolution. Existing excess particles are trimmed when quality drops, while a returning gate restores at least a `0.88` baseline so later runs do not visibly shrink.
- Browser-validated desktop and mobile blade/core stages, a complete automatic release, return-to-gate, second-cycle charge, stable buffers, zero positive horizontal overflow, and empty page error logs. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-19 Kisara procedural plasma core and morning gloss restoration

- Replaced the readable radial/curved lightning paths inside the black-hole sphere with a bounded low-resolution procedural electric field. Warped pink/blue ridges, mist, moving charge nodes, a dark moving void, and intermittent outer tendrils now fill the sphere without forming pasted lightning symbols.
- Kept the field at `64-96px` depending on viewport and adaptive quality, then scales it into the existing offscreen core. This removes most per-frame `Path2D` allocations and keeps the expensive field work bounded while preserving the established black-hole timeline.
- Initially tested a broad tide pseudo-element gloss, then superseded it after visual review. Restored the Beijing-time July 19 morning implementation from `5b1d2b7`: a dedicated `.kisara-title-gloss` span with the original white/pink vertical gradient, slanted polygon clip, screen blend, glow, and `0.01 -> 0.3` sweep timing.
- Added explicit sharp-filter and text-clipping overrides because later title reconstruction work applies blur to every title span, and a `background` shorthand reset the inherited text clip. The gloss now uses `background-image` so the morning geometry remains constrained to the glyph mask.
- Browser-validated desktop and mobile gloss/core stages, a complete automatic release, damped transition, blue reset, second-cycle charge, mobile full-refresh frame sampling, stable buffers, no positive horizontal overflow, and empty page error logs. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-18 Multi-theme implementation

- Added shared content/data helpers and theme registry under `src/core/`.
- Moved the existing frontend intact into `src/themes/fuyukawa-kagari/` and its fixed assets into `public/themes/fuyukawa-kagari/`.
- Added independent Blank pages and routes under `src/themes/blank/` and `src/pages/themes/blank/`.
- Added persistent context-preserving theme switching to both context menus plus visible touch/keyboard controls.
- Browser testing found and fixed a non-executable Astro conditional 404 script and a hidden-menu focus restoration issue.

### 2026-07-18 Pre-migration survey and route decision

- Verified before migration that all existing pages ultimately used one theme-heavy `BaseLayout.astro`, while `global.css` mixed reset, page styles, theme visuals, and responsive rules.
- Verified before migration that no prior theme or dark-mode registry existed.
- Rejected same-URL client-only theme replacement because a static Astro build cannot select complete server-rendered layouts from `localStorage` without duplicate DOM/CSS or visible replacement.
- Chose root `fuyukawa-kagari` plus prefixed `blank` routes to preserve current public URLs and provide fully independent static documents.

### 2026-07-19 Kisara integrated procedural singularity core

- Removed the remaining complete offscreen sphere composite from the black-hole center. The release no longer draws a standalone ball, SVG-like shell, Fresnel circle, or the extra three elliptical horizon arcs.
- Kept one bounded `68-108px` procedural electric field only as moving plasma texture. Its irregular alpha envelope, central exclusion, spiral ridges, and pink/blue currents are generated per frame and composited directly into the main release Canvas.
- Rebuilt the visible center from overlapping absorption clouds, five tapered inward currents, and a very small animated irregular throat. These elements share the accretion-disk transform and collapse timing, so the core grows out of the existing scene instead of reading as a pasted asset.
- Preserved rollback checkpoints for each visual direction: `0668d7c` (field without sphere), `b670763` (cloud aperture), `059ac5b` (deeper well), and `9d5c22e` (spiral inflow).
- Revalidated desktop and mobile `375x844` black-hole frames, zero positive horizontal overflow, empty browser warning/error logs, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind). Final visual acceptance remains with the user.

### 2026-07-19 Kisara post-reconstruction highlight and parallax pass

- Added a dedicated post-release Canvas behind the title and above the reconstructed fight artwork. It projects two eye points and one pale-blade point from normalized `fight.jpg` coordinates through the live cover crop, avoiding viewport-percentage stickers that drift off the artwork.
- Added soft integrated pink blooms, anime-style tapered light trails, and bounded lifetime particles. Movement particles remain in screen coordinates after emission and eject opposite the current background travel, so they separate naturally from all three moving sources.
- Added a `Kisara` text-only pixel-data strip. A direction change starts one mirrored journey; its position and fade derive from the actual damped background displacement, so repeated pointer events in the same direction cannot restart the strip.
- Composed parallax additively with the existing black-hole fight shifts, dynamically capped it against the background layer's physical bleed, and reset the new RAF, Canvas, particles, CSS variables, and direction state during visibility changes, section entry, restored navigation, and return-to-blue flows.
- Fixed two stale `lightningCoreLastPaintTimestamp` reset references left after the procedural singularity rename.

### 2026-07-19 Kisara ember eyes and persistent pixel-band refinement

- Removed the `YUIMI LAB / INTERACTION CHANNEL 03` gate label that crossed the character's eyes after reconstruction.
- Rebuilt the title data effect as a square-cell band with an independent roughly 1.2-second journey. It now travels opposite the background parallax, reverses from its current position, and remains fully visible at the left or right text edge instead of fading out at arrival.
- Recalibrated the third post-release source to the pale blade's right edge in `fight.jpg`. Both eyes now use larger breathing blooms, a layered rising flame core, and directional flame tongues; the blade uses an elongated edge flare rather than a circular point.
- Increased the bounded post-release particle budget and weighted emission toward the eyes. All three sources share one mirrored direction, launch upward, and retain stronger age-dependent lift after ejection so their free drift continues diagonally upward instead of resembling falling tears.
- Slowed the horizontal background response to match the longer data-band read time while retaining the existing physical bleed limits and no-black-edge guarantee.
- Revalidated `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind). Browser validation covered a complete desktop release, bidirectional pixel-band travel and edge parking, a complete `375x844` release, zero positive horizontal overflow, and empty page warning/error logs.

### 2026-07-19 Kisara title-data erosion and singularity-flow refinement

- Replaced the clipped CSS title grid with an oversized Canvas. It rasterizes the live title typography once per resize, samples the title gradient into deterministic `9px` data cells, and computes three neighboring mask rings so selected cells can continue outside the glyph silhouette.
- Ported the full-screen reconstruction language into the moving title band: hash-driven cell timing, animated noise, front/settle phases, sampled block color, sparse blue edge cells, and white front heat. Direction reversal still follows the existing damped parallax journey and the band remains parked at either title edge.
- Removed the black-hole field's global rotation, angular spiral families, five dark radial arms, and rotating rim starts. Replaced them with Cartesian turbulence, fixed-anchor short inward fragments, radial cloud pulsing, reduced orbit angular acceleration, stationary contour harmonics, and offset layered dark shapes.
- Browser-validated repeated desktop cycles and a complete `375x844` cycle. The title cells extend beyond the strokes without horizontal overflow, the mobile bottom navigation remains visible, and page warning/error logs are empty. The untracked source-material boundary remained intact.

### 2026-07-19 Kisara charge-beam enchantment prelude

- Rebuilt the local charge envelope around a moving centerline with faster early thickness growth, a softer shoulder, a tapered energy-knot nose, and stronger continuous internal helix alignment.
- Added three depth-separated exterior energy strands plus partial halos that become visible after 50% charge without introducing another DOM layer or dependency.
- Added an independent 2.7-second charge-intro state between 100% fill and the existing blade-gloss phase. The full-screen burst Canvas morphs the beam from its meter pose into a U-shaped cubic flight, preserves the moving internal filaments, carries wake-particle velocity with the head, and lands on the live title center.
- Added a dedicated `Kisara` enchantment span clipped to the glyphs. The impact expands through the letters, leaves a restrained animated pink/blue charge, then fades as the original blade gloss begins.
- Updated reduced-motion, skip, reset, reverse-scroll, visibility, and repeated-cycle paths so the new state cannot strand input or reuse an old flight clock.
- Browser-validated desktop charge, U-turn, impact, blade handoff, complete black-hole release, and a `375x844` mobile charge/impact pass. The mobile layout retained zero positive horizontal overflow, a reverse-and-recharge cycle restarted the intro, and page warning/error logs remained empty.
- Final verification passed `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind).

### 2026-07-20 Kisara charge handoff and renderer refinement

- Removed the visible phase restart by keeping the energy oscillator unbounded during a session and preserving its phase across gate resets while still zeroing the frame timestamp to prevent hidden-time catch-up.
- Replaced the local nose's sampled shoulder/semicircle join with two tangent-matched cubic curves. The rod's upper and lower wave boundaries now enter one continuous rounded front without the two diagonal connector lines.
- Changed the automatic-intro trigger from a fully settled `99.9%` spring to a `99.5%` visual threshold that atomically locks charge, target, and velocity at `100%`. The full-screen handoff inherits the live meter transform before detaching, removing the pause and ghosted pose change between canvases.
- Parameterized the launch as one shared path for both endpoints. The head leads while the tail follows `0.46` path units behind, so the left edge leaves the meter instead of staying pinned while the body turns back toward the title.
- Reused per-strand helix/orbit samples across front/back passes and replaced hundreds of per-segment `stroke()` calls with four depth-batched paths per pass. A normal-charge fast path now skips unrelated gate-wide CSS/WebGL presentation writes.
- Browser validation covered desktop `1280x720`, mobile `375x844`, `77%` charge, the full-meter handoff, tail departure, U-turn progression, and zero positive horizontal overflow. A 3-second desktop CDP sample at `77%` reduced `TaskDuration` from about `1.33s` to `0.65s` and `RecalcStyleDuration` from about `0.79s` to `0.05s`; these are local comparative samples, not a formal benchmark. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-20 Kisara post-release flow and title-mask integration

- Added rollback checkpoint `a20782e` for the post-release highlight flow. Eye and pale-blade particles now follow an eight-segment angular spring chain: the source turns first, older particles follow delayed segments, and direction changes travel through the stream as an upward whip instead of splitting into two binary exhaust branches.
- Rebuilt the final parallax title-data composite into two passes. Interior cells render first and are clipped with the rasterized `Kisara` alpha mask via `destination-in`; neighboring ring cells render afterward with distance-weighted opacity, preserving controlled pixel erosion beyond the glyph edge.
- Changed the title-data Canvas from additive `lighter`/screen blending to sampled-color `source-over` compositing, reduced its oversized bounds, lowered it beneath the sharp gloss layer, and softened the external glow so the blocks read as title material rather than a separate transparent overlay.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed. Exact final-stage visual acceptance remains pending user review in the local preview.

### 2026-07-20 Kisara liquid-title refraction pass

- Historical/rejected: this pass superseded the square-cell title presentation but was itself rejected after visual review because the still-visible CSS title plus a half-transparent 2D Canvas read as a plastic film, while four explicit post-mask curves read as four unrelated thin lines.
- Replaced pixel sampling with a reusable white glyph mask, a quiet rose material base, aligned `fight.jpg` sampling, and fine overlapping strips whose continuous phase creates liquid refraction without a rectangular overlay or visible scanline gaps.
- Added a damped pressure signal derived from actual pointer velocity, direction-change travel, and the existing particle whip. It drives a mirrored slanted pressure wave, restrained pink/blue contract veins, and four short post-mask energy threads that may cross the glyph edge without outlining the Canvas bounds.
- Initialized the liquid title at low idle strength immediately after release, reset all pressure/data state on section entry and blue-gate return, and removed the obsolete frequent pixel-read context option.
- Browser validation covered desktop pointer travel in both directions, section entry/reset, a second complete release cycle, and `375x844` mobile with zero positive horizontal overflow. Page warning/error logs were empty; `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-20 Kisara WebGL liquid-title takeover

- Kept `ba82ee4` as the rejected-overlay rollback point and removed the 2D title-data Canvas from the active final-stage rendering path.
- Extended the existing WebGL title-lens shader with final-flow uniforms driven by the established post-release phase, pressure, direction, flow-front, and parallax states. The shader now displaces the glyph texture itself, adds moving caustic/contract traces, modest chromatic separation, and broken sampled edge drag without fixed external lines.
- During the 880ms post-release intro, the CSS source title fades monotonically while the WebGL title becomes the sole visible glyph. Idle flow remains readable; pointer motion broadens the pressure field across multiple letters and visibly bends/compresses the word in the direction opposite the background travel.
- Fixed an entry flicker discovered during user testing: the residual burst RAF and post-release RAF were both writing title-lens/source state for several seconds. `updateBurstPresentation()` now stops calling `drawTitleLens()` once `postReleaseActive` owns the title.
- Desktop checks covered idle flow, both pointer directions, section entry, blue reset, and a second full release. A 60-sample entry audit recorded zero source-opacity reversals and one expected lens activation. Mobile `375x844` retained zero positive horizontal overflow. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-20 Kisara reconstruction light restoration and flame-tip emission

- Corrected the reconstruction-light interpretation after visual review: the target is not extra exposure. The processed fight background is darkened by the atmosphere overlay and by the WebGL scene mix, while `fight.jpg` itself is deliberately bright.
- Added a clean fight-art layer above the dark atmosphere. A feathered radial mask shares the black-hole reconstruction center and easing, so the original image brightness is restored only inside the expanding data circle and remains fully restored in the post-release state.
- Changed the WebGL reconstruction cells to sample the raw fight texture instead of the darkened scene sample. This keeps the square-cell transition and the CSS reveal on the same image-level brightness rather than compensating with `brightness()`.
- Eye particles now reuse the three live outer-flame lobe tips as their spawn positions, begin on the late segment of the existing flow chain, and use a shorter birth fade. The particles therefore detach from the moving pointed flame edge instead of appearing at each eye's central bloom.
- Browser validation covered desktop mid/late reconstruction frames, full original-brightness restoration, parallax, return-to-blue reset, and a complete `375x844` release with no positive horizontal overflow. Page warning/error logs were empty; `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-20 Kisara warning-to-singularity handoff refinement

- Compressed the procedural warning cross from roughly `1.48s` to about `0.65s` while keeping its rotation on one uninterrupted wall-clock curve.
- Moved black-hole ingress into the warning cross's final contraction and changed its opacity build to a fast ease-out. The singularity is therefore already established at the shared title-center impact point when the final cross rays disappear, instead of beginning a separate slow growth afterward.
- Started the restrained gravity blur and lens ingress slightly before the cross finishes, then shortened the monochrome focus tail so the warning hands control directly to the collapse stage without an empty visual pause.
- Rebalanced the remaining eight-second autoplay keyframes so collapse, shockwave, detonation, reconstruction, and particle rain retain their detail after the shorter warning.
- Browser validation covered the shortened warning frame sequence, a complete release, next-section entry, blue-gate reset, and a second warning cycle. The second cycle retained cross/lens overlap, `SIGNAL RELEASED / CONTINUE`, and zero positive horizontal overflow. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-20 Kisara staged singularity entrance

- Replaced the single `blackHoleOpacity` entrance switch with separate core, disk, orbit-particle, and full-screen field curves. The central singularity can now take over before the accretion disk and particle system appear.
- Added a short implosion bridge behind the DOM warning cross: a contracting white-pink nucleus, an inward-moving elliptical ring, and eight bounded deterministic fragments converge on the existing title-center impact point.
- Rebuilt the disk entrance as a horizontal throw rather than a uniform scale-up. Its radius, thickness, opacity, halo, front/back bands, lens arcs, and highlights unfold from the core on the same bounded phase, with a brief bright ignition ellipse masking the handoff.
- Delayed orbit-particle emission and the broad gravity shade until their own entrance phases, and extended the monochrome focus tail only through disk ignition so color restoration no longer lands on the same frame as the shape change.
- During development, a temporary `blackHoleFade` declaration-order error stopped Canvas drawing; it was corrected before validation. Fresh reloads after the fix produced no new page errors.
- Browser validation covered aligned entrance frames, the complete eight-second release, next-section entry, blue reset, and a second staged entrance. Both cycles reached `SIGNAL RELEASED / CONTINUE` with zero positive horizontal overflow. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-20 Kisara warning overexposure refinement

- Added a short-lived local exposure layer centered on the existing warning impact point. It uses a feathered radial mask and a bounded grayscale/brightness/contrast backdrop filter, so the scene clips locally around the cross instead of receiving another full-screen white wash.
- Added broad screen-blended bloom behind the four rotating cross rays and strengthened the procedural core's white/pink light only during the warning peak. The glow follows the existing uninterrupted cross rotation and does not add a separate rectangular cross asset.
- Added a thin elliptical shock ring with a bright inner echo and restrained dark outer edge. Its size, thickness, and opacity are driven by the existing cross build/release phases and fade before the staged black-hole field takes ownership.
- Browser playback reached the complete release state on repeated runs with no positive horizontal overflow. The current browser supports the localized backdrop filter; no new page error appeared after the previous fixed `blackHoleFade` timestamps. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-20 Kisara reconstruction-to-liquid handoff

- Removed the plain-red title interval between full-screen data reconstruction and the final WebGL liquid state. The root cause was that reconstruction restored the CSS source title to full opacity while the post-release liquid pass applied a second smoothstep to an already eased `880ms` intro.
- Started the liquid shader during the final `58%` of the reconstruction sub-phase. The old gravity, rupture, and dissolve parameters fade out on the complementary weight while `finalFlow` rises, so the same title-lens Canvas morphs from reconstructed data into the liquid material without changing owners on a blank frame.
- Faded the CSS title source against the liquid handoff and made the completed post-release renderer begin at full title ownership. The highlight/particle Canvas still keeps its original restrained fade-in; only the title material handoff is immediate.
- Browser validation captured reconstruction, blended handoff, and completed liquid frames. Immediately before ownership switched, CSS title opacity was approximately `0-0.0032`; the second cycle peaked at only `0.3828` while the liquid Canvas was already active, then settled at `0`. Both cycles reached `SIGNAL RELEASED / CONTINUE`, page logs were empty, and there was no positive horizontal overflow. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-20 Kisara fragmented warning shockwave

- Rejected the complete-ring warning treatment after browser review showed that the CSS ring, cross inner ring, and two Canvas ellipse passes combined into thick concentric white outlines with a pasted-on target appearance.
- Replaced the warning shell with three independent, non-closing conic-gradient arc fragments. Each fragment has its own radius offset, rotation, elliptic compression, opacity, and radial mask; a restrained soft pressure trail and one short white-hot center flash replace the previous full radial wash.
- Removed the DOM cross-ring element and made the four warning rays fade as the fragment wave expands. The monochrome scene is now deliberately darkened so the center and fragments remain the only strong highlights instead of turning the fight artwork into a broad white flash.
- Replaced the two later Canvas full-ellipse loops with deterministic short arc groups. Line widths are bounded near `0.7-2px`, and the fragments use limited glow instead of closed `lighter` rings.
- A failed intermediate fragment used dark normal-blended sectors at a radius near `1800px`, which appeared as rectangular black wedges inside the viewport. The final implementation bounds the DOM wave near the center, uses light-only screen-blended fragments, and keeps user source materials untouched.
- Desktop browser validation covered the warning peak, the later detonation, complete next-section release, blue reset, and a second same-tab warning cycle. The second cycle retained the same fragment size/state, no closed white rings or black sectors returned, and page warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-21 Kisara warning cross strike rhythm

- Split the warning cross into an independent three-beat action clock instead of inheriting the broad smooth interpolation used by the full eight-second release sequence.
- The cross now enters near full visibility within roughly `36ms`, completes a full `360deg` high-speed rotation in about `224ms`, holds its completed angle for about `96ms`, then retracts in about `160ms` with a short `12deg` reverse bite and a concentrated core/shard flash.
- Cross rays, shards, sparks, opacity, scale, and core light now share the same strike/hold/retract envelope. The warning arcs and singularity retain their existing timeline, so visual ownership transfers immediately after the cross retracts without extending the total autoplay duration.
- Browser frame sampling measured the first cycle at `98.842deg / 0.875 opacity` near `180ms`, `249.445deg / 0.879` near `267ms`, and the completed `342deg` endpoint (starting from `-18deg`) at `390ms`; the cross was effectively gone by `577ms`. A complete reset and second cycle produced the same `342deg`, full-ray hold state and `330deg` retracted endpoint. Page warning/error logs were empty; `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-21 Kisara protected distortion seam blending

- Traced the visible character-mask outline to a displacement discontinuity: the solid protection path forced the subject to zero motion while adjacent pixels resumed substantial lens displacement across only the SVG's narrow blur halo.
- Kept the existing `fight-distortion-protect.svg` unchanged. The space-lens shader now expands its protection weight with bounded axial and diagonal samples, preserves a fully stable core, and eases exterior distortion in through a wider computed guard band.
- Replaced the transition band's single interpolated UV sample with a conditional dual-sample blend between the stable scene and softened displaced scene. Most fully protected or fully distorted pixels still use one scene sample; only the feather band pays for both, limiting the additional shader cost.
- Destination-mask protection now prevents displaced samples from folding back across the subject edge, and caustic, horizon, refraction, and alpha effects share the same softened distortion weight so secondary effects cannot redraw the old hard boundary.
- Browser playback confirmed WebGL remained in the `ready` state with the lens active through collapse. Screenshots around `2.35s` and `3.0s` of autoplay retained a stable face/body/weapon core while the surrounding hair, wings, and background continued to distort without the previous frame-like seam. `npm test` (3/3), `npm run build` (36 pages, sitemap, Pagefind), and `git diff --check` passed. Exact FPS sampling was unavailable in the browser's read-only evaluation surface; visual playback showed no new fallback or obvious hitching.

### 2026-07-21 Kisara detached blade warning redesign

- Superseded by the later `d112f11` restoration pass; retain this section and `260ac46` only as rollback references for the rejected detached-blade direction.
- Replaced the rejected regular cross, complete rings, broad exposure wash, and dot sparks with one procedural strike assembly: four unequal black/silver/pink blade traces, four small fragments, and an irregular near-black core.
- The blades no longer share a rigid center axle. Each uses a different orbit angle, radius, local tilt, size, and vertical offset; the existing high-speed action clock rotates the detached array, then a single fold offset pulls every trace into the black core before the Canvas singularity takes over.
- Kept the warning inside the existing eight-second release timeline. No independent timer or animation loop was added, and the black-hole ingress still begins at the established handoff boundary.
- Rejected a full-layer interpolated `invert()` after browser frames showed a gray-white flashbang intermediate. The final warning keeps the scene dark and monochrome, raises contrast briefly, and uses two bounded diagonal difference-blend cuts plus a focused center pulse for the negative-image strike.
- Rollback checkpoints are `7d6c61b` (first blade replacement) and `a91f64e` (dark contrast / segmented negative pass).
- Browser validation covered desktop `1280x720`, mobile `375x844`, complete release, next-section transition, blue reset, and a second same-tab warning cycle. Both viewports retained zero positive horizontal overflow, warning variables reset to zero, and page warning/error logs stayed empty. Final verification passed `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind).

### 2026-07-21 Kisara July 20 warning restoration and exposure pass

- Used `f754c06` from `2026-07-20 20:13:10 +0800` as the exact visual baseline instead of reverting whole files. Restored the old procedural DOM assembly: one small broken ring, four unequal black/white tapered rays, four shards, four sparks, and the concentric black/white/pink core.
- Kept the current eight-second autoplay, subject-protection mask, WebGL lensing, black-hole renderer, reconstruction, and post-release interaction. The warning alone now starts at `144ms`, finishes its eased `360deg` turn by `344ms`, holds for about `56ms`, and retracts by `536ms`; black-hole ingress overlaps the final retraction instead of growing after an empty pause.
- Removed the rejected detached blade, difference-cut, and large masked exposure presentation from the active DOM. The warning still darkens/desaturates the scene for focus, but overexposure now raises brightness and contrast on the real gate visual. Only the compact procedural core and short flare draw white light, so no ellipse, rectangle, or full-screen white overlay boundary remains.
- Rollback checkpoints are `260ac46` for the detached-blade version, `556a90e` for the first restored-cross pass, and `d112f11` for the final original-scene exposure treatment.
- Browser validation covered repeated desktop `1280x720` releases, next-section entry, blue reset, a same-tab second cycle, and a complete mobile `375x844` release. Desktop and mobile retained zero positive horizontal overflow, the warning handed off at the title center, release completed at `SIGNAL RELEASED / CONTINUE`, and page warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-21 Kisara interlocking seal-chain pass

- Replaced the rejected energy-bar/flat-chain direction with three Canvas seal chains: one shallow outer loop plus two alternating weave paths that pass in front of and behind the live `Kisara` title.
- The outer loop is no longer a deep ellipse around the title. Its front and back arcs are compressed into a shallow perspective band inside the glyph bounds; the back arc is slightly narrower/smaller and renders first on the lowest Canvas layer, while the front arc renders last above every weave chain.
- Chain entry now travels from outside the path with Canvas-edge and path-distance feathering, removing the hard crop at the left boundary. Cold and hot metal sprites are cached separately and cross-faded with a moving heat front, so the chain no longer switches to red in one frame.
- Tightening removes slack through a direction-aware take-up wave and one bounded lock impulse instead of repeated whole-path scaling. Rupture opens one-link gaps, moves each side as a coherent weighted segment, keeps only the outer loop's split-link pieces, and uses restrained local sparks instead of independently scattered links, a heart icon, or a white energy ball.
- Rollback checkpoints for this pass are `e95f128`, `fce7b00`, `347ab07`, and `2efba36`. The final shallow-depth refinement passed `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind); the in-app preview retained zero positive horizontal overflow and no page warning/error logs.

### 2026-07-21 Kisara glyph-adaptive weave height

- Fixed the weave paths using one full-word cap height for every crossing. `rebuildTitleChainLayout()` now reads each `Kisara` glyph's Canvas `actualBoundingBoxAscent` and `actualBoundingBoxDescent`, maps the metrics onto the existing title bounds, and blends neighboring glyphs into seven local anchor bounds.
- `resolveTitleChainPath()` now applies each chain definition's insets against the local anchor height. The `i/s` and `a/r` arches therefore sit at their surrounding lowercase height instead of inheriting the tallest glyph, while Catmull interpolation keeps the transitions continuous and the established front/back draw order unchanged.
- Browser metrics confirmed the old mismatch (`K: 122`, `i: 133`, `s/a/r: 88` ascent units). Same-tab reload reached `INNER BIND / INTERLOCKING`, both chain canvases remained visible, and positive horizontal overflow stayed at zero. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-21 Kisara rupture and contract-heart restoration

- Traced the weak rupture presentation to two concrete issues: broken-ring pieces were only drawn for the outer frame, and their dark metal outline inherited `screen` compositing, which visually erased most of the fragment body. Broken pieces now reuse the cached chain-link metal sprites for all three chains under normal compositing, then add only the hot fracture edge and sparks with `screen` blending.
- Reworked the break motion into a short staggered snap with stronger tangent recoil, normal kick, and visible half-link rotation. A later three-link group wave travels from each break point toward the chain ends, keeping neighboring links coherent while letting the final chain mass separate and extinguish instead of fading as one rigid strip.
- Restored the love motif using the procedural heart geometry from historical checkpoint `4b294ff`, but did not restore its rejected diamond, oversized glow, or sticker-like afterglow. Three break-point tethers press a compact contract heart into the title center; the mark gains a central crack, separates into two clipped halves, and emits a bounded set of directional fragments.
- Current rollback point before this pass is `738e0e8`. Same-tab mobile preview showed the snap, heart formation, split, and grouped chain breakup with zero positive horizontal overflow. A 2.4-second animation sample recorded 301 frames, `p50 5.6ms`, `p95 16.7ms`, and `max 22.3ms`. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed; exact visual acceptance remains pending user review.

### 2026-07-21 Kisara immediate chain crush and fine shatter

- Removed the 100% handoff pause. The wheel event that raises `targetProgress` to full now calls `startChargeIntro()` in the same frame, so the gate no longer waits for the damped fill spring or another wheel input before beginning the seal break.
- Replaced the direction-by-direction take-up prelude with a short global inward pull and earlier lock impulse. All three chains visibly tighten at once and enter rupture immediately instead of pausing to gather strength.
- Removed coherent broken-segment motion from the active render path. Each complete link now disappears almost immediately after its local fracture starts and is replaced by four mobile or six desktop metal shards emitted from points around that link's elliptical rim. The shards inherit radial, tangent, and normal velocity, rotate independently, travel in multiple directions, and fade as particles rather than leaving large chain sections to vanish in place.
- Replaced the previous split-heart treatment in the active draw order with a compact contract-heart imprint, short break-point tethers, a lock pulse, inward collapse, and bounded fine particles.
- Changed the ordinary blue-to-red tide into a worn seal red: low-saturation wine/crimson gradients, restrained patina flecks, and fine directional wear remain visible through normal scroll charging. A new `--kisara-title-vivid` state stays at zero during that phase, then ramps during the chain/enchantment intro so the existing vivid pink-red returns only around the contract imprint.
- Rebuilt the cached chain-link sprites without changing their paths or timing. Every visible link now uses a higher-resolution blackened-steel bevel with multi-stop cold/hot reflection, a dark inner wall, broken highlight arcs, contact shadow, and deterministic micro-scratches; the hot version remains predominantly dark metal with crimson reflection instead of a flat pink ring.
- Rollback point before this refinement: `3204f96`. Same-tab desktop replay validated the immediate `100% -> is-enchanting` handoff twice with `scrollY = 0`; timed frames showed fine shatter beginning around the first quarter-second and expanding across the title without large flying chain sections. Additional material checks sampled `--kisara-title-vivid` at `0` during roughly `68%` charge, `0` at the instant the seal intro began, about `0.86` during the imprint, and `1` after the transition. The page retained zero positive horizontal overflow and no browser warning/error logs. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, 36-page Pagefind index) passed.

### 2026-07-21 Kisara custom page scroll rail baseline

- Hid only the Kisara document's native page scrollbar while preserving normal document scrolling and the separate scroll behavior of theme panels, code blocks, and mobile filter rows.
- Added a fixed contract-signal scroll rail to `KisaraLayout.astro`. Its pink/blue energy marker follows document progress, supports pointer dragging, track jumps, and keyboard Arrow/Page/Home/End controls, updates after resize/BFCache restoration, and disappears when a page has no scroll range.
- Prevented text selection and native drag behavior across the home gate performance plus visual assets (`img`, `picture`, `canvas`, `video`, and `svg`) without disabling selection in article prose or code content.
- Desktop browser checks at `1280x720` confirmed native scrollbar width `0`, zero positive horizontal overflow, title `user-select: none`, empty selection after double-clicking the title, article prose `user-select: auto`, and synchronized `0 -> 100` rail progress. The original home wheel gate still held `scrollY = 0` while charge fill advanced. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-21 Kisara gate-aware scroll rail

- The right-side rail now switches from an interactive document scrollbar to a read-only contract progress indicator while the home gate owns the wheel. The gate publishes a cached `kisara:gate-progress` event plus DOM dataset fallback, and `KisaraLayout.astro` restores the normal draggable/keyboard scrollbar as soon as the page transition begins.
- The complete gate timeline is mapped into stable sections: scroll-driven chain binding uses `0-45%`, chain rupture/enchantment uses `45-55%`, the manual blade/gloss release uses `55-65%`, and the eight-second automatic release uses a real elapsed-time clock for `65-100%`. This avoids the internal nonlinear `burstProgress` jumping the public indicator above `90%` near the start of the black-hole sequence.
- Rail presentation follows the dominant visual stage without adding a second large effect layer: cold blue chain stages warm into crimson rupture/enchantment, the warning uses a tiny white strike, singularity/collapse use a compact orbiting dark core, detonation pulses white-hot, and reconstruction emits three bounded data pixels.
- Desktop full-sequence sampling recorded `71 -> 77 -> 83 -> 89 -> 95 -> 100` through collapse, detonation, and completion. Mobile `390x844` retained zero positive horizontal overflow, hid the native scrollbar, cleared the bottom navigation, and replayed from `0 -> 4%` after returning to the gate. Desktop warning-stage inspection showed the rail at `66%` with the expected white strike; page warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-21 Kisara read-only rail guidance and return sync

- Used `67bd289` as the rollback point. Removed the custom rail's pointer drag, track jump, and dedicated keyboard handlers; it is now a read-only `progressbar`, while native wheel, touch, and page keyboard scrolling remain available.
- Added a manual-input hint beside the live rail marker rather than over the hero artwork. Two larger blue/pink chevrons follow the thumb, appear during scroll-driven charge, manual blade release, and the completed `SCROLL DOWN / ENTER CHANNEL` state, then hide during chain-break and eight-second automatic playback.
- Replaced the return-to-gate rail handoff with an explicit `returning` state. The spring-scroll callback publishes the real document progress on every frame, so the marker cools to blue immediately and travels continuously back to zero before returning to the `awakening` state.
- Desktop return sampling recorded `22 -> 10 -> 3 -> 1 -> 0` while the rail stayed blue and in gate mode; the final frame switched from `returning` to guided `awakening` without a red hold or position jump. Mobile `390x844` retained zero positive horizontal overflow and kept the guide beside, not over, the rail. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.
- The `2026-07-28` visual pass supersedes the standalone polygon marker and paired chevrons without restoring drag behavior. Current position is now expressed by the rail itself thickening into a short tapered accent segment with a narrow white core; manual guidance is a small light flow continuing downward along the rail. Existing stage colors remain, while singularity/collapse split the segment around a dark slit instead of drawing another orbiting badge. A shared marker-center coordinate keeps the filled rail, active segment, and guidance flow aligned at both ends. `git diff --check`, `npm test` (3/3), and `npm run build` (42 pages, 43-page Pagefind index) pass; subtle visual balance remains pending human review.

### 2026-07-21 Kisara protected-core clarity pass

- Traced the remaining softness inside the subject mask to two mismatched rendering paths: the CSS protection image was still translated and gravity-scaled while the WebGL protection texture stayed in the original cover coordinates, and both the SVG plus shader widened the feather.
- Re-aligned the CSS protection layer with the WebGL scene at viewport cover coordinates, removed its gravity transform, made the inner SVG path solid, narrowed the outer feather, and added an explicit WebGL alpha cutout over the solid protected core. The protection now reaches full opacity as the warning begins rather than waiting for collapse.
- Desktop `1280x720` replay sampled the strong-distortion frame near `87.8%`: the surrounding scene retained about `3.72px` performance blur while the protected layer stayed at opacity `1`, identity transform, and no local blur. The face/body linework remained visibly sharper without restoring the previous hard mask outline. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-22 Kisara maskless side-field distortion

- Superseded the character-shaped `fight-distortion-protect.svg` compositing path after repeated human-visible seams, empty guard bands, and duplicated edge texture. The asset remains untouched for rollback/reference, but the home performance no longer loads it into the DOM or WebGL renderer.
- The middle third of the viewport is now a continuous stable field while only the left and right fields receive lens displacement. The transition weight follows the real distortion field and viewport-edge fade, and the shader samples one interpolated UV instead of blending stable and displaced texture samples that produced ghost copies.
- The space-lens canvas now crossfades as one complete scene-replacement layer. Its shader no longer bakes in the atmosphere overlay already supplied by CSS, so the same scene is not darkened or composited twice around the former mask edge.
- Removed the warning stage's dynamic elliptical aperture and local oval highlight. The black/white cross now sits over a borderless full-screen vignette, leaving no character-mask or focus-ring outline to expose during the high-contrast flash.
- Extended the fifth memory scene through the chain rupture/contract-heart beat, then hands off to the fight artwork with a soft blur settle instead of cutting directly from the kiss frame to a sharp image.
- Desktop replay covered the warning frame, active lens/collapse frame, detonation, reconstruction, and final post-release state. The protection DOM/variables were absent, WebGL entered and exited its active state normally, positive horizontal overflow stayed at zero, and browser warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed. Fine visual acceptance remains with the user on the local display.

### 2026-07-22 Kisara interactive release finisher and smoke reveal

- Commit `4ed5efd` replaces the latter locked cutscene with a release state machine (`manual`, `forward`, `rewinding`, `paused`, `complete`). First autoplay is `3000ms`, later same-tab runs are `1750ms`, downward wheel input can boost playback to `1.8x`, and an early upward input rewinds under a short veil to a resumable blade-stage pause. Once detonation is unsafe to reverse, upward input accelerates completion instead.
- The progress rail now follows autoplay rate, visual pressure, release mode, and rewind state. Release rewind clears transient orbit, burst, rain, Canvas, and WebGL residue before pausing; the completed camera beats reset to identity before the next section.
- The fight-art handoff is now hidden behind a dense procedural smoke field. The manual title gloss sweeps from right to left; fight opacity, blur, brightness, saturation, and smoke dispersal share that blade timeline. The first half remains almost fully obscured, while the latter half clears on one continuous curve instead of a threshold pop.
- Removed the rejected full-rectangle fog base, animated parent blur, and moving `mask-image` after human review found a gray slab sliding left plus possible black compositor flashes. The replacement uses one oversized feathered elliptical veil and three independently fading/moving smoke banks: right clears first, center second, and left last. No parent mask or parent filter remains.
- Added a low-frequency pink energy leak around the fight artwork's upper-body area between the base veil and foreground smoke banks. The stronger core remains partially occluded by real smoke alpha, briefly rises as the blade begins to expose the scene, and fades before the smoke fully clears; its animation changes only opacity/transform, avoiding repeated large filtered-surface reallocations.
- Automated browser checks used the existing single local preview tab only. The pre-blade state reported fight opacity `0.008`, blur about `31.55px`, smoke opacity `0.99`, veil opacity `0.96`, and aura opacity `0.64`; computed parent `mask-image` and `filter` were both `none`. A later sample showed the right bank at `0`, center near `0.12`, and left near `0.66`, confirming staggered non-rectangular withdrawal. The page warning/error log was empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed. Fine smoke density, glow strength, and perceived transition quality remain pending user judgment on the local display.

### 2026-07-22 Kisara original transformation scenes and continuous lensing

- Superseded the procedural fight-smoke handoff with three user-selected original frames. Source files `kisara/6.png`, `7.png`, and `8.png` remain untouched; 1920x1080 WebP runtime copies now form the `smoke-wide -> detail -> silhouette` bridge between the kiss memory and reconstruction.
- Split the WebGL space-lens inputs into a pre-release texture and a reconstruction target. Frame `8` is the only artwork distorted through warning, collapse, and detonation; `fight.jpg` is introduced by the existing block-reconstruction front and becomes the stable background only near completion.
- Returned the lens to a full-frame gravity field now that the final fight subject no longer needs protection. Removed both sign-based half-screen polarity and non-periodic `atan()` phase terms after they produced visible vertical and horizontal split lines; the active displacement now uses continuous direction-vector components across the viewport. The user visually confirmed the seam-free result before the idle-motion pass.
- Frame `8` enters with an additional `10.5px` blur. The manual blade sweep continuously removes that blur, restores brightness, and increases its local scale from `1` to `1.022`, creating a restrained push through the smoke before the warning starts.
- Added an automatic scene-camera breath to every pre-fight image stage, including the initial blue artwork, all five memories, and frames `6/7/8`. The current refinement increases the travel and scale range while lowering the secondary frequencies, producing a broader continuous drift instead of a small fast tremor; the envelope still fades through reconstruction and reaches zero for the final fight state.
- Rollback point before the larger-breath/edge-coverage refinement: `4abbc00 Add Kisara pre-fight scene breathing` (`603abbc` remains the transformation-sequence baseline). Per-frame overscan now derives the minimum scale from the live X/Y camera translation and adds a three-CSS-pixel guard on every moving edge, preventing compositor rounding from exposing the viewport boundary. A six-second, 60-sample run in the existing preview tab recorded 60 distinct camera states, at least `3.14px` of bottom coverage, and about `3px` minimum coverage across all four edges; exact one-pixel visual acceptance remains with the user. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed after the final guard increase.

### 2026-07-22 Kisara theme audio signal

- Rollback point before this pass: `501f22a Refine Kisara scene breathing overscan`. Added a theme-wide `KisaraAudioControl.astro` mounted by `KisaraLayout.astro`, so every Kisara route exposes the same compact lower-left control for `対策完了`. The source under the untracked user-owned `kisara/` directory remains untouched; the deployed copy uses the ASCII path `public/themes/kisara/audio/taisaku-kanryo.mp3`.
- The control attempts audible playback immediately at `48%` volume, loops the track, and falls back to an explicit `READY / PLAY` state when browser autoplay policy blocks sound. Click/touch/keyboard interaction retries playback; the same button pauses without resetting time and resumes from that position. Per-tab `sessionStorage` preserves the enabled flag and current time across Kisara document navigation, while avoiding a forced SPA conversion that could destabilize the homepage performance runtime.
- Playback state drives the UI rather than a timer: active audio enables the orbit, scan, pulse, and equalizer animations; pause removes those animation names immediately. Runtime checks confirmed `readyState = 4`, successful click playback with increasing media time, stable media time after pause, and paused-state restoration at the same timestamp after navigating to Blog. Full-document navigation can still require one fresh interaction when the browser reapplies audible autoplay policy. Browser warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed; exact lower-left visual sizing remains pending user review.
- Human review rejected the first framed-card treatment as too conspicuous and disconnected from the scene. Using `ffa8b7b Credit Yuimi and Codex on Kisara gate` as the rollback point, the control is now a frameless lower-left audio signature: the standalone play/pause glyph, five-bar micro waveform, dim two-line label, and one animated signal filament are the only persistent shapes. The former rectangular panel, border, inset background, circular emblem, orbit, and scanning slab were removed; hover adds only a feathered local aura. Playback, pause, autoplay fallback, session persistence, and volume behavior are unchanged. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed; final visual acceptance remains with the user on the local display.

### 2026-07-22 Kisara persistent playlist

- Rollback point before this pass: `00349e9 Restyle Kisara audio control`. Expanded the theme player from the single `対策完了` track to an eight-track playlist by adding seven user-provided songs; `ココロスペアキー.mp3` remains excluded. Runtime copies use ASCII filenames under `public/themes/kisara/audio/`, while the untracked source directory `kisara/` remains untouched.
- Added restrained previous/next controls, automatic advancement at track end, live `NN / 08` status text, and track-aware accessible labels without restoring the rejected framed-card presentation.
- Replaced per-tab playback memory with `localStorage` key `yuimi-kisara-playlist-v2`, preserving the enabled/paused state, active track, and playback position across reloads and later visits. Existing `sessionStorage` state under `yuimi-kisara-audio-v1` is migrated once when no new state exists.
- Runtime verification switched to `愛の力`, paused near `23.28s`, reloaded, restored the same track and timestamp, and resumed forward from that point. Final inspection left `合鍵` paused near `59.70s`; browser warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-22 Kisara warning cold-start prewarm

- Rollback point before this pass: `21f7848 Add persistent Kisara playlist`. Traced the first-run-only hitch around the black/white cross to cold compositor/filter activation plus the first WebGL and singularity-buffer work; the same sequence was already smooth after those resources had been cached once.
- Kept the cross in the render tree at zero opacity instead of revealing it through a first-use `visibility` switch. During the first idle window, the page now briefly prewarms the warning filter layers, executes transparent draws through both WebGL lens pipelines, and allocates the singularity field buffer before the automatic release reaches that stage.
- Cached the gate width and height once per presentation frame so later warning-variable writes no longer interleave with repeated root layout reads. The animation timing and visible parameters remain unchanged.
- A cold reload confirmed both WebGL renderers reached `ready`, returned to an inactive transparent state after warming, and produced no browser warnings/errors. The user then confirmed the refreshed first trigger no longer felt stuck. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-22 Kisara continuous in-theme audio navigation

- Superseded the full-document Kisara-to-Kisara audio resume approach after human review still heard a roughly `100ms` dropout/electrical click. Saving `currentTime` earlier and preloading the next document reduced the gap but could not preserve the browser's active decoder/output stream because every document navigation destroyed the `HTMLAudioElement`.
- Added Astro `ClientRouter` only to `KisaraLayout.astro` and marked the complete `KisaraAudioControl` root with `transition:persist="kisara-audio-player"`. Internal Kisara links now swap page DOM while retaining the same live audio element, decoded buffer, current track, playback position, and output stream. Cross-theme switching remains a full-document navigation and must not persist Kisara runtime or CSS into another theme.
- Converted the shared Kisara layout and inner-page scripts to transition-aware lifecycles. Rerun scripts use scoped `AbortController` cleanup; article RAF/timers, Blog intro timers, filters, tabs, arcade controls, context-menu listeners, scrollbar observers, and page listeners are released before each swap.
- Kept the large homepage runtime as a cached module rather than an inline rerun script. A single `astro:page-load` scheduler reinitializes it only when `[data-kisara-gate]` exists, while `astro:before-swap` cancels RAFs, timeouts, idle warmup, observers, wheel/touch/key listeners, and both WebGL renderers. This preserves return/back behavior without duplicating the roughly ten-thousand-line runtime or leaking its wheel capture into inner pages.
- Same-tab validation covered `Home -> Game -> Blog -> Article -> Projects -> About -> Home`, then browser Back/Forward. Across every transition the same track remained unpaused with `readyState = 4`, and `currentTime` advanced continuously instead of restoring from storage. A post-navigation pause/play click still toggled exactly once, the returned home gate reported `data-bound="true"`, and browser warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-22 Kisara scene-aware opening edge continuation

- Rollback points: `7659550 Add scene-aware Kisara section bridge` preserves the generated continuation implementation, and `eec66b7 Checkpoint Kisara bridge synchronization` preserves its final geometry-sync experiment.
- Superseded diagnosis: the visible line was not primarily the lower live-copy/texture feather. The boundary itself changed rasterization context: copied background properties and post-transform rectangles repainted each layer independently, so the first row below the hero could not share the composed hero pixels even when DOM geometry differed by less than `0.01px`.
- The accepted fix temporarily moves the real `.kisara-gate-visual` node, including its live Canvas layers, into a fixed bridge portal while the gate scrolls into the opening section. The gate and the transparent top of the opening section therefore reveal the same rendered frame on both sides of the moving boundary; a section-owned gradient then settles that frame into the pale content background. Generated scene continuations remain only as restoration/fallback coverage after the live bridge leaves view.
- The user visually accepted the boundary continuity on the local display. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) pass for this implementation.

### 2026-07-22 Kisara Blog layered-focus entrance

- Replaced the raw four-cutout reveal with depth-aware signal focusing. Kisara appears first as a softened chromatic silhouette; Shu enters before Kisara becomes sharp, preventing the incomplete overlapping upper-body cut from being shown clearly. Ayano and Sharon retain progressively stronger final depth blur.
- A localized blue/pink reconstruction veil hides the front-pair assembly boundary without globally blurring the full cast. The stable rollback points are `8bf0693 Refine layered Blog hero entrance` and `582f510 Delay Blog foreground focus until pair assembly`.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed. Final human review of the pair-synchronized focus timing remains pending.

### 2026-07-22 Kisara GPU compositing stabilization

- User reported intermittent full-screen flicker and large black rectangles during the homepage interaction. The highest-risk combination was a dynamically filtered full-screen ancestor containing two active WebGL canvases, followed by reparenting that live visual subtree into a fixed bridge portal.
- Superseded and forbidden approach: distributing the warning grayscale/contrast/brightness filter to every background layer. That multiplied full-screen filter work, caused a severe performance leak on page load, and was immediately restored to rollback point `582f510`. Do not retry this strategy.
- Current implementation keeps exactly one filtered image composite in `.kisara-gate-visual`, moves the WebGL/post-release canvases into an unfiltered sibling layer inside `.kisara-gate-visual-shell`, and keeps that shell under a stable parent. Opening transitions switch the shell to fixed positioning instead of moving live Canvas nodes between parents.
- Added `gl.isContextLost()` draw guards and staggered the two renderer warmups by one animation frame. This avoids drawing a lost context and reduces same-frame GPU allocation pressure without lowering render resolution or duplicating filters.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) pass. Browser was intentionally not opened after the earlier performance incident; human validation of visual continuity, black-block recurrence, and long-run GPU stability is still required.

### 2026-07-22 Kisara memory-stage flicker isolation

- Human review confirmed that the intermittent black rectangles and flicker already begin during the five-image memory/chain sequence, before the black-hole renderer becomes visually active. This narrows the primary risk to the shared full-screen image compositing path rather than the later singularity effect alone.
- Removed the persistent full-screen `filter` from `.kisara-gate-visual`. Even identity-valued ancestor filters force the dynamically blurred memory images through another large offscreen surface. The previously rejected alternative of distributing warning filters across every background remains forbidden because it fixed the artifact only by multiplying full-screen filter work and caused a severe performance leak.
- Rejected follow-up: a full-screen `mix-blend-mode: saturation` replacement removed the black rectangles but drove CPU and memory sharply upward because the continuously animated scene kept the backdrop blend surface active. The layer was removed; warning emphasis now relies on the existing title-local treatment and screen-impact overlays. Do not restore a persistent full-screen blend layer.
- Updated the legacy direct-child performance-blur exclusion for the newer `.kisara-gate-visual-shell` architecture. Without that exclusion, `blur(0px)` still wrapped the complete image plus dual-WebGL shell in a full-screen filter surface during the memory stage; visible performance blur remains on the individual image layers where it was already implemented.
- Rejected follow-up: toggling `visibility` on the full-size memory/transformation/fight layers caused filtered textures to be repeatedly discarded and recreated during forward/reverse playback. Removing `filter` from their `will-change` declaration also destabilized the continuously animated blur path and shifted work back toward repeated rasterization. Both experiments were reverted to the previously stable per-image compositing behavior.
- Operational note: terminating a long-running tool cell did not terminate its detached npm/Astro children. Two preview servers were found simultaneously using roughly 670 MB of working set and were explicitly stopped. Before starting another preview, check for existing project-specific `astro dev` processes and terminate the complete process tree rather than only the wrapper.

### 2026-07-22 Kisara two-slot scene compositor

- Human testing established a compositor deadlock: keeping all five memory and three transformation layers permanently promoted avoided CPU churn but reproduced GPU black rectangles; dynamically hiding/recreating those filtered layers removed the artifacts but caused CPU and memory growth. Further `filter`/`visibility` tuning was abandoned in favor of changing the scene architecture.
- Replaced the eight full-screen filtered scene nodes with a JSON scene manifest and two permanently mounted reusable scene slots. The slots never change `display` or `visibility`; only their semantic scene binding and background URL change at timeline boundaries, while opacity/filter/transform continue smoothly inside the stable surfaces.
- The absolute memory and transformation timelines remain the source of truth, so forward scroll, reverse scroll, and rapid direction changes do not depend on slot identity. Existing active bindings are retained first, new scenes use the free slot, and a deterministic overflow guard prevents future timeline edits from silently creating a third full-screen scene.
- Opening-edge live continuation now resolves memory/transformation layers by semantic scene ID to whichever slot currently owns that scene. Base, fight, reconstruction wash, WebGL lensing, the accepted live bridge portal, and the final fight handoff remain dedicated layers.
- Runtime inspection used one temporary local tab without screenshots. It covered `rescue -> attack -> clash -> embrace -> kiss`, reverse playback, and `kiss -> smoke-wide -> detail -> silhouette`; exactly two slots remained mounted, no legacy full-screen scene nodes existed, and browser warning/error logs stayed empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-22 Kisara compositor performance follow-up

- Rollback point `af06f4c Replace Kisara scenes with two-slot compositor` preserves the first human-validated version where the black rectangles and flicker no longer reproduced and the previous severe CPU/memory growth remained absent.
- Removed the remaining startup burst from the opening-section continuation system. It no longer decodes, copies to `ImageData`, and generates PNG continuation textures for all ten base/memory/transformation/fight scenes during the first interaction. Base is prepared initially; another scene is sampled only when that exact visual state is about to cross into the next section.
- Opening-edge state is now retained as lightweight data during the gate animation. Its ten offscreen DOM layers are no longer updated or geometry-read every frame, and their permanent `will-change` promotion was removed. Forced geometry synchronization remains at the actual enter/return/lock boundaries, so the accepted live bridge behavior is unchanged.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed. One temporary runtime tab confirmed two scene slots, exactly one generated base continuation texture at startup, and no browser warnings/errors. Full-sequence smoothness remains a human visual/performance check because automated scrolling did not engage the custom wheel gate.

### 2026-07-23 Kisara homepage boot and runtime recovery

- Production feedback showed an intermittent false-ready homepage: the gate image appeared before the interaction runtime became usable, allowing normal document scrolling and leaving the opening continuation inactive until a full refresh.
- Root cause: the scheduler called the previous cleanup before checking whether the same gate had already completed initialization. When the initial zero-delay task won the race against `astro:page-load`, the second schedule destroyed the fresh runtime, observed the stale `data-bound` marker, and returned. Earlier code also wrote `data-bound` before the large synchronous initialization had actually completed, so any exception could leave a permanently false-bound gate.
- The scheduler now treats `data-binding` as an intermediate state, writes `data-bound` only after the first successful render, checks both markers before cleanup, clears them during cleanup, and retries two bounded times after a synchronous initialization failure. The main runtime remains a Vite-built external module; a small `data-astro-rerun` guard raises its fetch priority, retries a failed module URL with a cache-busting query, and rearms the boot lock during that retry.
- Local validation covered first load, captured wheel input, `Home -> Blog -> browser Back -> Home`, and a second captured wheel input. Both homepage entries ended with `data-bound=true`, no lingering `data-binding`, `data-kisara-home-ready=true`, `scrollY=0` while charge advanced, and no `[Kisara]` warning/error logs. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.

### 2026-07-23 Cross-theme mobile long-press and selection boundary

- The shared `ThemeLongPressMenu` remains mounted by all three layouts, but mobile opening no longer depends on synthesizing a browser `contextmenu` event. A completed touch hold now emits `yuimi:context-menu-request`; Fuyukawa Kagari, Blank, and Kisara each open their existing menu from that explicit shared event while desktop right-click behavior remains unchanged. Pointer Events are preferred, with a touch-event fallback for older mobile browsers.
- All layouts now mark article documents explicitly. Non-article pages receive `data-yuimi-selection-lock=true`, disabling text selection, native touch callouts, and visual-asset dragging without changing scrolling or pointer interaction. Article pages receive `data-yuimi-article-page=true` instead, retain normal text selection, and ignore custom long-press handling inside the article element so copy/select gestures are not stolen.
- Build inspection confirmed the lock on all three home routes and its absence on all three article variants. Synthetic touch-pointer holds opened the Kagari, Blank, and Kisara menus; Blank and Kisara article content retained computed `user-select: auto`, did not open the custom menu, and produced no page warnings/errors. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.

### 2026-07-23 Kisara playlist and skip-cue follow-up

- Expanded the persistent Kisara playlist from eight to eleven tracks with `悲しい過去`, `本気モード`, and `記憶の行方`. Runtime copies use `kanashii-kako.mp3`, `honki-mode.mp3`, and `kioku-no-yukue.mp3`; SHA-256 comparisons match the untouched user sources under `kisara/`. `ココロスペアキー.mp3` remains intentionally excluded.
- The player's initial server-rendered count now derives from `kisaraAudioTracks.length`, preventing a brief stale `01 / 08` label before the runtime updates it to the current total.
- Moved the homepage `SKIP` cue slightly left on desktop and mobile. A restrained opacity/glow breath plus a short underline scan makes it easier to notice without adding another framed control; both animations are disabled under `prefers-reduced-motion`.
- `ffprobe` read all three deployed MP3 files successfully at approximately `141.97s`, `178.92s`, and `176.00s`. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed; the built player begins at `01 / 11` and contains all three new assets.

### 2026-07-23 Kisara context-menu redesign

- Replaced the minimal theme list with a complete Kisara quick menu: back, forward, refresh, theme home, scroll to top, copy link, copy title, and all three theme routes. Copy actions keep their icons and report success through the menu status instead of replacing the entire button.
- Removed the rejected large dark slab. The menu now uses staggered translucent contract modules with a pink/blue header, live backdrop blur over only the small visible modules, and a transparent outer surface. This keeps the scene visible behind the menu without adding a full-screen filter layer.
- Menu placement now measures `offsetWidth` and `offsetHeight`, so its entry animation cannot underestimate the final size and push the expanded menu beyond the bottom viewport edge. Desktop right-click, keyboard opening, and the shared mobile long-press event continue to use the same lifecycle-safe handler.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-23 Kisara title-tide boundary simplification

- Superseded two failed blue-halo experiments: reverse-masking the blurred base exposed a rectangular mask boundary, while a nested blurred text halo rasterized as a large blue rectangle in Chromium. Do not restore either approach.
- Removed the independent moving `kisara-title-crest` highlight from markup, CSS, and frame updates. The charge-phase title no longer carries the old `8.8px -> 2px` blur; only the later automatic-release performance blur remains, so the blue source stays clean during reversible input.
- Superseded the bright crystal-blue treatment. The clear source is now a desaturated, partially translucent memory-loss state with weak contrast, dark missing strokes, worn grain, and almost no highlight; it remains sharp rather than blurred.
- The sealed wine-red area starts restrained. Runtime now writes explicit numeric energy-overlay opacity, brightness, and saturation from the actual fill, guaranteeing visible whole-region accumulation while keeping the sealed state below the later vivid unsealed red.
- Removed the continuous multicolor reaction strip and the rejected regularly spaced radial dots. The current boundary keeps the accepted dark depleted zone on the blue side, while its recovery field uses muted angular fragments and interrupted veins without `screen`-blended white circles. Pattern motion, opacity, and directional bias derive from the same fill/velocity state, so advance and rewind share one continuous timeline.
- Removed every repeating gradient from the title material range. Blue now uses one-off memory voids, large fracture planes, pale remnants, and horizontal loss strata; wine red uses asymmetric heat pools, deep conic veins, isolated energy knots, and layered tonal fields. Boundary fragments have distinct sizes and fixed glyph-relative positions instead of tiled motifs, and the obsolete pattern-shift variable was removed.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed. Fine visual acceptance remains with the user in the existing local preview.

### 2026-07-23 Kisara Works cooking-game workbench

- Replaced the Works lower-page technology filter and project-sheet console with one bounded kitchen game while preserving the accepted `work-kitchen-hero.webp` hero. A stronger bottom wash now blends the image into the counter base instead of ending at a hard section edge.
- Six project-backed ingredients map Unity, UE5, Blender, AstrBot, Blog design, and MMD to distinct food forms, official technology marks where available, project names, colors, and preparation states. Their source project copy still comes from `src/core/data/projects.ts`; the page-local mapping controls only the kitchen presentation.
- Pointer Events support pantry-to-board dragging and prepared board-item drops into the blender or trash. Click/tap and keyboard-accessible buttons provide equivalent add, chop-all, load, blend, reset, and remix actions. The board uses a knife cursor, the pantry/drag path uses a hand cursor, and coarse/mobile pointers fall back to native controls.
- Board items have two reversible-looking visual cut states, the blender accepts only prepared items, and exact or fallback ingredient combinations produce named abstract drinks with ingredient readouts and side-effect copy. Runtime cleanup remains under the existing `data-astro-rerun` / `AbortController` contract; RAFs, timers, pointer capture state, and drag ghosts are released before ClientRouter swaps.
- Follow-up interaction refinement: drag ghosts preserve the original pointer anchor, follow through a bounded spring with velocity-based lean/lift, and settle into accepted targets or return toward their source instead of jumping to the pointer center and disappearing. Board rendering is now keyed by ingredient ID, so cutting mutates the existing food pieces instead of recreating every board item; the six ingredients retain separate cube, ice, orange, candy, leaf, and cherry forms and sit as scattered food plus small prep tags rather than rectangular cards.
- Result handoff uses one interruptible exponential-damping scroll controller. A completed blend moves smoothly to the drink result; `再开一局` clears the round and uses the same controller in reverse to return to the cutting board. Wheel, touch, pointer, or keyboard input cancels either automatic move immediately, and reduced-motion uses a direct position change.
- `astro.config.mjs` now explicitly includes the bounded Tabler icon set needed by the kitchen (`blade`, `blender`, `chef-hat`, `hand-finger`, `hand-grab`, `tools-kitchen-2`, and `trash`). No package was installed and no user-owned untracked source asset was staged or modified.
- Validation: `git diff --check` reported only the existing line-ending notices; `npm test` passed 3/3; `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index. Desktop `1280x720` and mobile `390x844` retained zero positive horizontal overflow. The tested `U3D + BLD` path completed board loading, two cut passes, blender loading, and produced `碰撞体橙昔`. `Home -> Works` ClientRouter navigation rebuilt the six ingredients with `data-kitchen-ready=true`, removed the old project console, and logged no browser errors. Fine visual judgment remains pending user review.
- Refinement validation: the same `U3D + BLD` path retained `cube` and `orange` shape identity through cut states `0 -> 1 -> 2`. After blending, the result settled with its top about `58px` below the viewport; after `再开一局`, the cleared board settled about `62px` below the viewport. Mobile `390x844` held all six shapes in two `145px` columns at `545px` board height with zero horizontal overflow. Browser error logs remained empty; final drag feel and food styling remain a human visual check.

### 2026-07-23 Kisara Skip reverse-scroll race recovery

- Reproduced the homepage race where `SKIP` began the damped forward transition, the user immediately scrolled upward, and the visual returned near the hero while the internal mode could remain `snap`/`next`. The next downward wheel input then escaped the gate runtime, shook the page, and exposed a stale opening bridge/transition boundary.
- Root cause: the forward scroll RAF had no transaction identity or cancellation path. Reverse input was prevented but did not invalidate the in-flight callback, so a late frame could still restore the destination and overwrite gate state after the user had changed direction.
- `HomePage.astro` now gives each smooth-scroll transaction a serial, direction, and owned `scroll-behavior` state. Reverse wheel input during the Skip transition cancels the forward RAF immediately and enters the existing `returnToGate()` flow; stale RAF callbacks cannot mutate scroll or page mode after cancellation.
- Added a native-scroll fallback that detects movement back toward the gate while mode is `next` and starts the same reset path below the established return threshold. Cleanup also restores the root scroll behavior and invalidates any active transaction.
- Same-tab validation covered reversing during Skip, reversing after Skip had completed, and repeating the full sequence without refresh. Every return settled at `scrollY=0`, `progress=0`, `active=true`, with bridge classes removed; the next downward wheel was captured and advanced the gate timeline. Browser warning/error logs were empty. `npm test` passed 3/3 and `npm run build` generated 36 pages, sitemap, and a 36-page Pagefind index.

### 2026-07-23 Kisara Game arcade cabinet and route recovery

- Reworked the Game page away from the Blog archive composition. The hero keeps the accepted night image, manga speech bubble, and entrance, but now dissolves directly into a dark playfield with an unframed channel rail and a physical arcade cabinet.
- The cabinet has a lazy game screen, marquee, centered vertical joystick, physical START control, previous/next channel buttons, speaker grille, coin bay, and service plate. No iframe receives a `src` until START is pressed; EJECT, page cleanup, and selecting another channel unload the current frame back to `about:blank`.
- Hextris and Clumsy Bird use their original GitHub Pages displays. The original 2048 URL now redirects to a site that rejects framing, so `public/themes/kisara/games/2048/index.html` provides a noindex local entry using Gabriele Cirulli's original HTML and CDN-hosted repository assets. Pagefind scans the extra HTML file but its route and game copy are absent from the generated search index.
- Reproduced the stale-style bug with a real `Blog -> Game` ClientRouter transition: the new machine DOM arrived with an older prefetched `games.css`, shrinking the joystick base back to `36px`. Astro enables `prefetchAll` automatically with ClientRouter, so removing only explicit `data-astro-prefetch="load"` had not fully disabled caching. `astro.config.mjs` now sets `prefetch.prefetchAll=false`; soft navigation remains active and a repeated Blog-to-Game transition retained the current `152px` joystick base, final control-deck columns, vertical stick, and zero horizontal overflow.
- Fixed the refresh autoplay recovery race. Non-activating `pointermove` and `wheel` events no longer consume the first retry. A real pointer/click/touch/key gesture can invalidate a pending failed bootstrap promise and issue a fresh `audio.play()` inside that activation. Runtime validation started at `paused=true / blocked`, then the first Game channel click changed the same audio element to `paused=false / playing`.
- Validation: `git diff --check` reported only existing line-ending notices; `npm test` passed 3/3; `npm run build` generated 36 Astro routes, sitemap, and Pagefind output. Initial Game state had no iframe source; local 2048 rendered its original `.game-container`; Hextris rendered its original page; EJECT and channel switches cleared the active frame. Fine visual acceptance of the completed cabinet remains with the user.

### 2026-07-23 Kisara gate-return transaction hardening

- Human testing disproved the earlier Skip race closure: rapid alternating wheel input while the damped return-to-top animation was still running could still leave `pageMode="snap"`, a live opening bridge, or a small nonzero document scroll. The next downward wheel then bypassed the gate and exposed several stale transition strips. The same window existed after a normally completed release, not only after `SKIP`.
- Return-to-gate completion is now one idempotent transaction. It clears the bridge portal and deferred reset flag, restores `scrollY=0`, resets the gate timeline, paints the blue opening-edge state once, releases that state for the next interactive run, and reasserts the top position on the following frame.
- Directional input now blocks every non-idle scroll transaction, heals a stranded `snap` state, and refuses native document scrolling whenever `pageMode="gate"` has drifted away from the top. A short post-return guard absorbs queued wheel events at the RAF completion boundary instead of letting them race the reset callback.
- The footer DOM was never removed. Its missing appearance was consistent with a residual full-screen gate bridge stacking above later content, so `.kisara-footer` now owns a stable positioned `z-index: 3` stacking context after section `004`.
- Runtime validation repeated `SKIP -> upward return -> rapid alternating wheel -> downward replay`. It settled at `scrollY=0`, rail progress `0`, no bridge classes, and the next downward input advanced the gate while keeping document scroll at `0`. Scrolling to the document end exposed the footer above all gate layers. `git diff --check`, `npm test` (3/3), and `npm run build` (36 pages, sitemap, Pagefind) passed.

### 2026-07-23 Kisara Blog cover, Me flow, and article reading pass

- Kisara Blog no longer assumes every cover is a portrait poster. The static page reads local cover metadata through the existing project-level `sharp` dependency during build; images with a width/height ratio of at least `0.85` use the Fuyukawa-style `object-fit: cover` crop and theme focus position, while true portrait covers retain the existing contained poster treatment. Current content classifies `cover-04.webp` and `cover-08.webp` as cropped and the other four published covers as portrait, with no client-side fit swap.
- Replaced the Me page's full-document two-column grid and `100vh` sticky profile with a finite top dossier split. The profile and opening statement now leave the viewport together, while technical lines, interests, and live signals continue in one centered document flow. Desktop and `390x844` mobile checks showed ordinary relative positioning and zero horizontal overflow.
- Article detail pages now import a dedicated `article.css` instead of adding another override layer to the shared theme stylesheet. The title/header, `760px` reading column, prose hierarchy, lists, quotes, code, images, tables, reading end controls, and responsive layout were restyled as one reading surface.
- The right reading map now removes trailing autolink `#` text, offsets anchors by `108px` below the fixed header, highlights the current `h2/h3`, constrains long maps to the viewport with a themed internal scrollbar, and scrolls only the directory when its active item leaves view. The 28-entry AstrBot deployment article clicked to `#9-填入-api-key-并选择模型` with the heading at about `108px`, matching `aria-current=location`, and the directory followed to `scrollTop=298`.
- ClientRouter validation covered `Blog -> Me -> Blog -> Article`; every destination loaded its current page-specific structure and CSS without horizontal overflow or page warning/error logs. `npm test` passed 3/3 and the final `npm run build` generated 36 pages, sitemap, and a 37-page Pagefind scan.

### 2026-07-23 Kisara Me fixed chapter stage

- Superseded the finite long-document Me layout. The page is now one fixed viewport containing six absolute chapters: profile, technical lines, anime, character preferences, games, and current signals. The document itself remains at `scrollY=0`; only the active chapter changes.
- Wheel input advances one bounded chapter at a time with a threshold and transition cooldown. Arrow/Page keys, Home/End, direct chapter markers, and vertical touch swipes provide equivalent navigation. Inactive chapters are `aria-hidden` and `inert`, while a live region announces the current chapter.
- The page-specific runtime owns the root/body scroll lock, hides the shared footer and document scrollbar only on the About route, and releases every listener, timer, and class through the existing `__yuimiKisaraInnerCleanup` / `astro:before-swap` lifecycle. `Blog -> Me` ClientRouter navigation rebuilt the stage and captured wheel input without duplicate handlers.
- Responsive layouts keep all six chapters within `390x844` and short `390x667` viewports with zero horizontal overflow. Desktop `1280x720` and both mobile checks retained `scrollY=0`; forward and reverse chapter traversal reached every endpoint. Browser logs were empty.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 Astro pages, sitemap, Pagefind 37 pages) passed. Fine transition feel and final visual acceptance remain with the user in local preview.

### 2026-07-24 Kisara compact context menu

- Superseded the three-module Kisara context menu, whose repeated headings, status badge, and full theme route list made a quick action surface read like a settings page. The menu is now one translucent `252x190px` desktop panel (`244x188px` at `390px` width) with a compact identity row.
- Reduced the visible actions to back, forward, refresh, scroll to top, theme home, and copy link. Copy title was removed. Copy feedback now temporarily replaces the copy-link label instead of adding a second visible status module; the live status remains available to assistive technology.
- Theme selection is a single segmented KAGARI / BLANK / KISARA row with an explicit active state. A dedicated close icon improves mobile long-press use, while the existing desktop context-menu, keyboard, shared long-press event, theme selection, placement clamp, and ClientRouter lifecycle remain unchanged.
- Runtime validation opened the menu by context click, confirmed all six commands plus close and three theme choices, exercised copy feedback and close, and checked a `390x844` viewport with zero horizontal overflow. Browser logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` (36 Astro pages, sitemap, Pagefind 37 pages) passed.

### 2026-07-24 Kisara navigation warmup and Game hero unblock

- Production feedback reported roughly five-second ClientRouter waits, especially between Game and Works. The two generated documents are approximately `85KB` and `119KB`, and global prefetch remains intentionally disabled because broad development-time prefetch previously served stale page/CSS snapshots.
- Kisara navigation now applies production-only targeted prefetch. Every non-active homepage destination uses Astro's `load` strategy after the homepage finishes loading; Game and Works preload each other the same way; other non-active Kisara navigation links use `hover`. Development output receives no prefetch attribute, preserving the accepted stale-style fix.
- Game no longer awaits the `517KB` hero PNG's `decode()` before starting its entrance. The DOM/copy entrance begins on the next animation frame, while the image owns an independent `loading -> ready` state and fades in when load or decode completes. Slow-image delivery therefore no longer leaves the new page visually idle.
- Production-preview validation observed real prefetch requests for all four homepage exits and for Works-to-Game, not only static attributes. Under a throttled connection, Game entered its active copy animation while the hero image still reported incomplete/loading. The temporary preview server on port `4400` was stopped and the port was verified released.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 Astro pages, sitemap, Pagefind 37 pages) passed. Fine live GitHub Pages timing remains a post-push human check because the local browser automation click API waits for soft-navigation completion and cannot provide a trustworthy click-to-swap duration.

### 2026-07-24 Kisara staged release warmup and route CSS split

- Rollback baseline was `5278e24`; the completed pass was committed as `bcb12a8`. A read-only performance audit traced the intermittent first black/white-cross hitch to deferred first-use work accumulating at the automatic-release boundary: the old warmup timestamp could be advanced before the async WebGL renderers existed, the singularity warmup allocated its `ImageData` without executing the expensive pixel field, and `document.fonts.ready` could rebuild title textures even when the effective font metrics had not changed.
- The release warmup is now four independent idle tasks: CSS cross/exposure compositing, a real offscreen singularity-field paint, the full-screen space-lens WebGL renderer, and the title-lens WebGL renderer. Each task owns separate completion state, renderer readiness reschedules only the missing task, positive gate input raises scheduling priority, and the idle timeout is `480ms` per normal task (`120ms` when forced) so work remains spread across frames without waiting roughly four `900ms` windows.
- Added a font signature guard based on computed family/weight/size/spacing plus measured `Kisara` width. `document.fonts.ready` now refreshes title textures, chain geometry, and data canvases only when that signature changes. Cleanup still cancels pending idle/RAF handles and destroys both WebGL renderers during ClientRouter swaps.
- Split the former all-page Kisara CSS payload into the shared `theme.css` plus route-local stylesheet URLs. Current production output is `126.7KB` shared CSS, with Blog `18.9KB`, Game `83.1KB`, Works `52KB`, Me `24KB`, and Article `9.3KB` loaded only on their matching routes. ClientRouter soft navigation continued to attach the correct route stylesheet.
- On the Kisara homepage only, MP3 source assignment, preload, and playback bootstrap now wait until `DOMContentLoaded`; non-home routes retain the immediate bootstrap. The same bootstrap promise remains claimable by the persistent player, preserving the existing cross-route audio handoff while keeping initial scene/script work ahead of multi-megabyte audio loading.
- Browser validation used one existing local tab. A clean top-of-page reload reached `data-bound=true`, both lens renderers `ready`, and `data-release-warmup=ready` in about `2.7s`. A real-wheel full run completed `manual -> forward -> complete`, reached `100%`, and produced no browser warning/error logs. A temporary `PerformanceObserver` captured no main-thread long task at or above the browser's `50ms` threshold during the measured full sequence; the probe was disconnected and removed afterward.
- Final validation: `git diff --check` reported only existing line-ending notices; `npm test` passed 3/3; `npm run build` generated 36 Astro pages, sitemap, and a 37-page Pagefind index. User-owned untracked source assets and directories remain untouched and unstaged.

### 2026-07-24 Kisara homepage gate CSS extraction

- Rollback baseline for this pass is `bcb12a8`. Moved only the continuous 1,943-line homepage gate block from `.kisara-gate` through the motion-control styles into `src/themes/kisara/styles/home.css`; the source block is about `55.9KB` and contains the title, chains, warning cross, singularity, smoke, canvases, Skip, and gate controls.
- `HomePage.astro` now injects `home.css?url` through the existing `KisaraLayout stylesheets` contract. Shared selection locking, navigation, context menu, audio, document/gate progress rail, footer, content widths, mixed responsive rules, and inner-page styles remain in `theme.css`, avoiding a broad cascade rewrite.
- Production CSS output changed from one `126.7KB` shared Kisara stylesheet to an `81.2KB` shared stylesheet plus a `45.5KB` homepage-only stylesheet. Homepage CSS weight is effectively unchanged, while Blog, Game, Works, Me, Article, and 404 routes each avoid downloading the `45.5KB` first-screen performance block.
- Desktop reload reached `data-bound=true` and warmup `ready`; real wheel input remained captured at `scrollTop=0`. A complete replay reached `manual -> forward -> complete` and `100%`. ClientRouter `Home -> Blog -> Game` loaded the correct route DOM and styles. Mobile `390x844` retained a `72px` title, full-height gate, and zero horizontal overflow. Browser warning/error logs were empty.
- Validation: `git diff --check` reported only existing line-ending notices; `npm test` passed 3/3; `npm run build` generated 36 Astro pages, sitemap, and a 37-page Pagefind index. Fine visual acceptance remains with the user on the local display.

### 2026-07-24 Kisara low-risk loading pass

- Rollback baseline is `18f644d`. Continued the homepage CSS extraction without changing the shared stylesheet order: Opening visuals, channel links, Signals, active projects, Latest transmissions, and their dedicated `980px/600px` responsive rules now live in `home.css`. Mixed page-lead, article, record-list, footer, scrollbar, and reduced-motion selectors remain shared.
- Production CSS output is now `64.7KB` shared plus `62.5KB` homepage-only. Compared with the original `126.7KB` all-route Kisara stylesheet, inner routes avoid about `62KB` of homepage CSS while the homepage total remains effectively unchanged at about `127KB`.
- Replaced the eight simultaneous scene `decode()` calls with staged forced decoding. The first two memory scenes decode immediately; later memory scenes warm at fill thresholds `0.08`, `0.25`, and `0.42`, while transformation scenes warm at `0.55`, `0.70`, and `0.84` or the matching intro thresholds. Image URLs still enter the browser cache immediately, the timeline never awaits a Promise, and cleanup invalidates late decode callbacks through a generation counter.
- Added `content-visibility: auto` only to homepage sections `002-004`. Desktop intrinsic content estimates are `500/680/470px`; responsive estimates rise for stacked layouts. A first attempt on individual Blog cards was reverted after filtering four of six records increased the document height, proving CSS Grid and dynamic filtering were not stable with per-card intrinsic sizing. The final Blog filter restored stable document height.
- Homepage document-height sampling after calibration recorded `4506px` initially, `4504px` after Skip, and `4505px` through middle/bottom scrolling. Mobile `390x844` retained zero horizontal overflow, a captured gate wheel, and the established `72px` title.
- Preserved `game-night-hero.png` and added a runtime `game-night-hero.webp`. The new asset remains `1260x709`, shrinks from `517,210` bytes to `41,176` bytes, and measured `49.81dB` PSNR against the source. Blog-to-Game ClientRouter navigation loaded the WebP with the expected natural dimensions and `data-image-state=ready`.
- Browser regression covered staged memory/transformation scene sources, a complete `manual -> forward -> complete` run, Skip-to-content scrolling, Blog filtering/restoration, Game soft navigation, and desktop/mobile layouts. Browser warning/error logs were empty. Final command validation is recorded with the commit for this pass.

### 2026-07-24 EdgeOne cross-border navigation cache

- The EdgeOne Pages project `yuimiblog` is configured for `全球可用区（不含中国大陆）`; the domain is not ICP-filed, so mainland acceleration is not available. Live measurements against `yuimi-chaya.636.ltd` showed roughly `5ms` DNS, `230ms` TCP, `460-490ms` TLS, and `0.72-1.07s` conditional-HTML TTFB even when `EO-Cache-Status` reported a hit. The platform default HTML header was `public,max-age=0,must-revalidate`, so every ClientRouter fetch crossed the network again.
- Added root `edgeone.json` with one catch-all short-cache rule and seven specific long-cache overrides. HTML and small non-critical metadata request `public, max-age=60, s-maxage=600, stale-while-revalidate=600`; `/_astro/`, Blog media, Kagari assets/music, and Kisara assets/audio retain one-year immutable caching.
- Replaced Kisara's `load` links with a dedicated idle route warmer rather than combining Astro's native prefetch with a second fetch. It warms at most two routes concurrently, skips offline, Save-Data, and 2G-class connections, fetches the target HTML plus its route-local stylesheets, refreshes after `45s`, and prioritizes Game then Works before Blog and Me. Non-eager navigation links retain Astro's `hover` strategy.
- Production-preview network capture first exposed duplicate native/custom requests; the final version removed that overlap. Home idle warmup issued one Fetch each for the prioritized target HTML and CSS, a subsequent soft navigation issued only the expected ClientRouter HTML request under Astro preview's `no-cache` response, the controller remained alive in the page main world after the swap, and site warning/error logs were empty. Temporary preview port `4330` was stopped after testing.
- Local validation passed: `npm test` passed 3/3 and `npm run build` generated 36 Astro pages, sitemap, and a 37-page Pagefind index. The first live deployment proved wildcard page groups correctly changed Blog/Game/Works/Me/article responses to `max-age=60`, while exact directory-home rules did not match. A second deployment confirmed naming the generated `index.html` files still did not affect directory requests. The final catch-all-plus-specific-override deployment changed the canonical and themed homes to the intended 60-second cache while `/_astro/` and character images retained one-year immutable caching. Live CDP measured the unavoidable overseas Game/Works HTML fetches at `1075/1248ms` during idle warmup, then only `15ms` for the cached Home-to-Game click and `5ms` for Game-to-Works; both click requests were served from disk cache.

### 2026-07-24 Kisara warning-to-black-hole cold-path diagnosis

- Human feedback still reports a one-time hitch perceived at the black/white warning cross, while same-page replays are smooth. A full cold local CDP trace showed that the cross itself first became visible without the largest stall; the dominant dropped frames arrived about `0.55-0.72s` later, during the handoff into the black-hole/collapse branch. The first run recorded roughly `60ms` and `43ms` GPU renderer-main-thread command flushes plus matching `48ms` and `37ms` raster-finalization work, while JavaScript animation-frame work stayed around `13ms`. This is a GPU raster/compositor cold path, so the earlier main-thread `PerformanceObserver` could not see it.
- Before this patch, `data-release-warmup="ready"` meant only that the four JavaScript warmup calls were issued, not that the real full-size rendering paths completed. CSS warmup toggled a class without exercising the actual warning inline-variable writes; the singularity warmup painted only a `4x4` canvas; both WebGL warmups drew transparently, called `gl.flush()`, and cleared in the same task. The full-size `drawScreenEnergy()` black-hole disk, shadows, gradients, particle allocation, and visible WebGL-canvas import therefore still occurred for the first time during the automatic sequence.
- Same-page replay is additionally cheaper because GPU resources and raster pipelines remain hot, and the adaptive renderer can retain a lower quality after a costly first run (`resetGateState()` restores at least `0.88`, not necessarily `1`). Do not treat smooth replay as proof that the cold path is fixed.
- Implemented the compositor-backed warmup without lowering visible quality. Warning/cross properties now use the same centralized inline-variable writer during prewarm and the live sequence, leaving the full property set initialized after a two-frame imperceptible CSS pass. Both WebGL renderers issue representative transparent draws, insert a WebGL2 completion fence, remain active through a real compositor paint, and clear only after the fence settles; `data-release-warmup="ready"` is set only after each asynchronous task completes.
- The black-hole raster warmup now runs on the actual visible Burst Canvas rather than the rejected offscreen snapshot path. It appends three extremely low-alpha representative passes to ordinary early energy frames: the large disk/gradient/shadow family, the full-size singularity field, then lensing ellipses and curved streams. This shares the exact backing surface and compositor route while spreading first-use raster work across frames. Superseded and forbidden: resizing an offscreen canvas to full viewport size and forcing `createImageBitmap()`; that did not share the visible surface and worsened the measured maximum frame gap to about `111ms` with `97/71ms` GPU waits.
- Final repeated cold trace reduced the previously dominant `60/43ms` renderer-main-thread flushes to a maximum observed GPU wait of about `36ms`; the automatic warning-to-black-hole window contained no attributable trace event at or above `12ms`. The RAF probe still observed one `83ms` scheduling gap near `81.7%` burst without a matching long JavaScript or GPU trace event, so human perceptual validation remains the final gate. A fresh blue-state reload reached warmup `ready` with fill/burst/cross at zero, both WebGL canvases inactive at zero opacity, and no browser warning/error logs. `git diff --check`, `npm test` (3/3), and two full `npm run build` passes completed successfully; each build generated 36 Astro pages, sitemap, and a 37-page Pagefind index.
- Lifecycle hardening after review: canceled Burst/WebGL warmups now resolve as unsuccessful and remain retryable instead of falsely marking their task ready; the real Burst warmup has a bounded timeout so a missing 2D context or transient `1x1` canvas cannot block the remaining tasks forever. A force request made while another task is pending now carries into the next task. CSS prewarm cancellation/reset/ClientRouter cleanup restores the default variables before removing the hiding class, preventing a full-strength warning frame from leaking into a page swap.
- Post-fix browser validation reloaded the blue state from a cleared console, reached `ready` with rail progress `0`, cross opacity `0`, both lens canvases inactive at opacity `0`, and no warning/error logs. A continuous first-run interaction then advanced through `awakening -> warning -> singularity -> collapse -> detonation -> reconstruction` while warmup remained `ready`. The raw trace export itself exceeded the browser-control window, so the earlier completed GPU trace remains the quantitative comparison and human perceptual validation remains required. Final local validation passed `git diff --check`, `npm test` (3/3), and `npm run build` (36 Astro pages, sitemap, 37-page Pagefind index).

### 2026-07-24 Kisara footer chibi interaction stage

- Rollback checkpoint is `a82519b`. Replaced the two framed footer blocks (`YUIMI LAB` wordmark and route directory) with one open Q-character stage while retaining the signal divider, social links, copyright row, shared header navigation, audio control, and document scrollbar.
- Added `scripts/extract_kisara_chibis.py` to reproducibly extract normal Kisara, Ayano, Shu, and Sharon from the user-owned untracked `kisara/Q.png`; demon Kisara and the source logo are intentionally excluded. The generator uses edge-connected white-background removal, connected-component filtering, a local Shu hair-intrusion cleanup, one-source-pixel inward alpha erosion, inward feathering, white-matte reversal, rim decontamination, and 2x Lanczos output. Generated runtime PNGs live under `public/themes/kisara/assets/chibi/`; the source `kisara/` directory remains untouched and unstaged.
- Superseded visual treatments: a scaled CSS-mask acrylic contour amplified the low-resolution edge into a blocky halo, and two acrylic-base designs read as a floating ellipse or an unnecessary crystal pedestal. The accepted implementation removes the physical base entirely, keeps only a faint ground-contact shadow, and uses a restrained dark/character-color drop shadow around the newly cleaned alpha edge.
- `KisaraChibiStage.astro` owns four independently draggable Pointer Event characters, velocity tilt, lift/drop/speech bounce, clamped coordinates, smooth nearby-character snapping, non-overlapping group relayout, reset, and pure-emoji cloud bubbles with circular thought-tail dots rather than triangle callouts. Pair, three-character, and four-character groups select separate short emoji sequences; complex groups can speak in multiple ordered beats without visible dialogue text.
- Group membership is explicit rather than inferred from every frame. Dragging a member detaches it from the previous group; dropping near another member merges into that member's existing group and rearranges all participants. Desktop uses a single aligned row; mobile uses a bounded two-column layout for groups of three or four. Crossing the `620px` breakpoint resets to the correct desktop/mobile starting arrangement, while same-breakpoint resizes preserve dragged positions.
- The stage follows the existing ClientRouter lifecycle with one `data-astro-rerun` controller, `AbortController`, `ResizeObserver`, timer/RAF cleanup, and `astro:before-swap` teardown. Browser validation covered solo emoji display, pair/three/four-character snapping, reset, `Blog -> Game` soft navigation rebuilding one ready stage with four characters, and `390x844` zero horizontal overflow. `git diff --check` passed, `npm test` passed 3/3, and `npm run build` generated 36 Astro pages, sitemap, and a 37-page Pagefind index. Fine alpha-edge judgment remains a human visual check because the original Q artwork is only `600x600`.
- Follow-up selection and occupancy refinement: fine-pointer hover and keyboard focus now lift the active character into a role-colored glow with two 1:1 circular side arcs; all four characters derive the arc diameter from height, avoiding the tall Ayano/Shu ellipse distortion. Group expansion no longer recenters around the new actor/target midpoint. The existing group's start edge and baseline are preserved, the actor is appended on its approach side, and the group shifts only by the minimum amount needed to stay inside the stage. Measured desktop expansion retained existing X coordinates across pair (`218/408`), triple (`218/408/580`), and four-character (`218/408/580/926`) joins with no page warnings/errors.

### 2026-07-25 Kisara homepage 001 memory spread

- Rollback checkpoint `c89bdf6` preserves the first framed implementation. The accepted direction removes its white photograph frame, polygon ink-drip overlay, and ring-shaped tear ripples.
- The user-owned untracked `kisara/001.png` is converted to tracked runtime asset `public/themes/kisara/assets/opening-memory-001.webp` at `1800x1013` and `77,518` bytes; the source remains untouched and unstaged.
- Homepage section `001` is now a warm sunset-memory editorial spread: the left side prioritizes the source image's left subject, the right side keeps open copy and route notes, and the image develops from blur to clarity when the section enters view. Superseded: the soft CSS radial fade still exposed a rectangular skeleton and must not be restored.
- Added deterministic `scripts/generate_kisara_memory_mask.py` and tracked `opening-memory-ink-mask.png` (`1280x1000`, about `118KB`). Multi-scale noise creates uneven top/left edges and deep right/bottom notches for the main image window. The source image itself remains a separate full-coordinate layer so later expansion can reveal previously cropped source content without resampling or moving the image.
- The generator now also emits `opening-memory-grain.webp` (`384x384`, about `65KB`) for a subtle repeating paper/film texture. Section `001` combines that grain with uneven vignette, faded light stains, and a slightly desaturated warm image treatment; text remains unfiltered and crisp. Desktop grid tracks were rebalanced and their gap reduced, moving the main right column to about `770px` at `1280px` viewport width with an observed image-to-copy gap of roughly `49px`; the existing single-column tablet/mobile rules remain unchanged.
- Memory diffusion is a distinct post-development stage. `HomePage.astro` adds `is-memory-blooming` from the image's `kisara-memory-image-develop` animation-end event, with a bounded timeout fallback. Superseded implementations: translate/scale overshoot (`81ba168`) visibly drifted; jagged `clip-path` strips still behaved like moving sampled patches; independent radial CSS masks read as falling explosives and separate craters. Do not restore any of them.
- The current implementation draws the complete source into a fixed `960x540` Canvas at exactly the same top/height coordinates as the main image. No droplet or falling trajectory is visible. Deterministic soft brushes grow outward from implied impact points along the right and bottom edges, merge into one continuous capillary-style alpha field, and reveal the hidden source without moving it. A four-sided destination-alpha safety fade reaches zero before the source canvas edges, preventing the original rectangular image boundary from becoming visible.
- The reveal is one-shot per homepage runtime, paints at a bounded roughly `42fps`, follows the existing ClientRouter RAF/ResizeObserver cleanup lifecycle, and renders the final static mask under `prefers-reduced-motion`. The right copy and caption own higher stacking contexts so the expanding image cannot cover text. Local desktop inspection confirmed `transform: none`, viewport-width document geometry, and no browser warning/error logs; fine diffusion shape remains a human visual judgment.
- Follow-up editorial pass removed the profile identity block from section `001`. The right column now begins about `46px` farther left at `1280px`, uses three indented serif paragraphs, a page-margin annotation, and a two-column textbook-style continuation index instead of icon-heavy route rows. The obsolete mobile four-column link override was removed from shared `theme.css`; at `390px`, all four entries remain two columns at `58px` height with zero horizontal overflow.
- The isolated lower-left vertical remnant was the source artwork's first wall seam surviving inside the main procedural mask, not the hidden Canvas edge. Bottom extension generation now begins at `20%` rather than `3%` of image width, and a soft diagonal lower-left suppression field fades the main mask before that seam reaches the paper. Canvas diffusion also excludes lower-left nodes, while its linear safety fade now applies only toward the true right and bottom source boundaries.
- A separate pre-bloom horizontal line came from static mask extensions reintroducing low alpha at the core wrapper's physical bottom edge. Generated-mask inspection measured `278` nonzero pixels on the final row (`max alpha 15/255`). Extension alpha is now guarded by `alpha_bottom ** 0.58` before compositing; the final row and rows 10/30 pixels above it all measure zero, so the non-animated main image cannot be clipped into a straight bottom edge.
- Validation passed `git diff --check`, `npm test` (3/3), and `npm run build` (36 Astro pages, sitemap, 37-page Pagefind index). The existing local dev server remains available at `http://127.0.0.1:4321/`.

### 2026-07-25 Kisara Me epilogue frame

- Added a seventh fixed Me chapter, `END / 谢幕`, after Live Pulse. It uses the final-frame artwork from the user-owned untracked `kisara/tk u for watching.png`; the deployable derivative is `public/themes/kisara/assets/me-epilogue.webp` (`2560x1440`, about `207KB`), while the source remains untouched and unstaged.
- Human review immediately rejected the first contained 16:9 frame because it read as a large picture card and let the page UI cut through the original thank-you subtitle. That treatment is superseded and must not be restored.
- The current epilogue takes over the full viewport, including the space normally occupied by the inner header. Header, chapter HUD, ambient marks, and scroll cue fade away; the chapter rail and persistent audio control remain only as faint recoverable controls. The image enters through a short overexposed/soft-focus settle and then holds a restrained breathing motion. Desktop preserves the original full-frame composition through a near-16:9 cover; mobile uses a face-focused crop with a small bottom epilogue caption because the source is horizontal.
- `npm test` passed 3/3 before the final full-bleed refinement. `git diff --check` and the final `npm run build` passed; Astro generated 36 pages, sitemap, and a 37-page Pagefind index. Exact crop and visual acceptance remain pending user review on the local display.
- Follow-up review rejected desktop `object-fit: cover` because real browser viewports are not reliably 16:9 and the original frame lost content at the top and bottom. Desktop now keeps the complete source with `object-fit: contain`; the uncovered viewport area is filled by a low-saturation, softly blurred continuation of the same frame rather than black bars, a hard container, or a visible card. The old scale-based breathing animation was replaced with a tiny brightness/saturation pulse so it cannot reintroduce cropping. Mobile retains the separate face-focused vertical treatment. The follow-up `git diff --check` and `npm run build` passed with the same 36-page/sitemap/37-page Pagefind output.

### 2026-07-25 Kisara 003/004 first-entry stability and footer skyline

- Homepage section `004` and the Blog index now share `src/themes/kisara/lib/coverPresentation.ts`. Covers with width/height ratio `>= 0.85` use the established cropped presentation and focus metadata; true portrait covers retain the contained-poster treatment, so mixed cover formats no longer produce avoidable black side bars in `004`.
- The first-entry flash around sections `003/004` was traced to delayed section materialization plus cold image/filter work. `content-visibility: auto` now remains only on section `002`; sections `003/004` keep stable layout before first entry. Event `003` uses the optimized `picture.webp`, decodes before camera focus begins, starts from its intended soft-focus frame, and uses a lighter focus transition. Section `004` no longer creates duplicate blurred background layers for cropped covers.
- The footer Q-character stage now has an unframed Beirong City night backdrop with near/far building silhouettes, lit windows, a signal tower, a restrained grid, and faded stage lettering. Mobile reduces the skyline height and removes the tower; reduced-motion disables its ambient movement.
- `git diff --check`, `npm test` (3/3), and `npm run build` (36 Astro pages, sitemap, 37-page Pagefind index) passed. The user subsequently accepted this pass without further visual recheck.

### 2026-07-25 Kisara 001/003 replay reset and Me edge fade

- Homepage sections `001` and `003` now re-arm their entrance presentations whenever the user returns to the hero. The clue scene resets immediately because it is already far off-screen; section `001` waits until the smooth upward return has covered it before clearing its reveal/bloom classes, Canvas generations, timers, observers, and pending RAF work. This prevents a visible face-to-face reset while ensuring the next descent starts from the initial state.
- The `003` custom element exposes a bounded `resetPresentation()` lifecycle hook. It closes any open reward dialog, restores `data-state="idle"` and the initial copy, cancels a pending camera-focus frame, and installs a fresh one-shot visibility observer. Section `001` similarly re-arms its observer and can replay both the develop and diffusion stages without refreshing the page.
- The desktop Me `END / 谢幕` artwork now preserves the complete source inside its actual rendered rectangle and fades only the left and right edges of that image outward before they meet the blurred continuation layer. Mobile retains the existing face-focused cover crop and does not apply the desktop edge mask.
- Same-tab interaction validation covered `001 revealed/blooming + 003 traced -> upward damped return -> blue hero -> SKIP -> second descent`. The return settled at `scrollY=0`, `001` retained only `is-memory-armed`, `003` returned to `idle`, and the second descent replayed `001`, camera focus, and hotspot tracing. Browser warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` passed; the build generated 36 Astro pages, sitemap, and a 37-page Pagefind index.

### 2026-07-25 Kisara screenshot refresh and site-wide WebP completion

- Rollback checkpoint is `d3c123e`. The user-owned untracked `kisara/new0042.png` and `kisara/new116.png` remain untouched. `scripts/prepare-kisara-refresh.py` creates deployable derivatives, repairs the upper-right capture mark with a feathered local background reconstruction, and removes the refrigerator subtitle glyphs without replacing the foreground with a hard rectangular overlay.
- Replaced the 002 refrigerator runtime pair with fresh `1920x1080` derivatives: `public/themes/kisara/assets/fridge-discovery-002.webp` and its regenerated blurred veil. Replaced the Game hero with the cleaned `1920x1080` `game-night-hero.webp` and synchronized the legacy PNG slot. Astro dimensions now match the actual derivatives.
- Expanded `scripts/optimize-images.mjs` to recursively scan `public`, skip up-to-date WebP siblings, preserve alpha assets losslessly, and preserve animated GIF frame counts/timings when producing animated WebP. All 67 static PNG/JPEG sources and both animated GIF sources now have WebP counterparts; original sources remain in place for compatibility and future editing.
- Runtime references for Kisara masks, fight background, reward image, profile, chibi figures, and Kagari profile/brand/scroll/404 assets now prefer WebP. Favicon and Apple Touch PNG references remain for broad browser compatibility while their WebP siblings are also available.
- Validation: no missing raster-to-WebP counterparts, `git diff --check` passed with only existing line-ending notices, `npm test` passed 3/3, and `npm run build` generated 36 Astro pages, sitemap, and a 37-page Pagefind index. The local preview remains at `http://127.0.0.1:4321/`.

### 2026-07-26 Kisara footer apple-throw easter egg

- Local rollback checkpoints are `a2a88d0` for the browser-console heart/key easter egg and `9aca48d` for the first complete footer apple scene. Neither checkpoint was pushed during this task.
- The user-owned untracked source `kisara/apple.MP4` contains the same short throw four times. The runtime scene keeps only frames `0..13`, crops the right-side watermark out of the composition, keys the green screen, and ships as `public/themes/kisara/assets/effects/apple-throw.webm` (VP9 alpha, `740x540`, about `137KB`) with `apple-throw.webp` as the transparent animated fallback (`640x468`, about `226KB`). Do not restore the rejected `delogo` treatment because the watermark overlaps the arm and leaves a visible repair patch.
- `KisaraChibiStage.astro` triggers the scene only when Kisara is placed beside Ayano twice within nine seconds: the first placement plays `😤💢 -> 😏🍎 -> 👀🍎❓`, and the second enters a short Galgame-style cut-in with wash, speed lines, impact flash, throw/hit reactions, and automatic left/right mirroring. The scene has a 12-second cooldown; reset re-arms it. Media preloads only when the footer approaches the viewport, supports Astro soft-navigation cleanup, and becomes a lightweight reaction under reduced motion.
- Human feedback found the first `340..680px` cut-in too obscured by the two foreground Q characters. The media now uses approximately `414..830px`, about 22% larger at both desktop and mobile sizes. Narrow screens may crop a small amount of the already feathered media edge instead of shrinking the subject again. Center clamping now handles media larger than the stage by using an even center crop, preventing the enlarged layer from being pushed toward one edge.
- Browser geometry checks measured an `830px` computed desktop media width inside the `1220x560` stage and `414px` at a `390px` viewport, with document `scrollWidth` remaining exactly `390px`. No page warning/error logs were emitted. `git diff --check`, `npm test` (3/3), and `npm run build` passed; the build generated 36 Astro pages, sitemap, and a 37-page Pagefind index. Fine visibility behind the two Q characters remains a human visual judgment.

### 2026-07-28 Kisara footer jealousy easter egg

- The user-owned untracked source `kisara/emoji.png` remains untouched. Its explicit runtime copy is `public/themes/kisara/assets/chibi/jealousy-emoji.png` and is loaded only after the first qualifying interaction.
- `KisaraChibiStage.astro` now counts interaction events whose final group contains Shu plus Ayano and/or Sharon but not Kisara. Pair interactions, a three-character interaction, and repeated use of the same character each count as one event. After two hits, only the immediately following group interaction containing both Shu and Kisara triggers the easter egg; any other group interaction clears the armed sequence.
- The trigger gives Kisara a compact screenshot bubble first, then Shu responds with `😰`. The existing reset button clears the sequence together with the apple counters, while normal emoji bubbles remove the temporary image-bubble treatment before reuse.
- Validation passed `git diff --check`, `npm test` (3/3), and `npm run build` (42 Astro pages, 43-page Pagefind index). Bubble scale and timing remain pending human visual validation.

### 2026-07-26 Kisara lower-home damped section navigation

- Rollback checkpoint before this pass is `1af7243`. Desktop homepage content after the hero now treats `001`, `002`, `003`, `004`, and the footer as five explicit stops. A single wheel gesture advances to the adjacent stop through the existing spring scroller with a section-specific near-critical damping profile; trackpad inertia extends a short quiet guard instead of queuing extra sections. Keyboard directional inputs use the same stops. Touch/mobile keeps the existing free-scroll behavior because the snap controller is limited to `(min-width: 981px) and (hover: hover)`.
- Returning from `001` to the hero now requires two distinct upward gestures within a `1600ms` confirmation window. The first gesture is consumed at `001`; the second starts the established damped hero return and replay reset. Gesture serials distinguish a new wheel action from the tail of the first action, preventing one long trackpad swipe from satisfying both steps.
- Desktop band sizing was tightened without changing the mobile single-column layouts. At a fresh `1280x720` runtime, measured heights changed to approximately `779px` (`001`), `700px` (`002`), `798px` (`003`), `601px` (`004`), and `782px` (footer). The two former outliers were about `1040px` and `1433px`; both now fit inside the roughly `800px` CSS viewport produced by the user's common 90% browser zoom. Section `003` uses a more compact two-column event board and three-column objective row; `001` keeps its editorial composition with reduced dead space.
- Fresh-module browser validation used a temporary dev server on `127.0.0.1:4322` and confirmed exact settled stop positions `720 -> 1608 -> 2430 -> 3324 -> 4111`, plus `001` first-up armed at `720` and second-up returning to `0`. The temporary server and test tab were closed afterward. Page warning/error logs were empty. `git diff --check`, `npm test` (3/3), and `npm run build` passed; the build generated 36 Astro pages, sitemap, and a 37-page Pagefind index.

### 2026-07-26 Kisara lower-home compactness and 001 return blend

- Kept `8bb10fe` as the rollback point before this visual pass. Desktop `003` now uses a narrower photo column, reduced section padding, tighter brief spacing, and a compact border-box clue panel moved farther right so it no longer covers the extension line; mobile layout overrides remain separate.
- The `001` memory ink bloom now starts at `2050ms` after reveal instead of `2600ms`, bringing the ink spread into the tail of the image development rather than leaving a long pause.
- Returning from lower stops to `001` now reactivates the same fixed live gate bridge used by the initial downward handoff instead of substituting a blue fallback gradient. The bridge remains behind `001` while that stop is active, then is finalized or released when section snapping continues to `002`, so both directions share one transition style.
- Runtime check at `1280x720` measured `003` at about `689px`; the expanded clue panel measured about `108px` wide with roughly `17px` clearance from the SVG line endpoint. `SKIP -> 001`, `002 -> 001`, and the subsequent `001 -> 002` return path all switched bridge classes as expected. `git diff --check`, `npm test` (3/3), and `npm run build` passed; the preview page emitted no warning/error logs.

### 2026-07-26 Kisara hero wheel pacing and blade handoff

- The hero keeps the existing spring-driven chain and memory-image timeline, but discrete mouse-wheel input now receives cadence-sensitive gain. A slow isolated notch stays close to the former precision, while a sustained same-direction roll ramps smoothly toward roughly double input; reversing direction or pausing resets that momentum. Precision trackpad deltas keep a restrained near-1:1 response.
- The post-enchantment blade phase receives a separate input multiplier, and its manual endpoint moves from `0.42` to `0.37`. This removes most of the visually empty wheel distance after the blade gloss has left the title without shortening the automatic warning/black-hole timeline.
- The remaining blade-to-autoplay handoff now pushes the scene camera inward by up to about `5.5%` with a slight upward drift, then releases that push across the warning-cross build. The visual remains reversible because it is derived entirely from `burstProgress`.

### 2026-07-26 Kisara hero autoplay control and release pacing

- Rollback checkpoint before this pass is `7706442`. A lightweight `AUTO` control now sits beside `SKIP` without adding another panel. It advances the existing hero timeline rather than duplicating effects: about `4.6s` for the blue/red chain-memory phase, the established `2.15s` enchantment bridge, about `0.85s` for the blade phase, then the existing automatic warning/singularity/reconstruction sequence. It stops in the completed liquid-title state and never calls the transition into section `001`; the next downward user input still owns that move.
- The control starts from the current timeline position, reports `RUNNING` while active, and becomes `REPLAY` after completion. Replaying resets to the blue initial state first. Downward input is consumed while the automatic run is active; upward wheel/touch/keyboard input cancels the controller and returns the existing reversible manual timeline to the user. Reduced-motion users receive the completed state without replaying the full animation.
- The previous first-run/session split (`3000ms` first automatic release versus `1750ms` on repeats) was removed. Both first and later runs now use one `1850ms` release duration, close to the formerly smooth replay cadence. The existing staged warning/WebGL warmup remains independent and is forced as soon as `AUTO` starts, so first-use preparation no longer depends on stretching the visible black/white-cross timeline.
- Initial local use exposed two runtime-only faults despite the successful build. A newly inserted button could remain unbound when an already-running gate kept `data-bound=true` across HMR/DOM replacement, and the automatic controller's first RAF could advance by less than the global settle threshold, immediately stopping the animation while leaving `RUNNING` visible. The scheduler now detects an unmarked replacement control, cleans up the stale runtime, and rebinds without refresh; the control marks its runtime ownership. Automatic playback also starts with a normal first-frame interval and is excluded from the manual settled-state shutdown while active.
- Same-tab validation covered the stale-button recovery without refresh and one complete automatic run. At about `420ms`, fill had reached `11.668%`; the run finished at `fill=100%`, rail progress `1`, `releaseMode=complete`, button state `REPLAY`, and `scrollY=0`, with no warning/error logs. The existing preview tab was kept open. `git diff --check` passed, `npm test` passed 3/3, and the final `npm run build` generated 36 Astro pages, sitemap output, and a 37-page Pagefind index.

### 2026-07-26 Kisara 001 background-tab bridge recovery

- The `001` procedural ink bloom fallback now starts at `1700ms` after the image reveal instead of `2050ms`, so the lower/right diffusion begins earlier while the source image is still resolving.
- The shared hero-to-`001` live bridge could lose its visible fade after a long background-tab suspension because the runtime trusted the retained fixed compositing layer and did not rebuild the return path when the document became visible again. Recovery now covers every lower-home stop: while parked below `001`, it rebuilds the blue hero and edge-texture state off-screen; when a section-return targets `001`, it force-remounts the same live hero shell before damped scrolling begins. If the tab resumes while already on `001`, the bridge is remounted in place. `visibilitychange`, `pageshow`, window focus, and initial restored-position startup all use this bounded recovery, while active scroll transitions are left untouched and retry at the `001` arrival callback if suspension interrupted the handoff.
- `git diff --check`, `npm test` (3/3), and `npm run build` passed; Astro generated 36 pages, sitemap output, and a 37-page Pagefind index. Long-idle visual confirmation remains pending human browser validation because actual compositor eviction cannot be reproduced reliably by a short automated check.

### 2026-07-26 Site-wide mobile performance and loading pass

- Rollback checkpoint before this pass is `e10b5e1`. Added one shared `PerformanceProfile.astro` probe to all three layouts. It classifies the current client as `full`, `mobile`, or `lite` from pointer type, viewport, reduced-motion preference, Save-Data/2G status, memory, and core count; the profile is refreshed after resize/orientation/network changes and now drives bounded runtime budgets instead of scattered width-only guesses.
- Kisara automatic route warming now runs only on the full desktop profile and preloads at most the first two priority exits. Mobile keeps one-at-a-time, direct user-intent warming on hover/focus/press; Lite, Save-Data, offline, and 2G-class clients skip custom warming. This prevents the four homepage exits and their route CSS from competing with the hero runtime on mobile while preserving fast desktop navigation.
- Kisara hero scenes now assign later memory/transformation image URLs only when their existing progress thresholds request a decode. Mobile/Lite Canvas DPR caps are approximately `1/0.9`, the main energy loop is capped at `30/24fps`, post-release motion uses `30/24fps` with lower idle rates, WebGL lens buffers and field textures use smaller mobile budgets, and adaptive particle quality cannot climb back to desktop density. The completed release's otherwise mostly idle energy loop now updates at a much lower cadence. Desktop remains on the full-resolution/full-rate path.
- `KisaraChibiStage` moved from a repeated inline runtime to a cacheable Astro module that rebinds through `astro:page-load` and retains the existing before-swap cleanup. The generated Kisara homepage HTML fell from about `139.4KB` before this pass to `113.8KB` (`~18%` smaller); the shared Chibi runtime is a `14.4KB` hashed asset. Mobile no longer preloads the apple-throw video 600px before the footer and instead loads it only when the easter egg is actually triggered.
- Touch devices no longer run the Game hover-parallax or the Works custom cursor renderer, while drag interaction remains active. Blog keeps high fetch priority only for the scene background/front character and first article cover; BFCache returns on Mobile/Lite settle directly instead of replaying the full multi-layer intro. Kisara audio still persists on navigation/page hide, but routine progress writes move from every 3 seconds to every 5 seconds on desktop and 10 seconds on Mobile/Lite.
- Fuyukawa mobile backgrounds no longer use fixed attachment, mobile Sakura count is reduced from 34 to 18 (12 on Lite), the pig scrollbar is RAF-throttled, footer timers pause in hidden documents, date/time formatters are reused, and the tag-rain Canvas uses reduced DPR, `30/24fps`, two collision passes, and a true hidden-tab RAF stop.
- Verification passed `git diff --check`, `npm test` (3/3), and `npm run build`; Astro generated 36 pages, sitemap output, and a 37-page Pagefind index. The existing `http://127.0.0.1:4321/themes/kisara/` preview returned HTTP 200 with the new performance probe and external Chibi module. Fine mobile sharpness and animation feel remain pending human device review; no source images, user-owned untracked assets, audio behavior, or desktop effect stages were removed.

### 2026-07-26 Kisara 001 foreground bridge atomic resume

- Rollback checkpoint before this correction is `5d9fc14`. Human screenshots invalidated the previous background-tab recovery: the accepted `001` state is not a generated transition strip. Its upper paper layer fades to transparency and must continuously expose the currently rendered live hero image underneath.
- Root cause was a resume race introduced by `e10b5e1`: `visibilitychange`, `pageshow`, and focus could queue overlapping recovery frames while restored scroll geometry was still transient. The recovery also forcibly removed and re-added both bridge classes. A false early position check therefore exposed the static edge fallback first; its delayed texture render then appeared as a second gray translucent overlay.
- The runtime now snapshots whether the live bridge is active before the document becomes hidden and keeps that portal mounted. Foreground resume reasserts the existing portal idempotently before restarting effect loops, without tearing down the real hero layer. Recovery callbacks require an outstanding recovery flag at scheduling time and again in both RAF stages, so late focus/pageshow callbacks cannot overwrite an already restored live bridge.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass. Astro generated 36 pages, sitemap output, and a 37-page Pagefind index. Long background/foreground visual confirmation remains a human browser check because compositor eviction and browser scroll-restoration timing are not deterministic in a short local run.

### 2026-07-26 Kisara mobile hero chain and image continuity pass

- Rollback checkpoint before this pass is `aef477b`. The user-owned untracked asset directories and loose images remain untouched. The existing preview continues on `http://127.0.0.1:4321/`; no additional preview server was started.
- Mobile chain rendering now uses mobile geometry instead of shrinking the desktop composition. The frame loop has wider vertical depth, fewer links, one cached full-ring sprite pass for ordinary links, no per-link live side-metal gradients, and no full-canvas mask/drop-shadow filters. The two weave layers enter later in sequence so the frame and interleaving chains do not collapse into one horizontal pile on the smaller title.
- The main mobile hero spring is capped at 30fps (`24fps` on Lite) while preserving elapsed-time integration. Mobile memory scenes keep crossfades and breathing transforms but use a stable light soft-focus rather than recomputing a full-screen blur every frame. Runtime style writes are cached and fine-grained mobile values are quantized to avoid needless compositor invalidation.
- Scene slots now retain the current visible image until the incoming image is decoded. Decode completion explicitly requests a render, preventing a fast mobile timeline from fading the old scene before the next background exists. The refrigerator media also has a non-animated CSS background fallback beneath both animated image layers.
- Mobile `AUTO` and `SKIP` share a safe-area-aware row; the guidance copy is moved above them. `AUTO` was verified through a touch-coordinate click without Playwright auto-scroll: it entered `playing`, reached about `23.3%` after `0.95s`, and kept both active scene backgrounds populated.
- Mobile idle work was reduced without changing desktop behavior: four ambient particles remain (three on Lite), their secondary halos are disabled, the second scrollbar guide chevron is removed, the footer wave is static, and the off-screen `003` hotspot/reward animations remain paused until the section or dialog is active.
- A `390x844` browser profile measured the three-second idle task duration falling from about `1.25s` to `1.01s`, with style recalculation falling from about `0.46s` to `0.30s`. The active chain sample fell from about `2.15s` total task duration and `1.11s` style recalculation before this pass to about `1.78s` and `0.64s`. These are development-preview measurements, useful for relative comparison rather than production-device guarantees.
- Twelve timeline samples found no visible scene slot without a background image. The mobile chain Canvas settled at `401x109`; a human-scale screenshot check at about `36%` showed the outer frame and first weave as distinct rows rather than one dense clump. Fine feel and device-specific GPU behavior still require the user's real-phone validation.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass. Astro generated 36 pages, sitemap output, and a 37-page Pagefind index.

### 2026-07-27 Kisara lower-home viewport fit and 003 layout

- Rollback checkpoint before this pass is `73c31b4`. Desktop-only rules compact `001`, `002`, and `003` without changing the mobile layouts or the established hero/lower-home scroll controller.
- At the existing `1280x720` preview viewport, measured section heights are approximately `612px` (`001`), `550px` (`002`), and `592px` (`003`). `001` uses a wider editorial image ratio and tighter lesson spacing; `002` removes an unnecessary refrigerator-title wrap and reduces vertical inventory spacing.
- `003` no longer sacrifices the photo column when the section is shortened. Its fixed compact stage uses an approximately `661x382px` left photo against a `519px` right investigation panel, with the photo metadata/caption overlaid inside the camera image and the objectives restored to a compact vertical sequence. A dedicated top inset keeps the section title below the fixed site navigation.
- The in-photo `CLUE 01` card now sits farther left (`right: 6.5%` on desktop), about `35px` inward from its former final position at the measured viewport, bringing it closer to the clue-line endpoint.
- Runtime checks found no horizontal overflow, no right-panel content overflow, and no browser warning/error logs. `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 36 pages, sitemap output, and a 37-page Pagefind index. Fine visual spacing remains subject to the user's browser judgment.

### 2026-07-27 Kisara 003 hidden-track reward

- Rollback point before this feature is `8d372b8`. The previously excluded source `kisara/ココロスペアキー.mp3` is copied to tracked runtime asset `public/themes/kisara/audio/kokoro-spare-key.mp3`; the user-owned source remains untouched and untracked.
- Completing the `003` reward now offers two explicit choices. `立即播放?!` temporarily unlocks a twelfth track and switches the persistent lower-left player to `ココロスペアキー`; `先收下吧` adds the track to the current tab's playlist without interrupting the current song. Both choices finish the case and close the reward dialog.
- The unlock lives only in an in-memory `window` session. It survives Astro soft navigation in the same document, but a full reload or new tab restores the normal eleven-track playlist. Hidden-track id/progress are never written to the existing local-storage record; the last normal track, time, and enabled state remain the persistence fallback.
- While active, the hidden song gives the player a restrained pink highlight. Its completion returns to the previously active normal track and position; manual previous/next exits cleanly into the temporary twelve-track playlist. A user pause/resume on the hidden track still updates the normal persisted playback intent.
- Browser validation covered both dialog branches. `先收下吧` kept `本気モード` playing and changed the temporary status to `SECRET 12 / 12 · UNLOCKED`, then `10 / 12 · PLAYING`. `立即播放?!` loaded `/themes/kisara/audio/kokoro-spare-key.mp3`, reached `readyState=4`, played from `0.65s`, and showed `12 / 12 · PLAYING`. Reload returned to `本気モード`, `10 / 11`, with no secret styling or browser warning/error logs. `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 36 pages and a 37-page Pagefind index.

### 2026-07-27 Kisara shared article-cover aspect fitting (superseded)

- The Blog archive and homepage `004` already shared `getKisaraCoverPresentation()`, but portrait covers were still forced into one `7:10` frame with `object-fit: contain`. Covers narrower or wider than that frame exposed the dark backing as visible side/top bars; the new `cover-02.webp` (`1145x1800`, about `0.636`) and `cover-07.webp` (`1400x1867`, about `0.750`) exposed both cases.
- The shared presentation helper now returns a measured aspect ratio from Sharp metadata. Portrait covers below the existing `0.85` crop threshold use their natural frame ratio in both surfaces, while near-square/landscape covers retain the established `7:10` crop treatment. New article covers therefore inherit the behavior without filename-specific overrides.
- Generated HTML confirmed matching dynamic variables in both surfaces (`--archive-cover-aspect` and `--transmission-cover-aspect`), including `0.636111...` and `0.749866...` for the two new articles. `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 42 pages and a 43-page Pagefind index.

### 2026-07-27 Kisara uniform article-cover frames

- Human review found that natural portrait frame ratios removed the dark backing but created uneven article-card heights. The dynamic frame-ratio approach above is therefore superseded.
- Blog and homepage `004` now keep one stable `7:10` frame for every article. Every cover uses `object-fit: cover` with the existing per-cover focus position, so portrait, square, and landscape sources fill the same frame without black bars or vertical card drift. Source dimensions and the portrait/crop label remain available from the shared Sharp helper, but no longer resize layout geometry.

### 2026-07-27 Codex article guidelines repository section

- Superseded the standalone repository-introduction section after human feedback that it repeated the preceding development-note and `AGENTS.md` material.
- Actual examples from `skills/maintain-development-notes/SKILL.md`, `reference/AGENTS.md`, `references/rule-catalog.md`, and `workflows/ADOPT.md` are now integrated into the existing relevant sections: the complexity gate, verified status vocabulary, canonical-note discovery, stable behavior IDs, semantic policy comparison, and separate write approval.
- The repository link now closes the `AGENTS.md` discussion instead of opening a separate promotional-looking section. No repository instructions were executed from remote content; those files were used only as article source material.

### 2026-07-27 Codex article final-use file walkthrough

- Human review rejected the previous integration because repository-specific names appeared before the repository was introduced and the prose still focused on adoption machinery rather than the files used after installation.
- Downloaded repository commit `d8f8614d1b6dab3f6c75784547979b62fc128311` to a temporary review directory and rebuilt the article section from the actual final-use files only: `reference/AGENTS.md`, `skills/maintain-development-notes/SKILL.md`, `skills/maintain-development-notes/references/note-schema.md`, and `skills/maintain-development-notes/agents/openai.yaml`.
- The new combined section introduces the repository context first, explains all 27 `AGENTS.md` rules, walks every operational section of the Skill, covers all nine note-schema sections, explains each YAML field, and places the repository link only after the four files have been connected. Adoption/update workflow files are intentionally omitted from the article body.

### 2026-07-27 Kisara mobile chain removal and 003 photo continuity

- Human mobile testing still found severe hero jank during the early chain phase. The prior mobile path only reduced chain geometry and frame rate, but still created two Canvas 2D contexts, generated chain paths/sprites, and performed a chain draw call in the energy loop. Mobile and Lite now skip those contexts entirely, skip the draw call, and hide both chain canvases; the reversible image/color progression remains intact.
- The `003` clue-frame image could appear decoded and then become blank on mobile. Its `picture.webp` source is valid (`1516x1422`, opaque WebP, ~129KB); the likely risk is the high-cost full-image focus animation (`filter` plus `transform`) being promoted into an unstable mobile compositing surface. Mobile/Lite now use one static clear image style with no image animation or `will-change`; desktop camera-focus behavior remains unchanged.

### 2026-07-27 Kisara 003 desktop camera search

- Desktop fine-pointer devices now treat the `003` photo as an interactive viewfinder. Pointer movement pans the crop vertically and slightly horizontally, motion speed drives a bounded blur, and a short quiet period triggers one autofocus search. The anomaly button stays disabled and invisible until the stopped framing is within both horizontal and vertical target tolerances; mobile/Lite retain the approved static photo and immediately available hotspot.
- Human review caught the clue extension line showing before the anomaly was clicked. The entire SVG line layer is now opaque only in `traced` or `solved`, so focus lock reveals only the hotspot and the existing click remains the sole transition that draws the clue path and card.
- Rollback checkpoint before the closer-framing correction is `854ef56`. The desktop view is now approximately `1.12x`, starts from the upper part of the source, and has enough horizontal overscan for bounded left/right movement without exposing an edge. `npm test` passes 3/3 and `npm run build` passes with 42 Astro pages and a 43-page Pagefind index; final framing tolerance and motion feel remain for human mouse validation.
- High-DPI mouse feedback showed that even the faster adaptive damping still lagged behind a `1600dpi` pointer and that the first magnetic target was too strict. That approach is superseded: pointer input is now coalesced to one direct camera write per animation frame, and `pointerleave` contributes one final clamped edge sample so a fast exit still reaches the intended framing. The active input also reaches full range before the outer `10-13%` edge band. Near-hit autofocus now uses a substantially wider rectangular capture area and animates the final horizontal/vertical correction into the exact clue framing before revealing the hotspot.

### 2026-07-27 Kisara 001 bottom-edge fade

- Human screenshots identified the visible horizontal seam at the actual bottom of `001`; an earlier experimental internal radial wash targeted the wrong upper area and was removed.
- `001` now uses a short `42-56px` bottom overlay that fades directly into `--kisara-memory-paper-deep`. The following memory-to-fridge transition begins with the same `#e4d8d1` color and the same grain opacity, so the section edge no longer switches abruptly while the existing inter-section height and scroll geometry remain unchanged.
- Desktop browser inspection at `2048x697` confirmed the fade reaches the measured `001` boundary with only the established `3px` overlap. `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 42 pages and a 43-page Pagefind index. Fine seam perception remains subject to human visual confirmation.

### 2026-07-27 Kisara 002 to 003 lower-glow refinement

- Human review identified the lower white halo inside the `002 -> 003` bridge as too tall; the established `3px` overlap, opaque endpoint seals, and overall bridge height were not the problem and remain unchanged.
- The base gradient now holds its dark and mid-gray range longer and reaches the light `003` endpoint only near the bottom. The right-side radial glow was moved into the lower half and reduced in opacity/blur, while the full-width bottom mist shrank from `42%` to `20%` height with lower opacity and blur.
- Browser checks at `1280x720` and `390x844` kept the bridge-to-`003` join continuous and reported zero horizontal overflow. `git diff --check` passes, `npm test` passes 3/3, and `npm run build` passes with 42 Astro pages and a 43-page Pagefind index; final halo thickness remains subject to human visual confirmation.

### 2026-07-27 Kisara Blog article/timeline views

- Rollback checkpoint before this feature is `eca6f91`. The Blog archive now has two accessible same-page tabs: the existing article grid and a new chronology view. Switching views does not reload the four-character hero, and the selected view is kept in session storage so article navigation and reloads return to the same archive mode.
- The chronology groups published posts by year and keeps the existing newest-first order. Each entry exposes its month/day, channel, title, description, tags, and a compact cover; channel-specific node colors and a continuous signal rail distinguish it visually from the article-card grid. Mobile collapses the chronology onto a left rail with compact records and stable cover dimensions.
- Search and channel filters operate on both rendered views while result counts are deduplicated by post id. Empty year groups disappear during filtering, keyboard arrow/Home/End navigation works across the two tabs, and reduced-motion disables the new hover transitions.
- Validation passed `git diff --check`, `npm test` (3/3), and `npm run build` (42 Astro pages, 43-page Pagefind index). Browser checks confirmed article/timeline switching, a `Codex` search returning one unique signal in both views, session view restoration after reload, and zero horizontal overflow at `390x844`.

### 2026-07-28 Kisara Blog archive reading strip

- The user-owned untracked source `kisara/read.jpg` remains untouched. Its explicit runtime copy is `public/themes/kisara/assets/blog/read-interlude.jpg`.
- The archive heading is now an open horizontal night-reading scene rather than a plain text gap or framed image. The source is enlarged and vertically cropped into one panoramic strip, with Kisara kept on the left and the archive copy moved onto the empty dark wall at the right. Top and bottom masks fade the scene back into the page instead of exposing a hard image edge.
- Desktop, tablet, and mobile keep separate crop anchors; all three were shifted upward after user feedback so the framing retains more of the upper scene. The production build passed before the final crop-only adjustment, and the final CSS change passed `git diff --check`; detailed crop acceptance remains a human visual check.

### 2026-07-27 Kisara Me Episode 11 memory sequence

- Rollback checkpoint before this redesign is `e82b50f`. `TECH` through `PULSE` retain the existing personal data but are rebuilt as five full-screen Episode 11 memory scenes instead of repeating the former light dossier/list layout. The official Episode 11 story, `優しく愚かな嘘`, was used only to establish the visual progression and emotional framing.
- User-owned sources `kisara/e1.png` through `e5.png` remain untouched and untracked. Tracked runtime assets live in `public/themes/kisara/assets/me/episode11-e1.webp` through `episode11-e5.webp`; each is a deterministic 16:9 crop resized to `1920x1080`, which removes the top-right platform watermark and, for `e3`, the bottom subtitle without generative reconstruction artifacts. Final files are approximately `80-122KB` each.
- The five transition grammars are deliberately distinct while retaining one-wheel/one-scene navigation and reversible upward movement: aircraft-window lateral reveal (`e1`), circular ritual focus (`e2`), diagonal amendment split (`e3`), right-to-left sunrise wash (`e4`), and perspective contract-sheet reveal (`e5`). Scene input blocking now matches the longest `860ms` transition, and the existing Profile/Epilogue scenes remain in the seven-step rail.
- A browser check found that focusing rail buttons could programmatically scroll the `overflow:hidden` Me root by about `82px`, exposing a bottom band in later scenes. The root now uses `overflow: clip` and resets its internal scroll position on every scene change. Desktop and `390x844` checks showed loaded scene images, zero content/page horizontal overflow, reversible wheel navigation, stable root scroll position, and no page console warnings/errors. Fine transition timing and text-over-image balance remain subject to human visual review.
- Human review accepted the overall Episode 11 direction but rejected the repeated rectangular list/table treatment. Rollback point before the typography refinement is `08f957a`. `TECH` now keeps only a vertical right-half flight-instrument tick arc and text arranged along that arc; the rejected oversized shell, rectangular boundary, internal circles, crosshair, horizon bar, and icon discs are removed. Fine-pointer movement updates bounded `--flight-roll` / `--flight-tilt` values through one queued animation-frame write, with cleanup on scene exit and Astro navigation.
- `ANIME` now places all eight titles on the feet of a CSS contract circle in the lower-right, with concentric rings, radial divisions, a central sigil, staged node entry, and the heading lifted clear of the formation. `XP` no longer presents four equal cards or a horizontal attribute row: a hand-drawn private-signature path develops across the lower image and the four amended preferences arrive at separate turns in that signature.
- `GAMES` now holds a dark high-contrast exposure through the opening reveal, lifts it into a pronounced warm overexposure, and settles into the sunrise grade. The effect uses the existing image, motion, and flare layers and replays whenever the scene becomes active, including reverse navigation.
- `PULSE` now removes the translucent signal table entirely. Its three larger, tightly spaced signal lines are raised into the same counter-clockwise angle as the source contract writing, then crossed out in sequence by three clipped, uneven CSS ink strokes per line. Non-active state clears the strokes so returning to the scene replays the writing; mobile reduces the tilt and offsets while preserving the same composition.
- Desktop broad-layout checks found no console warnings/errors, and the responsive rules keep the flight arc static, scale the contract circle, and compress the private signature on narrow screens. The browser test surface remains unreliable for clicking the tiny fixed mobile rail dots, so exact mobile scene composition stays for real-device/user judgment. `git diff --check` passes, `npm test` passes 3/3, and `npm run build` passes with 42 Astro pages and a 43-page Pagefind index. Subtle sunrise timing, tick weight, stroke weight, and exact text placement remain for user visual judgment rather than screenshot-based acceptance.

### 2026-07-27 Kisara Me contract kaō and shared-save typography

- Rollback checkpoint before this refinement is `d186569`. Human review rejected the regular `ANIME` contract circle and every subsequent orbit-style variation: concentric or offset loops still read as an atom diagram, relationship graph, or tangled wire regardless of line weight. That circular composition is superseded and removed from both markup and CSS.
- `ANIME` now uses a horizontal wet-ink contract kaō in the lower-right. A pressure-weighted wine-red stem, a single hooked body stroke, a tail, and an underline form the signature; blue-gray and pink threads begin only after the stem is visibly underway, then alternate beneath and above the main stroke through dedicated under/over path layers. Eight titles attach to irregular turns and ink pools rather than an enclosing perimeter.
- The first kaō implementation used SVG turbulence/displacement to force hand-drawn edges. Human review correctly identified the result as either hair-thin or visibly jagged. The displacement filter and `non-scaling-stroke` path are removed: core strokes now use smooth geometric curves with approximately `6-7px` main weights and `3.5-4.5px` thread weights, while a separate blurred bleed layer supplies soft wet-ink spread without damaging the crisp edge.
- Internal drawing delays account for the existing `860ms` scene transition. The stem begins after the image is materially visible; the body and two colored threads enter around its midpoint, followed by short over-crossing segments, ink pools, labels, and a restrained highlight. Reduced-motion renders the settled final signature directly, and mobile uses a compact `14:9` field with no SVG filter.
- `GAMES` no longer presents four evenly spaced numbered rows. Its header and four shared saves sit in the left dawn shadow as a slightly angled typographic trail with larger translucent save numbers, staggered title placement, fine rays toward the sunrise, and one traveling glint per line. The existing dark-to-warm sunrise reveal remains intact.
- Browser checks at `1280x720` and `390x844` found zero horizontal overflow and no warning/error logs. `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 42 pages and a 43-page Pagefind index. Fine kaō shape, ink-pool placement, and text balance remain explicitly subject to human visual review.

### 2026-07-28 Kisara Game two-scene rebuild

- Replaced the accumulated Game page with two stable full-viewport scenes: the `game1` interrupted-message opening and the `game2` arcade room. The old night hero, manga speech bubble, long heading block, duplicated arcade shells, and layered 2D/3D control-deck overrides are removed rather than covered by another CSS patch.
- User-owned sources `kisara/game1.png`, `kisara/game1.mp4`, and `kisara/game2.png` remain untouched and untracked. Runtime assets are `public/themes/kisara/assets/game-message-hero.webp`, `game-message-hero.mp4`, and `game-arcade-room.webp`. The five-second H.264 hero clip keeps `1280x720` video, drops the unused audio stream, uses fast-start metadata, and is reduced from about `7.9MB` to about `2.16MB`.
- Each real entry to the Game route resets to the first scene and plays the muted hero clip once from time zero. It does not loop or replay during same-page up/down navigation; the ended video remains on its final frame. The still WebP remains the poster/fallback, and the former duplicated image-echo layers were removed so the source motion carries the hand-afterimage effect.
- The arcade keeps the existing three game choices and explicit `START`-before-iframe behavior, but now uses one continuous gunmetal body with a shared highlight/shadow model across marquee, bezel, control deck, joystick, buttons, speaker, coin slot, and base. Width is constrained by viewport height so the complete machine stays inside a `1280x720` scene instead of pushing its controls below the fold.
- Desktop scene navigation uses one damped spring and no CSS scroll snap. The earlier combination let browser snapping override each incremental `scrollTo`, which felt like an instant jump. The replacement uses `24` stiffness, `9.4` damping, a bounded velocity, and delays active-scene handoff until `34%` travel; an observed downward trace was `0 -> 127 -> 442 -> 671 -> 720px` over roughly `1.25s`. Mobile retains only proximity snap and native smooth behavior.
- Browser checks covered direct load, `Projects -> Game` Astro navigation, one-shot video playback through `ended`, wheel movement in both directions, complete machine fit, local `2048` iframe launch, zero horizontal overflow at `1280x720`, and empty warning/error logs. `git diff --check`, `npm test` (3/3), and `npm run build` pass with 42 Astro pages and a 43-page Pagefind index. Exact transition feel and final visual balance remain subject to user review.

### 2026-07-28 Kisara Blog generated character layers

- The four-character Blog hero no longer depends on mutually occluded segmentation masks alone. A Luna subagent used the user-authorized third-party OpenAI-compatible endpoint with `gpt-image-2` to create one complete isolated candidate each for Shu, Kisara, Ayano, and Sharon. The temporary API credential was process-scoped only and is not stored in the repository, generated prompts, or this note.
- Source candidates remain under `output/imagegen/blog-character-reconstruction/luna/`. `scripts/prepare_kisara_blog_generated_layers.py` now uses the already-installed `isnet-anime.onnx` model for the subject matte; chroma color is only a background trimap and edge-decontamination source. Nearest trustworthy interior colors replace green/purple/magenta spill without globally eroding hair or weapon tips, and opaque interiors are hardened separately from soft boundary strands. SIFT/RANSAC still aligns each result to the accepted `1440x975` stage.
- Human annotations showed that several large defects were not matting failures: generated Shu clothing and Ayano hair/body/equipment were protruding through Kisara's separated hair gaps. Active `*-generated-v5.webp` assets therefore keep generated pixels but constrain Shu/Ayano to softly expanded envelopes derived from their accepted original visible masks. A local convex Kisara hair-occlusion volume blocks only the confirmed overlap regions. Ayano's isolated magenta strand cluster is removed directly, Kisara's unwanted green weapon ovals are locally inpainted, and Sharon receives targeted internal repairs for the green hair/hand block and blue fleck in the raised white stocking.
- Human feedback rejected the first final-state crossfade because generated and original Kisara silhouettes briefly produced two characters and two blades. That approach is superseded. The Blog hero now shows only the four independently generated layers, preserves the established front-to-back entrance order and back-to-front stacking, and leaves those four layers in place after the intro completes; it does not swap to the original group image.
- Automated checks against the annotated regions reduced Shu's confirmed leak from `938` high-alpha pixels to `0`, Ayano's isolated upper magenta cluster from `2213` to `165`, and the two right-side Ayano leaks by roughly `80%` and `85%`; the remaining pixels are principally connected blue-hair content. Kisara's marked green weapon pixels measure `0` after repair. `git diff --check`, `npm test` (3/3), and `npm run build` pass with 42 Astro pages and a 43-page Pagefind index. Final edge and overlap judgment remains a human visual check.

### 2026-07-28 Kisara Blog complete solo extraction

- The `*-generated-v5.webp` envelope workaround is superseded for the Blog hero. `scripts/extract_kisara_solo_characters.py` now produces four complete transparent source PNGs under `output/imagegen/blog-character-reconstruction/final/` and the active `1440x975` runtime layers `*-solo-v1.webp` under `public/themes/kisara/assets/blog/`.
- The extraction pipeline uses `ISNet-Anime` only for semantic topology, a chroma/background field for trimap evidence, and PyMatting only along the solved contour. Pixels more than three pixels inside the subject are forced opaque and retain source RGB, preventing the former translucent clothing and missing-hair failures.
- Final character placement no longer applies the old occluded-group SIFT transform to the complete bodies. Each solo asset is cropped to its alpha bounds and fitted independently into a stable stage box, preserving complete heads, clothing, weapons, and legs while retaining the accepted back-to-front order `Sharon -> Ayano -> Shu -> Kisara`.
- Human annotation exposed four internal generated-color artifacts that edge metrics did not catch: Sharon's green torso/arm patch, Ayano's magenta face-side patch, and Kisara's green chest and weapon patches. The script now repairs those exact source ROIs with per-region OpenCV methods: nearest trustworthy interior propagation for Sharon and Kisara's weapon, Telea inpainting for Kisara's chest, and an inverse-aligned accepted layer plus inpainting for Ayano. Kisara also receives a one-pixel RGB decontamination pass on hair/scythe edges and a local one-to-two-pixel alpha contraction on the hook and skirt edge.
- Final four-background QA passes for every solo layer. A comparison against the user's annotated composite confirmed all four internal artifacts removed without transparent holes, rectangular patches, duplicated facial details, or damaged clothing/weapon silhouettes. `npm test` passes 3/3 and `npm run build` passes with 42 Astro pages and a 43-page Pagefind index.
- Human page review rejected the first complete-body placement because it read as four independent street-standing cutouts. The final stage boxes now form one right-weighted triangular group: Sharon occupies the upper rear silhouette, Ayano enters from the right rear, Shu overlaps the left-middle plane, and Kisara binds the group as the foreground center. The full solo assets remain intact, but their deliberate overlap restores a shared focal point and depth without returning to the old incomplete masks.

### 2026-07-28 Kisara Blog hero scale and Works trash alignment

- The complete four-character Blog stage was visually smaller than the former full-group composition. The shared desktop stage grows from `72vw / 1320px` to `88vw / 1600px` with a slightly farther-right anchor; the mobile stage grows from `122vw / 760px` to `138vw / 840px`. The solo assets and their accepted triangular placement remain unchanged.
- The Works kitchen trash lid was positioned against the full stretched drop-zone button while the body was grid-centered, separating the two pieces in the two-column appliance layout. A dedicated `.kisara-trash-can` wrapper now keeps the lid and body aligned as one centered unit at every button height.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 42 pages and a 43-page Pagefind index. Fine Blog character scale remains subject to human visual judgment.

### 2026-07-28 Kisara Blog sharp layers and Me memory-return secret

- The Blog hero no longer applies blur independently to the four transparent character layers. Their entrance order, translation, saturation/brightness depth, shared atmospheric glow, and scan pass remain, but removing per-layer blur prevents four soft rectangular silhouettes from reading as separate masks.
- User source `kisara/记忆归还.mp3` remains untouched and untracked. Runtime asset `public/themes/kisara/audio/kioku-kikan.mp3` joins `ココロスペアキー` in a generalized multi-secret playlist. Unlocked secret tracks participate in previous/next and natural playlist wrap while keeping the normal persisted track/time separate.
- Visiting all five Episode 11 Me memories (`TECH` through `PULSE`) reveals a low-opacity `11` memory trace in the final contract scene. Activating it archives `记忆归还` without interrupting the current song. The mark then settles into an archived state, while the player uses a cool memory variant with light-fragment particles instead of the demon track's pink heart treatment.
- Superseded persistence rule: storing secret unlocks in `sessionStorage` allowed browser restoration to revive already solved hidden tracks. Both `ココロスペアキー` and `记忆归还` are now current-document, in-memory unlocks and are never serialized. A full reload or new document returns to the eleven normal tracks; stale empty/interrupt secret-session records are removed rather than retained as an empty shell.
- Current hidden-track contract: each successful 003/Me unlock grants one queued playback. Hidden tracks are appended in the order they are actually unlocked, not fixed configuration order. Completing either track removes only that track before selecting the next queued item or restoring the interrupted normal song. Replaying requires a fresh scene unlock; returning from the lower homepage to the hero already resets 003 for another investigation.
- `edgeone.json` needs no new rule for this delivery: the existing `/themes/kisara/audio/*`, `/themes/kisara/assets/*`, and `/_astro/*` immutable rules already cover the new MP3, runtime images/video, and hashed bundles. No cache configuration change was made.

### 2026-07-28 Kisara 002 refrigerator composition

- Human review rejected the three equal inventory cells and the oversized dark gap beneath `002`. The refrigerator interaction and reveal runtime remain unchanged; only the section composition and responsive sizing were rebuilt.
- Desktop `002` now places the main discovery copy and active detail on the left while the three selectable findings form an uneven right-side stack. Each finding has a different width and offset, uses an open-ended cold-glass strip instead of a shared framed grid, and keeps immediate hover/selection feedback after its staged entrance.
- The intermediate `500-610px` desktop compaction is superseded. Human review clarified that `002` should itself be a complete viewport scene, not a shorter section that exposes the following transition while parked. Desktop/tablet now use at least `100svh`; mobile uses `max(100svh, 760px)` so the scene still fills the viewport without clipping its stacked content. The `002 -> 003` bridge remains outside that viewport and appears only after the next downward move.
- A later `16:9` viewport screenshot showed that most of the remaining apparent empty space was not uncovered media: the shade layer had already turned the final `18%` of the image nearly black. The open image now retains a `1.052` scale with a higher focal anchor, and the shade keeps source pixels visible until roughly the final `6-12%`, reserving only a short dark seal for the transition into `003`.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass with 42 Astro pages and a 43-page Pagefind index. Exact spacing and the shortened bridge remain pending human visual judgment; no browser screenshot is treated as acceptance for this subtle layout pass.

### 2026-07-28 Kisara 001 memory-page recomposition

- Human review kept the sunset/ink-memory direction but rejected the remaining empty space, generic explanatory prose, and the four route shortcuts forced into the section. The Blog/Game/Works/Me links were removed from `001`; global navigation remains the route entry surface.
- The old independent heading, image, prose, and shortcut rows are replaced by one overlapping editorial spread. The memory image grows beyond its grid track, the two-line title presses into the image edge, and a low-opacity `17:42` imprint occupies former dead space without adding a panel.
- The first overlap pass placed a high-opacity borderless paper wash behind the copy. Human review correctly identified it as another independent layer that hid the procedural ink. That wash is removed on every breakpoint; the copy is pulled back toward the paper side, and readability now uses only small glyph-local paper-colored shadows so ink remains visible between and behind the text.
- Follow-up review requested the copy itself participate more strongly in the memory reveal. The text column now moves farther left into the exposed ink boundary, and the former whole-block fade is superseded by build-time character spans. Each paragraph preserves one complete `aria-label` while its visual characters fade in sequentially with a brief wine-colored ink haze. Characters remain native inline text with no per-glyph transform layer, preserving Chinese line breaking and avoiding a mobile compositor spike. Re-entering `001` after the existing reset replays the writing sequence.
- `001` now derives depth of field from three cached uses of the same source inside the accepted ink mask: a mildly softened wall/background base, a sharp elliptical Kisara focus layer, and a darker heavily softened right-side foreground silhouette. All layers reuse the established development timing through layer-specific final filter variables; reduced-motion renders the settled depth composition directly.
- Follow-up depth review keeps that separation through the entire development animation and its settled frame. The clear mask is narrowed around Kisara instead of restoring a broad oval of wall detail, the environmental base remains visibly soft at completion, and the right foreground figure uses an unshifted curved near-field mask with stronger blur so it reads as an intentional occluder rather than a duplicated image edge.
- The copy now describes concrete retained fragments from the source scene and the actual blog-building process instead of generic memory language. Paragraph widths and indentation vary deliberately, while the page-edge note remains a narrative annotation rather than navigation.
- Existing procedural ink diffusion is unchanged. The original image-development animation now resolves the three depth layers together, while the heading note, copy, and caption retain their lightweight reveal timing. No new Canvas, runtime observer, asset request, or independent animation loop was added. Tablet and mobile keep the overlap with bounded widths and hidden overflow.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass with 42 Astro pages and a 43-page Pagefind index. Final typography balance and overlap depth remain pending human visual judgment.

### 2026-07-28 Kisara Me profile prelude

- Checkpoint `1c56758` is a rejected intermediate, not the active design. Human review correctly identified that adding five faded previews and scan effects did not repair the underlying composition: the oversized dark dossier card wasted most of its area, while the opposite side repeated project-status copy, generic facts, a formulaic English display title, and low-value annotations already seen elsewhere on the site.
- The active Profile scene removes that card, all status/fact grids, the generic title, and the five-preview collage. Useful content is retained and made primary: the portrait is integrated into the light page with a fading print treatment, the real display name and handle become the signature, a concrete interest-driven working statement replaces the skill list, and GitHub/Bilibili remain direct open links.
- Foreshadowing is now one focused transition device rather than background clutter. A narrow diagonal aperture at the right edge reveals only Episode 11 memory `01`; when Profile yields to TECH, the aperture opens wider and the image settles toward the incoming full scene. This implies the later sequence without competing with the Profile content or showing five miniature screenshots at once.
- Entry motion is bounded to portrait development, one scan, staggered copy, and one aperture seam. Tablet/mobile compress the portrait into an open identity row, reduce the aperture, and preserve readable links; reduced-motion keeps the settled layout. No full-viewport animated filter or repeated image-shadow loop was introduced.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass with 42 Astro pages and a 43-page Pagefind index. Final composition, aperture width, and copy balance remain pending human visual judgment; no automated screenshot is treated as acceptance.

### 2026-07-28 Kisara Me epilogue rollback and hidden rail endpoint

- Human review rejected the experimental mouth-centered lens-contact fog and heart/key condensation pass from checkpoint `6ca133d`. The experiment is fully superseded and must not be partially restored; END is back to the previously accepted full-frame soft-focus/exposure entrance and restrained image pulse.
- The epilogue remains the seventh wheel/touch/keyboard scene and retains its reversible transition behavior, but it is no longer represented as an `END` item in the visible chapter rail. The rail now renders only Profile through Pulse, including a six-column mobile layout; internal scene indexing still keeps END at index `6` so navigation state does not shift.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass with 42 Astro pages and a 43-page Pagefind index. Human visual acceptance of the six-item rail remains pending.

### 2026-07-28 Kisara Me Pulse handwritten cancellation

- Pulse no longer places the oversized `YUIMI LAB` wordmark in the center of its footer. The footer now keeps only the left channel status and the right Yuimi-chaya/GPT attribution, leaving the contract artwork unobstructed.
- The lower two title lines are explicit handwritten rows rather than one italic serif phrase. They use a local system handwriting stack, small line-specific offsets, and dark contract-ink coloring with a restrained pink bleed so they sit closer to the amended writing already present in Episode 11 scene `e5`.
- Each row owns a broad irregular dark cancellation stroke and a slightly displaced thinner pink return stroke. The rows are crossed in sequence on scene entry through the existing bounded `kisara-me-hand-strike` keyframe; reduced-motion renders the completed crossed state directly. No extra DOM overlay, font download, image request, or animation loop was introduced.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass with 42 Astro pages and a 43-page Pagefind index. Exact handwriting character and strike weight remain pending human visual judgment.
- Follow-up styling extends the same handwritten contract language to the three upper-right current-signal rows. Labels and descriptions now share a Xingkai/Kaiti-first local system stack with normal weight, subtle ink bleed, and a slightly different baseline turn per row; numeric indices deliberately remain monospaced editorial marks. No font asset or network request was added. The follow-up `git diff --check`, `npm test` (3/3), and `npm run build` also pass with 42 Astro pages and a 43-page Pagefind index.

### 2026-07-28 Kisara Me Games sunrise bond and shared saves

- The Games headline now resolves as three deliberate rows: `Time held`, `with both`, and `hand to hand`. The first `hand` carries Kisara pink and the second carries Shu green. The middle `to` owns a fixed-width column from the first frame, preventing layout shift, but remains blurred and invisible until the same `1780ms` timeline used by the sunrise image reaches its brighter middle/late phase.
- The intermediate numbered save spine and pink/green connecting threads from checkpoint `472689e` were rejected as unnecessary structure and are superseded. Games now renders only the four title/subtitle terms, with no save numbers, rail, endpoint, glint, or line-animation markup.
- Desktop constrains the four terms to a lower-left `min(650px, 40vw)` two-by-two grid and reduces both type scales. The second row is shifted to the right, while small per-item y/rotation differences keep both rows loose without spreading into the characters or right side of the scene. Follow-up human review pulled the right column farther left through negative/modest item offsets while preserving the row stagger. Tablet/mobile retain a compact two-column layout with smaller offsets. Reduced-motion still settles the simple item entrance directly.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass with 42 Astro pages and a 43-page Pagefind index. Exact headline scale, sunrise timing, and lower-left term spacing remain pending human visual judgment.

### 2026-07-29 Kisara found-self Home interlude

- Trigger scope is deliberately narrow: `AboutPage.astro` records that scenes `0-4` were actually visited, disqualifies the run once Pulse or END is entered, and arms `yuimi-kisara-found-self-ticket-v1` only when the next destination is the Kisara Home route while Games remains active. The ticket is same-tab, timestamped, expires after 30 seconds, and is removed immediately by Home before initialization.
- The original GIF plus standalone-SE runtime is superseded and must not be reconnected. The authoritative user source is the untouched/untracked `kisara/Ep12.mp4` (`3840x2160`, `26.07s`, H.264 plus AAC original audio, about `180MB`). The site uses the fast-start `public/themes/kisara/assets/found-self-ep12.mp4` (`1920x1080`, H.264 plus AAC, about `11.5MB`) and `found-self-ep12-first.webp`; the old `found-self-memory.mp4`, poster, and `found-self-se.mp3` may still exist as unused historical assets.
- The normal title, chain, meter, black-hole canvases, particles, Auto control, and Skip control are hidden rather than mutated during the interlude. The dedicated handwritten `Kisara` remains absent through the early scene, begins an uneven whole-glyph dark-ink reconstruction around video time `20.75s`, and completes the pink color wake near `25.5s`, synchronized with the hand-written message entering and filling the source frame. The rejected polygon-fragment approach split every letter into upper/middle/lower wedges and looked like ordered triangular pieces; do not restore it. Human review also removed the hand-origin tether line, its point light, the separate final-frame image swap, the settled exposure change, and all post-video breathing; the accepted ending is the video's own completely still final decoded frame with no final flash.
- Wheel, touch, and keyboard directional input are captured for the entire automatic playback. The embedded video audio owns the scene while the normal playlist is suspended; there is no second SE element to drift out of sync. Playback begins only once the video actually starts, with pointer/touch/key/wheel retry hooks for autoplay-policy rejection. After settlement, the first downward intent or the dedicated Skip link restores the original song and calls the existing `enterNextPage()` transition. The interlude then unloads its video and clears all entry classes. Returning upward from `001` uses the existing `completeGateReturn()` reset and therefore shows the untouched blue initial gate.
- `KisaraAudioControl.astro` now supports the non-persistent `yuimi:kisara-audio-suspension` event. It pauses the current main/secret track without changing the user's enabled preference, disables player controls while suspended, preserves the exact track/time, and resumes only when the matching interlude id releases it.
- Earlier functional browser checks verified the positive Profile-to-Games trigger, rejection of a direct Profile-to-Games jump, locked normal title/audio state during playback, absence of the removed line/light/final-frame layer, Skip scrolling to `001`, exact main-track resumption, and empty page warning/error logs. After the Ep12 media migration, `npm test` passes 3/3 and `npm run build` passes with 42 Astro pages and a 43-page Pagefind index. The new `26.07s` original-audio playback and the retimed `20.75-25.5s` title window remain pending human visual/audio review.

### 2026-07-29 Kisara Home bridge continuity

- The normal Home camera breath no longer derives directly from absolute `performance.now()` time. A dedicated clock advances only while the gate presentation is actively painted, pauses when the gate loop is stopped/reset or the document is hidden, and resumes from the same phase. Returning from `001` therefore paints the reset blue gate at the exact camera `x/y/scale` phase already held by the opening bridge instead of briefly recentering and beginning a new breath.
- The found-self bridge deliberately does not capture a video frame and does not introduce another generated transition texture. The ordinary blue gate remains pre-rendered underneath the interlude; during downward exit the fixed found-self layer fades and lifts above that live base while the existing spring scroll runs. Cleanup is owned by the scroll completion callback, with a longer timer used only as a stalled-transition fallback, so the bridge cannot become empty before the normal scene is visible.
- Once the interlude has dissolved during the downward transition, later upward navigation naturally returns to the ordinary blue gate. This avoids both failure modes discussed during design: freezing a special video frame that would have to swap visibly on return, and replacing it with blue immediately while the user is still looking at the first viewport.
- Follow-up human review found that the first dissolve rule never became visible: the active found-self selector had higher specificity and kept forcing `opacity: 1` until cleanup removed the layer. The leaving selector now explicitly targets both active entry forms with higher specificity, so the overlay remains visible but transitions to zero opacity while the blue gate is revealed underneath.
- The same two-layer handoff now covers every normal Home image state, including the base blue scene, memory sequence, transformation/smoke frames, distortion canvases, and fight composition. A cached blue underlay lives inside the existing camera transform while the complete current composition is grouped above it; forward travel to `001` crossfades only those two layers, adding no screenshot, canvas loop, or additional image request.
- The first normal-bridge pass exposed a return-side replay: removing the dissolve class allowed the current composition to animate from its lifted/transparent exit pose back to rest, which looked like a page refresh. Return/reset now keeps the blue underlay visible, resets the hidden current composition, disables its transition for one forced style frame, and hands control back at `opacity: 1` with a neutral transform. The next frame restores transition rules without replaying an entrance.
- The title had remained outside that bridge, so returning directly from `001` could still expose red fill, chains, enchantment, gloss/blade light, and title Canvas state until `resetGateState()` cleared them in front of the viewer. The title now owns a co-located initial blue-gray underlay. During the late return motion the complete current title loses opacity, saturation, and focus into that underlay; after the hidden reset, the real initial title overlaps back over `460ms` before the underlay is released. This keeps all title effects under one parent opacity without duplicating their procedural Canvases.
- A rejected intermediate added a bright diagonal edge sweep to the title handoff. Human review correctly read it as a lightning strike followed by a refresh; that sweep and its CSS variables are removed and must not be restored. The active handoff is intentionally low-contrast and has no flash line.
- Wrapping the title exposed one coordinate assumption in the burst renderer: `title.offsetLeft/offsetTop` became relative to the new title stage and moved the black-hole origin near `0,0`. `resizeBurstCanvas()` now derives title coordinates from the title and burst-Canvas bounding-rectangle difference, restoring the event well to the wordmark center and making future wrappers coordinate-safe.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 42 pages and a 43-page Pagefind index. A lightweight browser check confirmed identical title/underlay bounds and fonts with no page warning/error logs. Exact return dissolve feel and black-hole placement remain pending human visual validation.

### 2026-07-29 Kisara Me XP compositor stabilization

- Human testing found an occasional white/compositor flash when entering XP from either adjacent memory scene. XP was the common transition path and uniquely changed the full-screen scene from a four-point `polygon(...)` clip to `inset(0)` while simultaneously animating opacity, horizontal translation, skew, scale, and the background image filter. The incompatible clip-shape transition could force Chromium to rebuild the full-screen compositing surface for the entering scene.
- XP no longer animates a scene-sized clip. Its before, active, and after states all retain `clip-path: inset(0)` while preserving the directional translation/skew/scale, local contract marks, text staging, and amendment-cut animation. The active state now keeps a zeroed `translate3d` transform and the scene hides its back face, avoiding a second layer demotion/recreation when the transition settles.
- This is deliberately scoped to XP. Other Me transition grammars and all image/filter values remain unchanged; if hardware testing still exposes a smaller flash, the next isolated fallback is to remove only XP's full-frame image `filter` interpolation rather than weakening every memory scene.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 42 pages and a 43-page Pagefind index. The intermittent GPU artifact remains a real-browser/hardware validation item because DOM or screenshot checks cannot reliably certify a missing compositor frame.

### 2026-07-29 Blog cover library expansion

- Fourteen newly added JPG source images in the ignored local `文章封面/` directory were prepared as `public/blog-covers/cover-11.webp` through `cover-24.webp`. Existing `cover-01` through `cover-10` and all current article frontmatter remain unchanged; the new covers are an unassigned library for later articles.
- Assignment follows source addition order: `FE2B... -> 11`, `5438... -> 12`, `8ABA... -> 13`, `C77D... -> 14`, `92C5... -> 15`, `96C0... -> 16`, `1FC0... -> 17`, `73CD... -> 18`, `10D3... -> 19`, `78C9... -> 20`, `3615... -> 21`, `E062... -> 22`, `C807... -> 23`, and `7382... -> 24`.
- Conversion uses Sharp auto-rotation, WebP quality `82`, effort `6`, smart chroma subsampling, a `1400px` maximum width, and no enlargement. All original aspect ratios are preserved. The fourteen runtime assets total about `2.05MB`, down from about `4.36MB` of source JPEGs (`53%` smaller).

### 2026-07-29 AI emotional attachment article

- Added `src/content/blog/is-it-strange-to-feel-for-ai.md` from the supplied manuscript. The source prose is retained; only the duplicate body-level H1 is omitted because the article layout renders the frontmatter title.
- The article is categorized as `life`, uses tags `AI`, `LLMRP`, `情感`, `记忆`, and `陪伴`, and assigns the reflective `cover-17.webp` from the newly prepared cover library.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass. Astro generated 45 pages and Pagefind indexed 46 pages, including the article under default, Blank, and Kisara routes.

### 2026-07-29 Blog short-sentence punctuation pass

- The original pass removed `221` Chinese full stops, but its visible-character threshold was incomplete: longer list items and lines ending after inline-code or other Markdown wrappers could retain a final `。`, producing inconsistent punctuation inside the same list. That length-based rule is superseded and must not be reused.
- The follow-up uses Markdown block semantics instead. Every list item and blockquote now drops only its terminal Chinese full stop, while an ordinary standalone paragraph drops it only when no earlier `。` exists in that block. Long multi-sentence paragraphs retain all internal boundaries and their final punctuation.
- The structural follow-up removed another `88` full stops across eight affected articles. A diff audit confirmed that every changed line differs only by one removed `。`; a second scan found no terminal full stops remaining on list items, blockquotes, or single-sentence prose blocks. Frontmatter, headings, fenced code, paths, commands, URLs, wording, BOM state, and existing CRLF/LF style remain unchanged.
- `git diff --check` reports no patch errors and `npm test` passes 3/3. The earlier full production build remains valid because this follow-up changes punctuation only; no new build was run for the content-only correction.

### 2026-07-29 Kisara Me epilogue audio and one-shot memory track

- The hidden Me END scene now treats its full-frame image as an unobtrusive audio button. The first click during each END entry plays the new `public/themes/kisara/audio/end.mp3` one-shot, temporarily suspends the theme playlist, then resumes the exact prior enabled state and position. A second click in the same entry is ignored; leaving END stops and resets the one-shot so a later re-entry can trigger it once again.
- `记忆归还` now declares `consumeAfterPlayback: true`. When it finishes during normal playlist flow, the player removes `kioku-kikan` from the in-memory unlock set and active track list, clears its secret-session state, emits a consumption event, and continues with the next normal track. The Me archive mark clears for that page instance and cannot be immediately re-armed without a fresh Me sequence.
- Functional browser checks verified END first-click suspension, automatic release after the 2.525-second clip, ignored second click, successful reset after leaving/re-entering END, and `kioku-kikan` changing from an unlocked secret track back to an empty unlock set plus normal track `01 / 11`. Browser warning/error logs were empty. `npm test` passes 3/3 and the final `npm run build` passes with 45 generated pages and a 46-page Pagefind index.

### 2026-07-29 Kisara Me narrative title pass

- TECH now treats both `Carry the tools.` and `TOOLS ON BOARD` as cold gray inventory language rather than an accent-colored declaration. This intentionally echoes the initial gray Kisara state and the point where Shu still regarded her primarily as a usable tool; no new motion was added.
- XP splits `never neutral.` into two independently colored words. A cold-blue contract line and a muted-pink line write inward from the words, then settle into two small interlocking rings at the center. The title therefore reads as an established connection rather than three decorative hyphens, while retaining a compact mobile width and a static reduced-motion endpoint.
- ANIME treats `a clause.` as an amended contract phrase: a faded formal serif version remains underneath, a pink handwritten version overwrites it once the scene becomes active, the article `a` is inserted above the baseline, and a short uneven underline completes the note. The treatment reuses the scene's existing handwritten-contract language without another frame, Canvas, full-screen filter, or continuous animation.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 45 pages and Pagefind indexed 46 pages. A lightweight browser layout check found no XP/ANIME title overflow at `1280x720` or `390x844`, and page warning/error logs were empty. Exact handwriting character, connector weight, and narrative emphasis remain pending human visual judgment.

### 2026-07-29 Shared SEO identity metadata

- Added `src/core/SeoMeta.astro` as the single identity/SEO source used by the Fuyukawa, Blank, and Kisara layouts. Search and social metadata now identify `喝益胃` as the display author, `Yuimi-chaya` as the handle, and `Yuimi Lab` as the site name through `author`, `creator`, `publisher`, `keywords`, `application-name`, Open Graph, Twitter summary, and `rel=author` tags.
- JSON-LD now exposes a stable `Person` node on every canonical page. The canonical home also exposes `WebSite + WebPage`, while `/about/` uses `ProfilePage` with the Person as its main entity; article pages retain a canonical author URL and normal WebPage identity without inventing unavailable publication metadata.
- Blank and Kisara keep `noindex,follow` plus canonical default-theme URLs, and `/themes/` remains excluded from the sitemap. This deliberately avoids indexing three visual copies of the same content while still keeping complete metadata in every generated document.
- The primary canonical/sitemap origin remains `https://yuimi-chaya.github.io` from `astro.config.mjs`. Switching search ownership to the EdgeOne custom domain is a separate high-impact decision and was not bundled into this identity pass.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 45 pages and Pagefind indexed 46 pages. A generated-HTML audit confirmed the expected author, keyword, Open Graph, article-author, robots, and JSON-LD values on the canonical home, About, an article, and the noindex Kisara home.

### 2026-07-29 AstrBot deployment article search targeting

- The canonical article keeps its existing slug `/blog/astrbot-napcat-baota-deploy/`, but its visible and search title now begins with `AstrBot 部署教程`: `AstrBot 部署教程：宝塔面板 + NapCat 搭建 QQ 机器人`. The original publication and modification dates both remain `2026-05-19`; the metadata pass must not present itself as a content update.
- Blog frontmatter now supports optional `seoTitle`, `seoDescription`, and `seoKeywords`. The AstrBot article uses focused terms including `AstrBot 部署`, `AstrBot 部署教程`, `宝塔部署 AstrBot`, `NapCat 部署`, `QQ 机器人部署`, `OneBot v11`, and `WebSocket`, while all other articles continue to fall back to their existing title, description, and tags.
- Article layouts now pass the full frontmatter into the shared SEO component. Canonical article pages emit `WebPage + BlogPosting` JSON-LD with headline, description, author, publication/modification dates, category, tags, cover image, and main-page relationship, plus article Open Graph dates/tags and a large-image Twitter card.
- The sitemap continues to contain exactly one canonical AstrBot article URL and no `/themes/` copies. Blank and Kisara article variants retain `noindex,follow` and point back to the same canonical article.
- `git diff --check`, `npm test` (3/3), and the final `npm run build` pass; Astro generated 45 pages and Pagefind indexed 46 pages. Generated HTML confirms the focused SEO title/description/keywords, `BlogPosting` cover and tags, exactly one sitemap URL, and both `article:modified_time` and `dateModified` restored to `2026-05-19`.

### 2026-07-30 Kisara canonical root migration

- Kisara now owns `/`, `/blog/`, `/games/`, `/projects/`, `/about/`, canonical article paths, and the root 404. The displaced Fuyukawa frontend remains fully available under `/themes/fuyukawa-kagari/...`; the existing `/themes/kisara/...` output is retained only as a backward-compatible alias.
- Theme path generation special-cases `DEFAULT_THEME_ID`, so Kisara links resolve directly to canonical paths even though its historical prefix remains registered for canonical stripping. Fuyukawa navigation, article rows, Pagefind result links, context-menu Home, Live2D Home detection, and 404 recovery now use the prefixed route family.
- The preference key changed from `yuimi-theme-id-v1` to `yuimi-theme-id-v2`. This is a deliberate one-time reset: `v1` was automatically populated with the former default for ordinary visitors, so migrating it would make the new default appear ineffective on most returning browsers.
- Root Kisara documents no longer emit `noindex` or `data-pagefind-ignore`. Blank, Fuyukawa, and legacy prefixed Kisara documents retain `noindex,follow`, canonicalize to root content URLs, and remain excluded from the sitemap. `edgeone.json` needs no change because its root HTML rule and existing theme asset rules already cover the new ownership.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass. Astro generated 60 pages and Pagefind processed 61 HTML documents including the standalone game document. Generated-HTML checks confirmed root Home and AstrBot article as indexable Kisara pages, both alternate theme homes as noindex canonical aliases, prefixed Fuyukawa navigation, and zero `/themes/` URLs in the sitemap.

### 2026-07-30 Kisara Home 001 memory-image replacement

- The untouched local source is `kisara/new001.png` (`2560x1440`). Home 001 now uses the stripped `1920x1080` runtime asset `public/themes/kisara/assets/opening-memory-001-v2.webp` (about `78KB`) for the hidden Canvas source, base image, subject-focus layer, and foreground-depth layer. The former `opening-memory-001.webp` remains available but is no longer referenced by 001.
- The old `opening-memory-ink-mask.webp` rectangle is no longer used by the main image. The replacement core uses three bounded radial mask lobes tailored to the lower-left embrace and central candle light. Every lobe reaches transparency before the visual's top and right bounds, preventing the container itself from becoming a visible straight crop edge.
- The first replacement attempt is superseded: quadratic `addFlow()` paths produced obvious strings of equally spaced dark smoke blobs across the copy, and oversized mask lobes still touched the top/right container edges. Do not restore that path-based layout.
- The active Canvas diffusion uses overlapping two-dimensional `addWash()` fields around the core's right interior, lower edge, upper interior, and lower-right detail. Its reveal is limited to the source's warm middle region; horizontal and vertical alpha falloffs end before the original image boundaries, including a transparent top guard. The core mask begins smaller and expands while the Canvas blooms after `480ms`, so the feathered/ink edge is formed during the reveal instead of appearing as a finished static border on the first frame.
- New depth treatment keeps the embracing pair and candle/book area sharper while softly defocusing the surrounding wall and dark frame. Desktop and mobile use separate source height/top/left variables to keep the lower-left subjects in view.
- The 001 editorial copy is enlarged through real font and layout dimensions rather than CSS scaling. Its usable width is now `640px`, the lead/body/margin-note type is larger, and the desktop block moves substantially left into the image's warm negative space. Tablet also uses a wider left-biased copy column; mobile retains restrained overrides.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass. Astro generated 60 pages and Pagefind indexed 61 documents. Exact mask contour, ink-wash placement, and copy overlap remain pending human visual validation.

### 2026-07-30 Kisara Home 002 refrigerator footage and physical inventory

- The untouched local source is `kisara/Ep08.mp4` (`3840x2160`, `1.710s`, H.264 plus AAC, about `3.9MB`). `scripts/prepare-kisara-fridge-video.py` removes the subtitle by cropping the lowest `260px` together with the matching horizontal margin, preserving a clean 16:9 composition rather than attempting a visibly repaired subtitle strip. Runtime outputs are `fridge-opening-002.mp4` (`1920x1080`, muted H.264, about `445KB`) plus first/last WebP posters.
- The former CSS curtain, blurred still, simulated light reveal, selectable finding strips, changing detail copy, and right-side inventory frames are superseded. `KisaraFridgeScene.astro` now owns the full 002 scene: the native door-opening footage plays once on entry, pauses on its decoded final frame, and exposes one restrained left-side editorial caption only after the door has opened.
- UE, AstrBot, and Blog are three independent transparent inline SVG objects: a folded milk carton using the Unreal Engine mark, a metal can with the AstrBot star label, and an irregular chip bag. They are not placed in cards, framed capsules, or a shared background.
- A bounded three-body DOM physics loop launches the objects from the top after video completion. Bodies collide with each other and all four scene boundaries, retain restrained throw velocity, and support pointer capture, window-level move/release handling, keyboard nudges, and focus/visibility cleanup. The loop stops when settled or outside the viewport, avoiding a permanent homepage animation cost.
- Home replay reset and the local replay button intentionally use different arming paths. Returning toward the hero requires 002 to leave the observer range before a new run can start; otherwise the second opening can complete offscreen while the upward transition is still moving. The local replay button remains immediate. Browser reproduction verified first entry, hero return at `SEALED / 0s`, a second entry actively replaying from `0s`, final pause at `1.710s`, and all three bodies relaunching.
- Desktop `1280x720` and mobile `390x844` checks found no horizontal overflow or page warning/error logs. `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 60 pages and Pagefind indexed 61 documents. Fine SVG proportions, caption balance, and physical feel remain pending human visual judgment.

### 2026-07-30 Kisara Home 001 mask review correction

- Human review found that the visible top ink block was not the new core mask contour. It came from the newly added upper `addWash()` field in the Canvas diffusion layer, which exposed a row of detached dark brush blobs after the old guard gradients were removed. That upper field is intentionally removed; do not restore it.
- The continuous quadratic ribbon experiment was also rejected and reverted because the bottom and right diffusion read as two broad dragged strips. The active Canvas behavior keeps only the earlier seeded right/bottom `addWash()` fields, with no path ribbons and no extra upper wash.
- The old large radial CSS window is replaced by the generated RGBA alpha mask `public/themes/kisara/assets/opening-memory-001-mask.png`, produced by `scripts/prepare-kisara-memory-mask.py`. The core mask no longer changes size or position during reveal; only opacity and image development animate, preventing the stretched-editor effect.
- The runtime canvas starts with the reveal and uses the same base image filter as the core image. The current visual target is intentionally conservative: remove the detached top blobs first, preserve the prior right/bottom diffusion state, and leave further contour tuning for direct human review.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 60 pages and Pagefind indexed 61 documents.

### 2026-07-30 Kisara Home 001 shared watercolor diffusion

- Human review confirmed that the previous Canvas `addWash()` system was visually unrelated to the core mask: its radial brush stamps read as particles or soft circles, so changing timing could never make the transition coherent. The old random brush generator and its right/bottom node fields are superseded.
- `scripts/prepare-kisara-memory-mask.py` now uses one shared textured-mask generator for the core, right expansion, and bottom expansion. It outputs `opening-memory-001-mask.png`, `opening-memory-001-expansion-right.png`, and `opening-memory-001-expansion-bottom.png` as RGBA alpha masks with the same irregular contour, low-frequency grain, and feathering.
- Home 001 loads the three small mask sources and the runtime Canvas only fades the two expansion masks by reveal progress. No random circles, path ribbons, mask stretching, or second visual language is used. The core CSS mask remains fixed throughout the reveal.
- The previous checkpoint `14ebc0e` remains the rollback point for this new shared-mask experiment. This experiment is intentionally uncommitted until direct visual review confirms that the expansion reads as one continuous ink treatment.

### 2026-07-30 Kisara Home 001 single-mask rollback

- The user clarified that the 001 rollback baseline is the parent of `14ebc0e` (`1f1a80b`), while 002 and the rest of the homepage must remain untouched. The current `new001` runtime image and accepted editorial layout are retained; only the 001 mask/diffusion implementation is being replaced.
- The shared-mask experiment is superseded. Home 001 now loads one RGBA mask and one hidden source image; the two expansion mask assets, their generator polygons, and the independent right/bottom diffusion regions are removed. Do not restore the rejected dragged-strip treatment.
- The reveal canvas derives one connected, low-frequency reveal order from the same mask and applies it back to that mask's alpha. It does not add a second mask, random brush stamps, ribbons, or a separately moving bottom/right layer. The core image stays fixed in place and crossfades only after the masked canvas reaches its settled frame.
- The soft reveal canvas is intentionally `640x360` and reuses its `ImageData` buffer to keep the 001 interaction lightweight; the final sharp/depth composition remains on the original image layers. `npm test` (3/3), `npm run build` (60 pages, 61 Pagefind pages), and `git diff --check` pass.

### 2026-07-30 Kisara Home 001 hazy-memory rebuild

- Human review rejected the single-mask rollback as another visible layer handoff. Its runtime Canvas, generated reveal order, fixed core mask, and duplicate focus/foreground image layers are superseded and must not be restored as the active 001 treatment.
- Home 001 now renders one foreground copy of `opening-memory-001-v2.webp` inside one fixed, wide edge blend. The image develops continuously from low-opacity blur and muted contrast into a restrained final focus; a cached same-source blurred underpainting bridges the top/right fade into matching pixels while the haze, grade, and focal glow remain stationary and never exchange masks.
- The reveal is CSS-only. The per-frame `640x360` pixel loop, hidden image/mask sources, offscreen mask Canvas, ResizeObserver repaint, and bloom/settled state machine are removed. Resetting 001 only toggles `is-memory-armed` and `is-memory-revealed` before rearming the existing IntersectionObserver.
- The old generated mask asset and preparation script remain in the repository only as rollback evidence and are no longer referenced by Home 001. Do not delete or reuse them without a new explicit visual decision.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass; Astro generated 60 pages and Pagefind indexed 61 pages. Exact haze weight, edge softness, and final focus remain pending direct human visual review.

### 2026-07-31 Kisara Home 001 forensic recovery

- The user clarified that the desired version was an uncommitted state between `1f1a80b` and `14ebc0e`, not either commit. The authoritative boundary was recovered from the Codex session JSONL at `2026-07-30T22:23:31Z` (`2026-07-31 06:23:31 +08:00`): remove only the detached upper `addWash()` group while retaining the original right, bottom, and lower-right diffusion fields.
- The later unrequested experiment began at `2026-07-30T22:30:01Z`: it removed those fields, introduced `drawRibbon()` strips, then replaced the restored field parameters immediately before `14ebc0e`. That aesthetic expansion was not authorized and is superseded.
- Recovery reverses `04dab79`, `2b83f83`, and `b0ff627` without rewriting history, preserving every later unrelated homepage feature. The recovered 001 tree matches `14ebc0e` exactly except for the three `addWash()` blocks restored to their pre-experiment values from the session log; no ribbon code or upper wash remains.
- Regenerating `opening-memory-001-mask.png` with the recovered script produced the identical SHA-256 before and after generation: `C5B474FE11C2053DC9630F0FA4311DEA25AAC6D38C777F3259ADFFB9212DCFFD`.
- Hard process rule: once a visual task is in final polish, do not broaden a narrowly reported defect into a subjective redesign. Ask first, and create a Git checkpoint before any visual change that exceeds the user's explicit description. Do not treat end-of-turn autonomy as permission to alter accepted aesthetics.
- Follow-up forensic review used the state immediately before the user's `你真的确定吗,是不是你清理错了东西` message as the right/bottom diffusion reference. Removing the rejected upper `addWash()` had also shifted the shared deterministic random stream, so the remaining washes no longer produced the same shapes; a later patch also replaced `nodes.sort((left, right) => left.birth - right.birth)` with the unsorted array, causing the renderer's early `break` to reveal nodes in batches.
- The active correction consumes the original upper wash with `emit: false` so it remains invisible while preserving the accepted random sequence for every later wash, and restores ascending birth-order sorting. No mask, layout, timing, or visible upper-cluster changes are included. `git diff --check`, `npm test` (3/3), and `npm run build` pass with 60 generated pages and 61 Pagefind pages.

### 2026-07-31 Kisara Home 001 candle reveal and panorama pass

- Commit `6a69c72` is a rejected intermediate, not the active architecture. Its duplicate-image candle Canvas produced a separately colored rectangular layer, delayed right/bottom expansion, visible handoff seams, and a final flash; do not restore that Canvas or its generated node mask.
- The active entrance reveals the real fixed-mask depth composition and the accepted right/bottom ink Canvas directly from the candle. Both use synchronized radial CSS masks only while `is-memory-candle-revealing`; completion removes the temporary masks without swapping image sources or copying pixels into another visual layer. Reset, reduced-motion, animation fallback, Astro cleanup, and replay paths now own the candle classes and RAF generation explicitly, preventing the all-blank state seen during the incomplete lifecycle wiring.
- Candle geometry is measured once before playback instead of twice per animation frame. The previous `30-42ms` manual paint throttle and independently expanding mask lobes are removed; native `requestAnimationFrame`, a smoothstep radius, and a broader feather now produce one continuous light expansion. The real ink Canvas is prepared once before the reveal and retains its accepted deterministic final drawing.
- Desktop pointer motion begins only after the candle reveal completes. The fixed core mask and ink Canvas never move. Inside them, the blurred background, subject-focus layer, and near foreground use different translation, scale, and mild perspective values driven by a damped spring; a masked warm-light/shadow field follows the viewing direction and fades away after the pointer leaves. An intermediate that transformed the whole ink Canvas was immediately rejected because it dragged half of the visible mask with the cursor and has been removed.
- Runtime inspection confirmed the source image decoded at `1920x1080`, completion reached `is-memory-candle-complete`, the three image layers received distinct `matrix3d(...)` transforms, the panorama overlay reached its active opacity, and the ink Canvas remained `transform: none`. `git diff --check`, `npm test` (3/3), and `npm run build` pass with 60 generated pages and 61 Pagefind pages. Exact candle softness and panorama feel remain pending direct human visual review.
- Panorama follow-up removed the underdamped velocity spring after human review found repeated edge oscillation. Pointer motion now approaches its target monotonically, uses the union of the fixed visual and the wider ink-Canvas bounds so the rightmost visible image area remains interactive, and no longer relies on container `pointerleave` events that fire at the overflow boundary. While the pointer owns the panorama, the automatic depth-breath animation pauses; holding the pointer at an edge therefore preserves one maximum transform instead of drifting back and forth. Runtime sampling at the right edge produced five identical transforms over about `1.3s`, while the ink Canvas remained fixed.

### 2026-08-01 Kisara Home 001 dual-shot memory transition

- Human review rejected the three-copy panorama architecture because relative motion between the background, subject-focus, and foreground masks visibly severed hair and clothing edges. The uncommitted follow-up experiment was discarded back to checkpoint `086f865`; do not restore character-segmented parallax for this scene.
- The new source `kisara/new00102.png` is the following close-up shot from the same sequence. `scripts/prepare-kisara-opening-memory-closeup.py` removes the small top-left pause label without overwriting the source and emits `public/themes/kisara/assets/opening-memory-001-closeup.webp` at `1920x1080`.
- Home 001 now contains only two complete shots: the accepted wide image and the new close-up. The old focus and foreground duplicate images and their silhouette masks are removed. The accepted candle reveal and right/bottom ink Canvas remain for the wide entrance, then retire together during a `1.9s` blur, exposure, and soft-bloom transition into the close-up.
- Pointer motion is enabled only after the close-up settles and affects the complete close-up image as one surface; it no longer moves character parts independently. Reset, replay, visibility cleanup, and Astro teardown cancel both transition timers and restore the wide entrance state.
- The first size reduction did not expose Kisara's face because the close-up was still nested inside the wide shot's fixed watercolor mask; its upper transparent region continued to remove the subject regardless of image scale. The close-up is now a sibling of the wide masked core, owns only a shallow edge-fade mask, and is rendered at `66%` source height (`64%` on the narrow mobile layout). Runtime inspection measured the settled image at about `78%` of the visual width and `68%` of its height, with Kisara's face visible and the wide shot and ink Canvas both at zero opacity.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass. Astro generated 60 pages and Pagefind indexed 61 pages. Local browser verification reached `is-memory-closeup-complete` with the new image decoded at `1920x1080`, no page console warnings/errors, and no retained wide/ink layer in the final state. Exact transition pacing and close-up scale remain pending direct human visual validation.

### 2026-08-01 Kisara Home 001 native-footage rebuild and lifecycle recovery

- The dual-shot image treatment is superseded. The untouched local source `kisara/001.mp4` is prepared by `scripts/prepare-kisara-opening-memory-video.py` into `public/themes/kisara/assets/opening-memory-001.mp4` plus first/last WebP posters. `KisaraOpeningMemoryScene.astro` plays the approximately `1.585s` native magnification shot once per 001 entry and settles on a guaranteed final-frame poster.
- The full-page video and its angled editorial copy replace the old image masks, Canvas diffusion, candle reveal, close-up handoff, and pointer parallax. Copy now enlarges into place with the footage and remains visible after settlement at tiered opacity; the rejected cycle that faded all three prose rows to zero must not be restored.
- 001 reuses the existing hero-to-001 bridge rather than drawing another transition. The bridge already retained the live/captured hero scene, but the new video custom element had an opaque `#eeece8` background above it. The scene background is now transparent while the video media keeps its top alpha mask, so the established 180px bridge is the actual layer revealed through the footage.
- 001 and 002 now actively resynchronize their visibility and playback state after `visibilitychange`/`pageshow`. This covers suspended video playback and missed IntersectionObserver exits after a long background-tab pause without resetting an already completed 002 scene unnecessarily.
- `npm test` passes 3/3 and `npm run build` passes with 60 generated pages and 61 Pagefind pages. Runtime inspection confirmed the 001 scene background is transparent, the bridge retains its captured `data:image/png` plus live hero surface, and the active base bridge scene remains visible at `0.98` opacity. Fine transition and typography judgment remains with human browser review.

### 2026-08-01 Kisara Home 001 direct hero continuation

- Human review rejected the remaining sampled opening-edge architecture. Since the 001 footage already owns a transparent top fade and the hero freezes its real camera-complete frame during the handoff, no generated continuation texture is necessary. `openingEdgeSamplingEnabled` is now false, the old 180px edge renderer is permanently hidden, and no sampler image decode, Canvas texture, `data:image/png`, or live-surface projection participates in the visible 001 connection.
- Normal Skip, section return, delayed native scroll restoration after reload, `pageshow`, `focus`, and foreground recovery all use the same real fixed `.kisara-gate-visual-shell` portal beneath the transparent 001 media. Reload recovery begins the portal as soon as restored scrolling starts instead of waiting for 001 to reach the viewport top, and keeps recalibrating until the browser's last restored-scroll movement settles.
- The site-introduction copy replaces the former memory-specific prose. Screenshot measurement showed that the contract line intersecting the copy is approximately `-8deg`; the desktop copy now follows that measured angle, uses shorter lines, stronger glyph-local contrast, and a larger settled scale. Mobile keeps a milder angle. Exact typography remains pending direct human review.
- Runtime reload sampling confirmed the portal stays active throughout the visible restored-scroll interval, the dormant edge remains hidden at opacity zero, and `hasSampledTexture` remains false from load through settlement. `git diff --check`, `npm test` (3/3), and `npm run build` pass with 60 generated pages and 61 Pagefind pages.

### 2026-08-01 Kisara Home 001/002 suspended-video recovery

- Human testing found an intermittent shared failure where the 001 and 002 native videos remained on their first frame. Re-entering from the hero, and sometimes refreshing, did not recover playback. The prior state machines treated `play()` being requested or `video.paused === false` as sufficient evidence that playback was advancing; Chromium can retain that apparent state while a frozen/suspended media pipeline leaves `currentTime` unchanged.
- `KisaraOpeningMemoryScene.astro` and `KisaraFridgeScene.astro` now use short-lived per-attempt generations plus a bounded progress watchdog. A run must advance `currentTime`; otherwise its lock is invalidated, the presentation returns to the armed/sealed state, the decoder is rebuilt once with `video.load()`, and no more than two automatic retries are attempted. Timers are cleared on completion, reset, disconnect, and lifecycle interruption, so this does not add a permanent polling loop.
- Automatic retries keep one shared budget, while a genuinely fresh user/scene attempt receives a new budget. Each active `ended` listener is owned and removed by its playback generation, preventing a queued completion event from an abandoned attempt from settling a newer one. The 002 reduced-motion path uses the existing final-frame image immediately instead of waiting indefinitely for video metadata.
- Both scenes now explicitly handle `pagehide`, `pageshow`, `focus`, Page Lifecycle `freeze`/`resume`, and `visibilitychange`. If a tab is suspended during active footage, the current attempt is cancelled immediately and the decoder is rebuilt before the visible scene retries. 002 also resets an unfinished opening when its observer range is exited, preventing an offscreen stalled `openingStarted` lock from blocking the next entry.
- EdgeOne investigation found that the production URLs for the not-yet-published new MP4 names returned cached 404 HTML with the `/themes/kisara/assets/*` one-year `immutable` header. The branch was still ahead of production, so the 404 does not prove a missing committed asset; it does prove that an error response can inherit the long-lived asset header and remain unrecoverable through an ordinary refresh. The two videos and their first/last posters therefore use content-version query strings. No global `edgeone.json` policy was changed in this patch.
- Local runtime verification used CDP `Page.setWebLifecycleState` to freeze each scene during active playback and restore it. 001 recovered to `is-memory-video-settled` at `1.544916 / 1.584917s`; 002 recovered to `is-fridge-open`, `OPEN / 04°C`, at `1.710042 / 1.710042s`. Browser logs contained no warnings or errors. `git diff --check`, `npm test` (3/3), and `npm run build` pass with 60 generated pages and 61 Pagefind pages.

### 2026-08-01 Kisara Home 001 copy/video timing sync

- The fixed-delay 001 copy animations are superseded. The native footage does not begin visibly magnifying its paper text until roughly the final third of its `1.584917s` duration, while the old CSS copy animation started near playback time zero.
- `KisaraOpeningMemoryScene.astro` now reads `video.currentTime` during the short active playback and writes the copy presentation variables directly. The accepted sequence is explicitly two-stage: the copy first lifts into position over timeline ratios `0.42-0.65`, then enlarges over `0.65-0.97` while the source gesture magnifies the paper text. Individual text rows fade in on staggered ratios but no longer animate CSS blur filters.
- The full-screen `kisara-opening-video-develop` blur/scale animation is removed because its compositor teardown coincided with the source magnification and could produce a one-frame full-screen flash. The final-frame poster remains fully opaque beneath the video, so the settled handoff cannot expose a transparent alpha gap while the video fades away.
- The bounded stalled-media recovery, lifecycle generations, watchdogs, and retry budget from `d6c04d2` remain unchanged. That pushed commit is the rollback point for this visual timing pass.
- `git diff --check`, `npm test` (3/3), and `npm run build` pass with 60 generated pages and 61 Pagefind pages. Exact lift/magnify feel and absence of the device-specific flash remain pending direct human visual validation.

### 2026-08-02 Kisara hidden-track queue and Memory Return player repair

- Human production testing found two coupled regressions: `ココロスペアキー` survived a refresh and replayed after normal track 11, while its fixed configuration position could sit ahead of `记忆归还`; the Memory Return player's complete `e4` image also disappeared only in the playing state.
- Both hidden tracks now declare `persistAcrossReload: false` and `consumeAfterPlayback: true`. Unlocks live only in `window.__yuimiKisaraSecretAudioSession`, follow actual unlock order, and are removed one at a time on `ended` in both playlist and immediate-interrupt modes. Consumption records the intended successor before rebuilding the queue, so one hidden track cannot skip, consume, or reorder the other. The head bootstrap and player delete stale session records when no persistent hidden id remains.
- The playing-image failure came from a generic `.is-secret-track ... .kisara-audio-emblem::before` `background` shorthand that replaced the Memory Return photo layer. Demon-only playing rules are now scoped to `data-secret-variant="demon"`. Memory Return renders `memory-return-player.webp` through a dedicated image element; pause/loading keeps the full frame dim and desaturated, while playing restores the same image to full opacity and color with restrained light behind it.
- Local browser checks verified the queue `normal 11 -> 记忆归还 -> ココロスペアキー` when unlocked in that order, consumption leaving only the untouched sibling track, final removal of both unlocks and the stale session key, and a decoded `480x270` source rendered at `92x52` with `opacity: 1` during Memory Return playback. `npm test` passes 4/4; `npm run build` passes with 60 generated pages and a 61-page Pagefind index.
- A remaining `ココロスペアキー` replay was not an `ended`-queue failure: the solved 003 reward dialog stayed reopenable and both reward buttons dispatched a fresh unlock every time. Each investigation reset now creates one grant id, disables both reward choices after the first claim, and sends that id with the unlock event. The persistent audio controller keeps a bounded current-document set of accepted grant ids and ignores duplicates, covering reopened dialogs, double clicks, and duplicate event delivery. Returning to the hero resets 003 and creates a new grant id, so a genuinely new investigation can still earn one new playback. Browser checks verified `ended` removes the track, replaying the same grant does not restore it, a new grant can restore it, and the reward buttons re-enable only after scene reset. `npm test` passes 4/4; `npm run build` passes with 60 generated pages and a 61-page Pagefind index.
- Follow-up testing exposed a second one-shot gap: leaving a secret track with previous/next before `ended` left its id in `unlockedIds`, so it stayed appended after normal track `11` forever. `stepTrack` now consumes any `consumeAfterPlayback` secret before selecting the requested neighbor, while the existing `ended` path uses the same `consumeSecretTrack` helper. Browser regression verified `先收下 -> normal 11 -> secret -> manual next` clears the queue and the next normal cycle returns to track `01`; the full build still passes with 60 generated pages and a 61-page Pagefind index.

### 2026-08-03 Kisara Blog/Game/Works independent hero redesign

- Rollback baseline for this pass is `b21e1f0`. The established Blog archive/search/timeline, Game two-scene spring navigation and lazy iframe lifecycle, and Works kitchen drag/state machine remain the functional owners; this pass changes only their first-viewport presentation and the visual handoff into existing content.
- Blog is now an archive threshold rather than another large English display-title hero. The four accepted character cutouts keep their original stack and move as one group over the city scene; compact Chinese reading copy, archive count, last-update metadata, a reading trace, unified low-amplitude pointer depth, and scroll-responsive retirement lead into the existing archive. Individual character parallax and per-layer blur were deliberately not restored.
- Game is now a message-interruption scene built around the native `game-message-hero.mp4`. The oversized `MESSAGE OVERFLOW` block and generic signal table are superseded by a restrained unsent-message annotation, short message echoes, and independent diagonal cut bands during scene/route transitions. Leaving the route pauses the video immediately while the next document loads, preventing the dense terminal message frame from continuing to play under a slow navigation; the existing final-frame behavior when returning from the arcade remains unchanged.
- Works is now a kitchen-prep scene rather than a portfolio-title overlay. A small daily-prep annotation, steam lift, onion-ring marks, knife/crumb trail, and a countertop fade connect the source image to the existing cutting board. Pointer response is limited to the image and counter light; mobile moves the copy and kitchen entry cue above the persistent audio control and bottom navigation.
- Each page owns a distinct `astro:before-preparation` exit treatment and keeps the existing `data-astro-rerun`, `AbortController`, `astro:before-swap`, and `window.__yuimiKisaraInnerCleanup` cleanup protocol. New motion is bounded to entry/exit, scroll events, or pointer-driven RAFs that stop at the target; no permanent animation loop, iframe preloading, or full-screen animated `filter + clip-path` combination was added.
- Local browser checks covered `1280x720` and `390x844`: all three heroes had zero horizontal overflow, pointer variables advanced on fine-pointer input, Blog/Works content bridges remained continuous, Game moved down/up without replaying the finished video, and its iframe retained no `src` before START. Fine typography, crop balance, and transition taste remain pending direct human visual judgment.

### 2026-08-03 Kisara route dissolve and hero direction correction

- This section supersedes the route-transition and hero-direction claims in the immediately preceding `Kisara Blog/Game/Works independent hero redesign` section while preserving its functional ownership boundaries.
- Safety checkpoint `e3b9c91` preserves the rejected pure-color route relay and the preceding Chinese-led Blog/Works hero draft. The opaque full-screen color cover, earlier white-fog curtain, blue handoff mark, and segmented route blocks are all rejected directions; do not restore them as the active navigation treatment.
- `KisaraRouteRelay.astro` is now a state controller only. It draws no visible DOM layer and does not delay `event.loader`; Astro's native old/new page snapshots cross-dissolve directly. Returning to Home uses the longest `680ms` dissolve, while Blog, Game, and Works use restrained target-specific opacity/translation/scale timing. The header and scrollbar own separate view-transition names so the page snapshots can move without doubling or misaligning the navigation chrome.
- Blog restores the accepted `Every signal / leaves a trace.` title. The four independent character cutouts are enlarged, lowered, and converged by moving the outer characters toward the center; staggered full-layer arrivals and one diagonal exposure pass add tension without reintroducing segmented character blur. The existing cast viewpoint buttons, archive search, filters, and timeline remain functional. Image decoding waits no longer than `420ms` before starting the intro, preventing a long blank hero on slower loads.
- Game replaces the restrained lower-left draft with a deliberately uncontrolled type field: the Japanese message lands one glyph at a time at oversized scale, outlined English failure words cross the frame, and pointer speed increases chromatic offset and supporting-line energy. The effect is isolated to typography and existing small overlays; the full video is not given a new animated filter or mask.
- Works removes the Chinese KaiTi recipe treatment and restores an English-led work identity: `WORKS, / one layer at a time.` A short editorial description and a compact six-item prep rail retain the real click-to-board behavior, while a bounded knife-line pass handles the entrance. The decorative onion-ring and crumb trail are retired from the hero; the kitchen game below is unchanged.
- Local checks covered `1280x720` and `390x844` with zero horizontal overflow. Captured route midframes confirm continuous old/new scene overlap with no opaque color frame, and the independently named header no longer shifts with the page snapshots. Blog cast focus changed to `kisara`; Works hero UE5 selection produced `01 / 06` and scrolled to the board. Browser console logs were clean. `git diff --check`, `npm test` (4/4), and `npm run build` pass with 60 generated pages and 61 Pagefind pages.

### 2026-08-03 Kisara inner hero interaction pass

- Rollback baseline is `8bb02f1`. The untracked source/candidate asset directories remain intentionally untouched. This pass changes only the tracked Blog, Game, Works hero presentation and the shared route relay.
- Blog now keeps its file code, two-line English title, description, date, and relationship note in one flow-owned copy stack, preventing the separately positioned text layers from crossing each other during intro or responsive layout. The four character layers are enlarged, lowered, converged, and explicitly staggered again; Ayano moves farther left. Desktop character hit regions reuse the existing cast-focus state to provide card-selection brackets, dim non-selected characters, lift the selected character, and update the relationship copy. Mobile hides those pointer-only hit regions and the crowded cast index.
- Game replaces the static type-storm block with a transparent outbox terminal. Four bounded message steps type, hold, mark the corresponding large irregular field message as sent, delete the terminal draft, and continue to the next message. All timers are owned by one cancellable run and are resolved during Astro cleanup; leaving the page still pauses the hero video and unloads the game iframe. Reduced motion settles immediately to the delivered state.
- Works reuses the six existing kitchen ingredients as a desktop-only hero slicing field. Pointer motion uses segment-to-center collision rather than click handling, so quick strokes can cut a fruit without taking pointer ownership from the existing kitchen game. Each cut splits the complete ingredient into two clipped copies, emits one short blade trail, increments a bounded counter, briefly separates the title's two cut halves, and respawns the ingredient. The field is disabled for mobile, lite, and reduced-motion profiles.
- The repeated route transition came from settling the same navigation on both `astro:after-swap` and `astro:page-load`. `KisaraRouteRelay.astro` now settles only on `astro:after-swap`, deduplicates identical in-flight route keys, ignores a second settle for the same serial, and retains a stalled-navigation cleanup fallback.
- Final verification: `git diff --check` passes; `npm test` passes 4/4; `npm run build` generates 60 pages and a 61-page Pagefind index. Desktop checks confirmed the Blog intro and character selection, the complete Game type/send/delete sequence, Works slicing (`CUT TEST 01`), and route-state cleanup. `390x844` checks showed zero horizontal overflow, a contained Game terminal, hidden Works slicing field, and no Blog copy/cast overlap. Browser warning/error logs were empty. Exact motion taste remains pending human review.

### 2026-08-03 Kisara hero interaction balance pass

- Rollback baseline is `dca3e4a`. This section supersedes the Blog selection-box/lift wording and the static Works fruit-respawn behavior in the preceding interaction pass; the archive, terminal sequence, kitchen game, route relay, and untracked source-material boundaries remain unchanged.
- Blog title descenders were being cut by the old horizontal-reveal wrapper's `overflow: hidden` and zero-height clip bounds. The reveal now keeps its left-to-right timing while expanding only the vertical clip extent, so `Every signal / leaves a trace.` retains complete `y`/`g` strokes on desktop and mobile. Sharon moves farther right and down. Character focus keeps the requested scale contrast but anchors each layer around its own body, uses a true slow-start `1150ms` transform transition, and removes the large translucent picker frame and label plate.
- Game's four outbox messages now build toward `HEAVY LOVE` and `MY GRAVITY`; the terminal gains denser weight and downward shadow, while delivered words land with a short gravity bounce. The existing type/hold/send/delete lifecycle remains unchanged, all four messages still settle, and the mobile terminal remains contained.
- Works no longer keeps six ingredients at fixed screen coordinates. A single desktop-only, intersection-paused RAF launches them from below the scene with staggered horizontal drift, upward velocity, gravity, and rotation. Pointer slicing uses the live physics center; both clipped halves inherit the parent trajectory plus opposite cut-normal velocity, continue falling, and respawn only after leaving the field. The RAF, observer, and timers are released by the existing page cleanup. Mobile, lite, and reduced-motion still hide the field.
- The hero fruit renderer no longer exposes the reused ingredient's rectangular color tile. The shape root is transparent and borderless; jelly, ice, orange, star candy, leaf, and cherry bodies are drawn independently beneath their icons, so slicing still divides the full visible item without restoring a shared card shell.
- Verification: browser sampling observed intermediate Blog scales before the final selected `1.07` state and confirmed the picker pseudo-frame is `display: none`. A real pointer stroke changed Works to `CUT TEST 01`; both halves retained separate transforms while the parent continued its parabola. Desktop Game completed all four delivered messages with zero horizontal document overflow and no console warnings/errors. `390x844` checks retained zero overflow, hid Blog pickers and the Works slicing field, and contained the Game terminal. `git diff --check`, `npm test` (4/4), and `npm run build` pass with 60 generated pages and a 61-page Pagefind index.

### 2026-08-03 Kisara Works fruit-shell and Blog cutout repair

- Rollback baseline is `97d70c7`. The untracked generated-source, model, and temporary QA directories remain excluded from the patch. Character review now uses explicit 64/80-pixel grid sheets plus marked source/runtime ROIs before any production asset is replaced.
- The visible Works fruit rectangle was not the fruit shape background addressed in the preceding pass. `.kisara-works-slice-fruit.kisara-ingredient` still inherited the desktop pantry card's `border-radius` and rectangular `box-shadow`; the hero override now clears both. Browser-computed style confirms a transparent background, zero border/radius, and `box-shadow: none` while preserving the independent fruit bodies and slicing physics.
- Kisara's non-wing-side underarm artifact came from the broad historical `chest_green` inpaint operating on green-screen pixels that the matte had already forced opaque. A source-chroma alpha correction now removes only that reported ROI, explicitly clears certain green background, and decontaminates retained edge pixels. Grid QA reports zero strongly green opaque pixels in the target region.
- Ayano's broken trailing strands matched the old runtime rectangle that deleted suspected magenta spill. The extraction pass now reconstructs only source components connected to the accepted hair matte, restores their alpha, and caps red spill on partial edge pixels. Sharon's remaining cyan thigh block is detected from the untouched source frame and replaced by a feathered continuation of the adjacent lower body colors rather than a white patch or transparent rectangular hole; the target cyan count is zero after repair.
- `write_cv` now uses `Path.write_bytes(encoded.tobytes())` instead of NumPy `tofile`, avoiding an intermittent Windows Unicode-path write failure observed while regenerating `sharon-solo.png`. Only the three reported runtime WebPs changed; Shu remains byte-identical. `git diff --check` passes apart from the existing Git line-ending warning, `npm test` passes 4/4, and `npm run build` generates 60 pages with a 61-page Pagefind index. Fine cutout judgment remains pending direct human review.
- Kisara shoulder follow-up (`2026-08-04`): the broad historical chest inpaint and later edge cleanup could borrow RGB from the red ribbon, leaving thin red contamination and partially red hair over the transparent shoulder. `repair_kisara_shoulder_pollution` now runs after the edge pass, removes false fragments whose untouched source is green background, recolors only pixels that became red despite a non-red source from nearby accepted pink hair, and performs two bounded convergence passes. The user-marked diagonal strand mapped to runtime `x=949..991, y=453..487`; because both ends were visually disconnected, its exact source-space path and detached lower tip are cleared while the vertical strand and connected right-side hair remain. Only `kisara-front-solo-v1.webp` changed in this follow-up. A clean full extraction into `tmp` reproduced identical alpha and only `0.027` mean absolute RGB difference from the iterative preview before its fresh WebP replaced the tracked asset. Local page QA loaded the updated `1440x975` asset, `python -m py_compile` passed, `npm test` passed 4/4, and `npm run build` generated 60 pages with a 61-page Pagefind index.

### 2026-08-11 Kisara Blog ribbon-edge micro cleanup

- User review marked floating pixels and small jagged color edges around Kisara's red ribbon/black outfit intersection. SIFT registration mapped the screenshot boxes to runtime asset ROIs `x=919..1045 / y=451..507` and `x=932..988 / y=493..545`; broad erosion was rejected because the lower box also contains valid red antialiasing and connected pink/white hair strands.
- `repair_kisara_runtime_ribbon_edges()` runs only after Kisara is placed into the `1440x975` runtime canvas. It removes seven independently disconnected components totaling 14 pixels only when `area <= 4` and `maxAlpha <= 12`, then repairs RGB on 155 partially transparent red/dark edge pixels without changing their alpha or silhouette. The production WebP is saved from the verified candidate losslessly so all decoded pixels outside `x=936..1041 / y=451..545` remain byte-identical; no other character asset, Blog markup, CSS, or untracked generated source changed. Pixel verification confirmed exactly 169 changed pixels, 14 alpha changes, and zero decoded changes outside the two ROIs. `python -m py_compile`, `git diff --check`, `npm test` (`22/22`), and `npm run build` (62 pages, 63-page Pagefind index) pass; final Blog-scale visual acceptance remains with the user.
- Follow-up human review after refresh/restart found that the page still looked unchanged. Runtime forensics ruled out a stale reference or browser/server cache: Astro source, `dist/blog/index.html`, the development-server response, and the browser image `currentSrc` all used the committed 260,576-byte asset with SHA-256 `C82A674F...B57F`, while the parent asset was 108,008 bytes. The first repair was therefore visually insufficient: only 14 alpha pixels changed, leaving the actual ribbon silhouette and three visible disconnected fragments intact.
- The active follow-up keeps the same two verified ROIs but now removes every fully contained disconnected component up to 15 pixels (three remaining fragments, 27 pixels in the committed input) and reconstructs only the red ribbon's local alpha contour. It retains red components of at least six pixels, limits work to a 7x7-supported ribbon region, closes/opens an `alpha >= 96` mask with a 3x3 ellipse, and applies a restrained `sigma=0.48` Gaussian edge inside a 2.2-pixel contour band. Connected hair and pixels outside the marked ROIs remain unchanged. Against `e4d8233`, the new lossless production WebP changes 1,301 decoded pixels, including 1,298 alpha pixels, with zero changes outside the target ROIs; its SHA-256 is `5599B11075D958F981722C3960190F3EEDEC43A7FB83A7B7DCA22E8F4DB62477`. `python -m py_compile`, exact pixel assertions, `git diff --check`, `npm test` (`22/22`), and `npm run build` (62 pages, 63-page Pagefind index) pass. The rebuilt `dist` asset and the live `127.0.0.1:4321` response both match that SHA-256, and `/blog/` returns HTTP 200. Renewed human Blog-scale acceptance remains pending.
- A second marked screenshot separated five orange hair-break boxes from three blue ribbon-bump boxes. The authoritative pre-matte source is `output/imagegen/blog-character-reconstruction/luna/kisara-green-v1.png` (`1536x1024`). Screenshot-to-runtime SIFT registration used 31 RANSAC inliers with `0.316px` mean error; the accepted source alpha crop `[435,134,1107,829)` maps to runtime `[463,310,1106,975)`. This invalidates area-only cleanup as a complete solution: the ribbon bumps are connected to the main component, while real thin hair can be disconnected and small enough to look like noise.
- `repair_kisara_runtime_hair_and_ribbon()` now runs after the general component/ribbon cleanup. Inside only the eight verified runtime boxes, it reconstructs pale/pink hair alpha from the source green-screen color distance (`smoothstep(10,72)`) and borrows RGB from nearby accepted opaque hair, then lowers alpha on the three scarf snippets only where the registered source does not contain the red ribbon path. The reviewed candidate adds 118 hair alpha pixels, removes 129 ribbon alpha pixels, changes 247 decoded pixels total within `x=931..1044 / y=473..532`, and changes zero pixels outside the marked boxes. The lossless production WebP SHA-256 is `8302660800FA31AC6B0B6BBD2B5EFA8554195CC42E29143E05B58BC26F93C330`. Script-to-candidate equality, exact pixel assertions, `python -m py_compile`, `git diff --check`, `npm test` (`22/22`), and `npm run build` (62 pages, 63-page Pagefind index) pass. `dist`, the live `127.0.0.1:4321` response, and the tracked public asset share that SHA-256; `/blog/` returns HTTP 200. Renewed human Blog-scale acceptance remains pending.
- Human review rejected the `e99e1d6` result. Pixel-grid QA is preserved under `tmp/kisara-crossgrid-qa-20260811/`, with the pre-matte source projection, `546f64f` before state, and `e99e1d6` after state on identical runtime coordinates. The ribbon pass is effectively a no-op: all 129 changed pixels lose only `1/255` alpha because the boolean ribbon mask is divided by 255 inside `feather_marked()`, so no contour pixel disappears at alpha thresholds 16, 32, 64, 96, or 128. Do not treat the reported changed-pixel count as visual evidence.
- The source-guided hair pass is also rejected as incomplete. At `alpha >= 16`, marked groups H1-H5 change component counts `3->3`, `5->3`, `3->1`, `2->2`, and `3->2`; only H3 connects at low alpha and it remains split at `alpha >= 64`. H2/H3 form an overly thick bend around runtime `x=1009..1014 / y=483..490`, while H4/H5 retain stair steps and visible gaps. The next pass must use grid/threshold connectivity as its acceptance test and replace this alpha-only interpolation rather than stacking another micro-adjustment onto `e99e1d6`.
- The replacement pass starts from the decoded `546f64f` asset rather than stacking on rejected `e99e1d6`. Source-projected one-pixel hair skeletons restore only verified paths with core alpha 176 and limited alpha-48 edge pixels; the three ribbon boxes now clear pixels outside the registered red path to full transparency. Independent read-only review passed H1, H3, H4, H5, and R1-R3, but found one false H2 bridge. Restoring runtime pixel `(1010,483)` to the `546f64f` baseline leaves a full grid-cell gap at alpha thresholds 64 and 128 while H3 remains connected.
- `repair_kisara_runtime_hair_and_ribbon()` no longer uses the rejected chroma interpolation/weak feather. It applies the exact 123-pixel reviewed RGBA patch after the general runtime edge cleanup: 62 restored hair pixels and 61 removed ribbon pixels, bounded to `x=934..1044 / y=477..525` with zero changes outside the eight marked boxes. Applying the function to the `546f64f` decoded baseline reproduces the final candidate pixel-for-pixel. H2 has two components and H3 one component at both alpha thresholds 64 and 128; every ribbon box removes visible contour pixels at both thresholds. The lossless production WebP is 260,224 bytes with SHA-256 `00049480445CC46E1FF04F566585CB39B323B0F547CC520D3B13F09E2B74258F`. Exact pixel assertions, `python -m py_compile`, `git diff --check`, `npm test` (`22/22`), and `npm run build` (62 pages, 63-page Pagefind index) pass. The tracked public asset, rebuilt `dist`, and live `127.0.0.1:4321` response share that SHA-256; `/blog/` returns HTTP 200. Renewed human Blog-scale acceptance remains pending.
- A later five-box edge-balance investigation is preserved under `tmp/kisara-edge-balance-20260813/` and has not replaced production. Candidate V4 is forbidden: `make_candidate_v4.py` copied projected chroma-source RGB directly into restored pixels at `candidate[added,:3]=source_rgb[added]`, visibly introducing green/grey-green edge pixels in every marked box. The registered pre-matte source may provide geometry or matte evidence only; output RGB must come from the current clean production asset's local hair, ribbon, or garment pixels.
- V5 removed source RGB but incorrectly applied one outer-alpha SDF pass to all five boxes; independent review passed only B1/B4 and rejected B2/B3/B5. V6 separated outer hair from internal ribbon/garment boundaries and eliminated green residue, but review still found B2/B3/B5 visibly stepped. Neither candidate is suitable for production.
- V7 uses rejected V4 only as ribbon geometry and never copies its RGB. It borrows all written colors from local current-production pixels, keeps B3/B5 alpha byte-identical, changes 604 decoded pixels inside the five boxes plus 3px only, and has zero green-dominant pixels in that support before and after lossless WebP round-trip. Its temporary WebP SHA-256 is `27196C57129FF26DF31AB12426B1BE9E0C6187C71143245629202A428B69082D`; the production asset remains byte-identical at SHA-256 `00049480445CC46E1FF04F566585CB39B323B0F547CC520D3B13F09E2B74258F`. Local B2/B3/B5 comparison sheets show the V4 green contamination removed and the most obvious grey protrusions reduced, but V7 remains a human-review candidate only. Do not update `scripts/extract_kisara_solo_characters.py` or the production WebP until the user accepts the page-scale result.
- The full rematte follow-up uses only the project-local `tmp/kisara-rematte-20260813/.venv`; no global package, mirror, proxy, Git, or certificate setting changed. PyTorch's official CDN was too slow, so only the install command was redirected to `https://mirrors.nju.edu.cn/pytorch/whl/cu128/`; the 2.87GB CUDA wheel completed in about 90 seconds, and `torch 2.10.0+cu128` verified the RTX 4060 Laptop GPU. ViTMatte small and the ToonOut BiRefNet weights are cached under `tmp/kisara-rematte-20260813/models/`. Pure ViTMatte was rejected because it widened/reconstructed B1/B4 hair and softened the B2 crossing; ToonOut alone removed real B4 hair. The ToonOut-topology/ViTMatte hybrid was retained only as the scarf/garment matte baseline.
- `tmp/kisara-rematte-20260813/refine_hybrid_hair.py` reproducibly builds the current review candidate from the hybrid plus the clean production RGBA; it never reads green-screen RGB. Narrow deterministic corridors remove the B1 false bridge, restore the real diagonal hair into B2, and restore both B4 hairs without thickening the upper strand. B2 decontamination changes RGB only on 87 low-alpha cyan-spill pixels, leaves alpha unchanged, and reduces the B2 cyan-spill check from 88 pixels to zero. B3/B5 remain byte-identical to the hybrid, every decoded change is inside the approved corridors, and the lossless WebP round-trip is pixel-identical. The independently re-reviewed candidate passed B1/B2/B4 bridge, seam, color-spill, and scarf-smoothness checks; its WebP is `tmp/kisara-rematte-20260813/candidates/refined-hybrid/refined-hybrid-runtime.webp`, SHA-256 `DB173BD599E5AD013BD5CAA3462D0F44114E895ECAA155BCB1C5DC59AFD8C02C`. It is approved only for user review. Production remains unchanged at SHA-256 `00049480445CC46E1FF04F566585CB39B323B0F547CC520D3B13F09E2B74258F` and must not be replaced before human acceptance.
- Human follow-up rejected further reconstruction of the short right-side hair and instead marked eight yellow and five blue regions whose broken strands and detached dots should be removed. `tmp/kisara-rematte-20260813/prune_fragment_hair.py` starts from the accepted scarf-matte candidate, restores only the upper hair area from the previous Blog production RGBA, clears every non-scarf pixel inside the registered yellow/blue polygons, and removes detached `1-7px` components in runtime `x=930..1060 / y=433..535`. The final candidate clears 659 marked fragment pixels plus three detached pixels; marked regions contain zero remaining non-scarf pixels, the cleanup ROI contains zero detached components, protected scarf pixels change by zero, and every change stays inside the approved upper-hair or deletion regions. A broader RGB decontamination experiment was disabled because it introduced dark jagged hair edges; do not restore it without new evidence. At the user's direction, the candidate replaced `public/themes/kisara/assets/blog/kisara-front-solo-v1.webp`; the production SHA-256 is now `7FF459F2A7C7834416F9A4FADA90AF07B11968133C1C66F6C1A48D5DE37330FD`, and its lossless WebP round-trip is pixel-identical. The previous production asset remains recoverable from Git and the external backup at `C:/Users/a1234/Desktop/codex-backups/personal-blog-kisara-rematte-20260813/kisara-front-solo-v1-production-000494.webp`.
- Publication checkpoint: local commit `fe74b6d fix(blog): finalize Kisara cutout cleanup` published the final Blog asset and its recovery record to `origin/main`. The independent new-stage branch was also published for the first time as `origin/codex/kisara-new-stage-20260810`, currently at `d6324f2 docs: record new stage side branch`. Existing untracked workshop and generated assets were deliberately excluded from both pushes.

### 2026-08-04 Kisara Blog cast depth entrance

- Rollback baseline is `6822b04`. The four accepted complete cutouts, final stage boxes, DOM stack `Sharon -> Ayano -> Shu -> Kisara`, picker hit regions, and final cast-focus interaction remain unchanged.
- The old entrance had correct `z-index` order but no usable depth motion. All four layers shared one converge keyframe, and a later base rule forced their final transform with `!important`, so animation transforms were suppressed and only opacity/filter timing remained visible.
- The entrance now uses three bounded keyframes: Sharon and Ayano establish the rear plane from a smaller, softer state; Shu crosses into the middle plane from the lower left; Kisara enters last from a larger near-camera scale and settles without a hard bounce. Character-specific scale, direction, blur, duration, and delay variables preserve the existing final positions. The exposure sweep is delayed until the middle/front handoff, and the intro completion timer now covers the full `2200ms` sequence.
- The base final transform no longer uses `!important`, allowing keyframe transforms to run; the existing cast-focus dim/selected transforms keep their own `!important` ownership. Browser sampling observed Sharon/Ayano near `0.92-0.95` entry scale, Shu at `1.065`, Kisara at `1.19`, and the selected Kisara control still settled at `1.07` while the other layers reached `0.62` opacity.
- Mobile uses smaller travel, at most `1.1px` entry blur, shorter delays, and a `1.1` maximum front scale. At `390x844`, all four `1440x975` images decoded, document and client widths both remained `390px`, and the stage stayed clipped without blank frames. Desktop/mobile console logs were empty; `git diff --check` passed, `npm test` passed 4/4, and `npm run build` generated 60 pages with a 61-page Pagefind index.

### 2026-08-04 Kisara inner hero focus and Ayano face cleanup

- Checkpoint 2903e7f predates this pass; ebf95d2 preserves the Blog/Game/Works interaction changes before the later asset repair. Untracked generated sources, model directories, and QA artifacts remain intentionally excluded.
- Blog no longer uses four hand-authored polygon picker regions. Desktop fine-pointer input builds bounded half-resolution alpha maps from the four accepted transparent WebPs, samples only inside the hero with one requestAnimationFrame at a time, and resolves overlap in the existing DOM depth order. Character focus now enlarges to 1.13-1.17, pushes non-selected layers to 0.34 opacity with strong grayscale, and places an unframed name/relationship note beside the active character. Mobile does not allocate or run the alpha hit test.
- Game expands the outbox sequence from four to seven shorter, fragmented messages and accelerates type/send/delete timing. Local runtime reached 07 MESSAGES / DELIVERED in roughly the prior presentation window with all seven field bursts settled and no horizontal overflow. Works replaces its English ingredient sentence with concise Chinese copy using the existing Microsoft YaHei/Segoe UI sans-serif stack.
- Ayano's reported cheek-side block was not valid hair or face detail. Source/runtime/cutout comparison showed the original magenta-screen image contains only one real blue strand there; the old face_magenta reference repair had made a source-key-colored component opaque. refine_reported_artifacts() now removes only the connected component inside source ROI x=918..960, y=167..220 when the untouched source remains close to the magenta key. The candidate removed exactly 678 source pixels, preserved the strand, cheek and jaw, and was verified in the actual Blog at the selected 1.145 scale before replacing ayano-middle-solo-v1.webp.
- python -m py_compile scripts/extract_kisara_solo_characters.py, git diff --check, and npm test pass (4/4). npm run build generates 60 pages and a 61-page Pagefind index. Desktop browser verification confirmed alpha-accurate focus for Kisara, Ayano and Sharon, empty-space rejection, decoded character assets, and zero horizontal overflow.

### 2026-08-04 Kisara Game shake easter trigger

- Rollback baseline `258450e` preserves the complete shake-easter prototype, including `game-shake-easter.mp4`, the one-shot `darekare-scramble` secret track, its independent player skin, the full-screen playback layer, and the session-only consumption protocol.
- The rejected trigger tried to accumulate and decay frame-by-frame pointer energy toward `100`; normal mouse input could remain visibly stuck at `98/99`, so do not restore that threshold model.
- The accepted trigger is two-stage. Pointer travel first accumulates to a bounded `620`-unit latch, which locks the typography at maximum shake. Continued ordinary movement then advances a stable `5.0 SEC` hold timer; a short pause is tolerated, while a real stop longer than `680ms` releases and resets the latch. This keeps the secret intentional without requiring extreme mouse speed.
- Progressive hints and visual interference now follow the hold timer. Reaching five seconds consumes the video and song once for the current tab; exiting, skipping, changing track, finishing playback, or revisiting the page cannot leave the secret track attached to the normal playlist or retrigger the scene.
- Browser checks covered successful latch/countdown/playback, reset after stopping, one-shot consumption, and the dedicated `scramble` player state. `git diff --check` passed; `npm test` passed 5/5; `npm run build` completed with 60 generated pages and a 61-page Pagefind index. The user subsequently confirmed the reported trigger problems were resolved.

### 2026-08-04 Kisara Game shake easter consumption repair

- Safety baseline `7d8ad79` contains the updated sub-agent operating rules; `d01d433` remains the rollback point for the accepted two-stage shake trigger. The visual easter and `darekare-scramble` track must remain independently single-use within one tab after confirmed playback.
- The intermittent missing `5.0 SEC` HUD was a false consumption state, not a pointer-energy failure. The old `v1` flow wrote its Game session key before `video.play()`. A rejected play, media error, or route cleanup before the first frame hid and reset the overlay but left the tab marked consumed, so refresh and Astro route changes stayed locked until the tab closed. A second eligibility mismatch let fine-pointer windows at `760px` or narrower accumulate while CSS hid both the HUD and video.
- Game consumption now uses a new `v2` key so historical false `v1` records cannot keep existing tabs locked. The trigger enters an in-memory arming state with an attempt generation; only a resolved video start commits the tab session, marks the visual consumed, and dispatches the secret-audio grant. Failure, media error, close, or route cleanup before that commit invalidates the attempt, restores the HUD, and leaves the same tab immediately retryable. Confirmed playback still remains one-shot across refresh and route changes, while the audio controller retains its separate `singleUseSession` protocol.
- Runtime eligibility now matches the CSS boundary through `(min-width: 761px)`. Narrowing the viewport cancels an uncommitted attempt or resets pressure; pointer movement while hidden cannot charge or consume the easter. Returning to a supported width re-enables the existing trigger without a reload.
- Added `tests/kisara-game-shake.test.ts` to guard the `v2` session migration, arming state, viewport boundary, and the ordering of video start before Game/session/audio consumption. `npm test` passes 7/7. Browser regression confirmed `pressure=1` with `4.5 SEC`, successful transition to video `playing`, one-shot persistence after confirmed playback, `700px` hidden movement staying at `pressure=0 / 5.0`, re-arming after widening to `1280px`, and re-arming after an unconsumed Home-to-Games round trip; console warnings/errors were empty and horizontal overflow remained zero. `npm run build` completed with 60 pages and a 61-page Pagefind index.

### 2026-08-04 Kisara Game shake repeat eligibility

- Supersedes the earlier tab-once eligibility rule. Entering the Game hero may start a fresh shake attempt whenever the visual sequence is idle and `darekare-scramble` is not actively playing; a completed attempt no longer disables the Game easter for the rest of the tab.
- Game no longer reads or writes its `v2` consumed session key. Eligibility is derived from the live persisted audio player (`scramble` variant plus an actually playing audio element), so finishing, skipping, or pausing the track makes a later Game visit eligible again while active playback still blocks duplicate charging.
- `darekare-scramble` remains consumable after each playback but is no longer a `singleUseSession` track. The Game grant no longer uses one fixed grant id, allowing each later accepted visual playback to grant and restart the song without leaving duplicate entries in the playlist.
- `git diff --check`, `npm test` (10/10), and `npm run build` (60 pages, 61-page Pagefind index) pass. The user then verified the real browser flow can complete and trigger a second time.

### 2026-08-04 Kisara Game shake progressive aftermath

- The five-second locked hold now drives a separate `--game-easter-shake` curve from a restrained initial tremor to full intensity. Terminal, large type, and sent-message layers increase both displacement and cadence continuously instead of waiting for one late discrete pressure stage.
- A naturally completed easter video enables a page-local `afterglow` state. The countdown/HUD charge clears when the overlay leaves, while the three text layers retain maximum shake until the route is actually left; closing or failing the video before completion does not create the aftermath.
- The Scramble audio `playing` listener no longer resets pressure during its own active visual sequence. This preserves a continuous maximum shake through video entry, playback, fade-out, and afterglow instead of visibly stopping and restarting at the handoff.
- `git diff --check`, `npm test` (11/11), and `npm run build` (60 pages, 61-page Pagefind index) pass. The user verified the progressive hold, uninterrupted video handoff, and persistent page-local afterglow in the real browser.

### 2026-08-04 Kisara Works pig obstacle

- The desktop Works fruit field now reuses Fuyukawa's transparent `public/themes/fuyukawa-kagari/assets/mini-pig-scroll.webp` as one dedicated `104x89` special body. Its initial and repeat waits are substantially longer than the six normal ingredients, keeping it a low-frequency interruption without changing the accepted fruit launch cadence.
- The pig is never sent through the fruit split path. Pointer segments are tested against its elliptical body first; the visible blade trail ends at the earliest pig contact and the shortened segment is then used for ordinary fruit slicing, so food behind the pig cannot be cut through it.
- Accepted pig contacts derive bounded linear impulse and torque from the pointer segment's direction, distance, and elapsed time. Side and top boundaries use damped reflection while the bottom remains open; after a randomized three-to-five wall contacts, a pig still in play fades for `0.9s` before returning to its longer queue.
- Pig hit squash and a short impact ring provide collision feedback without adding score or triggering the Works title cut. Mobile, lite, coarse-pointer, and reduced-motion profiles retain the existing disabled fruit field.
- `git diff --check`, `npm test` (14/14), and `npm run build` (60 pages, 61-page Pagefind index) pass. Final size, frequency, force, and rebound feel are reserved for the user's browser acceptance.

### 2026-08-04 Kisara Lovebrain ED secret hero

- Rollback baseline is `b81c038`. The new secret is eligible only inside one browser tab after visiting the canonical Blog, Games, Works, and Me routes, accepting the Home 003 `kokoro-spare-key` reward, and completing either the Chibi jealousy or apple stage event. `window.__yuimiKisaraLovebrainProgress` stores only this bounded qualification state in `sessionStorage`; the active ED scene and its current visual progress are never restored after a reload.
- The qualified normal Home gate gains one small heart control to the left of AUTO/SKIP. `found-self` keeps higher priority and suppresses the control while its ticket or playback owns the gate. Clicking the heart enters the independent `KisaraLovebrainEasterEgg.astro` scene rather than layering more effects onto the normal seal sequence.
- Runtime media uses eight `1600x900` WebP stills extracted from the supplied 4K/59.94fps Engage Kiss ED at representative route, message, character, chase, convergence, and final-park timestamps. Their production files live under `public/themes/kisara/assets/lovebrain/`; the original workshop video is never copied into the site. Image elements keep only `data-src` until the qualified control is hovered/focused or activated, so ordinary Home visits do not download the ED set.
- The scene interprets the ED as three unstable relationship routes: wheel, keyboard, or touch input continuously draws the pink/green/cyan paths through notification growth, message overload, three selectable character fragments, pursuit, convergence, and the final park frame. Notification clicks reveal temporary memory slices; character clicks change route emphasis and dim the other two. The final fifth of the sequence fades the notification and character fragments back out so the three-person frame resolves cleanly instead of remaining visually overloaded.
- Home owns a separate `lovebrain` page mode. Activation stops the normal autoplay, canvas RAFs, warmups, post-release pass, orientation input, and both WebGL renderers. The Lovebrain-to-001 and reverse transports use the existing damped scroll carrier without activating the blue gate portal, title reset, opening sampling, or warning warmup. The scene remains the covering surface until 001 reaches its stop; returning upward is designed to reveal the retained Lovebrain final state before the scroll reaches the top.
- `public/themes/kisara/audio/renai-nou.mp3` is a runtime copy of the explicitly supplied untracked `kisara/恋愛脳.mp3`. A direct user click starts it and suspends the persistent player; the scene heart pauses/resumes from the current position. Entering 001 or leaving the route fades and resets the ED audio, then restores the prior player state. The shared suspension protocol now owns a `Set` of ids, so overlapping found-self, Me END, Game, or Lovebrain interludes resume normal audio only after the final owner releases. The ordinary player is hidden only while the Lovebrain viewport is visible.
- Desktop browser verification covered unqualified loading, qualification, entry placement, all six visual stages, notification memory, character focus, the final park state, Lovebrain-to-001 transport without a blue gate frame, and normal-player restoration. Eight ED WebPs were absent before qualification and loaded only after the qualified interaction. Browser logs were clean apart from unrelated external Statsig telemetry timeouts. The reverse 001-to-Lovebrain path and `390x844` final layout remain pending direct human validation because the focused browser retry was stopped after its connection stalled.
- `edgeone.json` needs no new rule: the existing `/themes/kisara/assets/*` and `/themes/kisara/audio/*` immutable headers already cover every new runtime file. After the final convergence cleanup, `git diff --check` passes, the full `npm test` suite passes `20/20`, and `npm run build` produces 60 pages with a 61-page Pagefind index.

### 2026-08-05 Kisara Lovebrain persistent player and scrub stage

- Supersedes both the retained-final-frame behavior and the scene-owned private audio player documented above. `faf9197` remains preserved by `codex/backup-lovebrain-before-player-20260805`; the user-accepted global-player/smoother-scrub state is checkpointed at `7ebf59d` and preserved by `codex/backup-lovebrain-before-face-race-20260805`.
- `lovebrain-ed` now belongs to the existing persistent Kisara playlist. It uses the dedicated `lovebrain` player skin together with the shared pause/resume and previous/next controls, persists across Astro Kisara route swaps, and deliberately does not persist through a full browser refresh. `KisaraLovebrainEasterEgg.astro` no longer owns a private `Audio`, close button, suspension owner, or duplicate player UI. Re-entering the visual stage while the track is already selected preserves its position and resumes it when paused.
- `stage1-scrub.mp4` and `race-scrub.mp4` are `1600x900`, 30fps, short-GOP/no-B-frame derivatives intended for random access. Wheel/keyboard/touch input changes a target stage progress; one RAF smooths the visual progress, while each paused video keeps only one in-flight seek and flushes the latest queued target after `seeked`. Direction controls frame direction and stopped input holds the current frame without the old direct frame-to-frame jumps.
- The face/race refinement keeps the accepted media and ownership boundaries. The three avatar cards receive separate scroll-reversible spring values and appear left-to-right with a bounded Q-style overshoot; their selection transform lives on an inner card wrapper so focus interaction cannot overwrite the entrance transform. The FACES background runs only while the portrait layer is visible and uses lightweight moving diagonal color routes/scan bands without a full-screen filter loop.
- The RACE scrub mapping now spans normalized progress `0.39..0.76` instead of the former roughly `0.42..0.675`, and the active race region consumes up to `22%` more directional input. The later loop/final thresholds were moved accordingly, so the supplied chase clip reads at a slower pace while forward/reverse/hold semantics remain unchanged.
- The stage is consumed when its dissolve-to-001 transport completes. `lovebrainActive` is released at that boundary, so a later upward return restores the ordinary Home gate; the heart remains eligible for a deliberate new entry. Because Lovebrain activation destroys the normal gate WebGL renderers, forward completion still rebuilds both renderers in the background before the user returns to the gate.
- Existing EdgeOne `/themes/kisara/assets/*` and `/themes/kisara/audio/*` rules already cover the runtime media. After this refinement, `git diff --check` passes, `npm test` passes `21/21`, and `npm run build` produces 60 pages with a 61-page Pagefind index. Luna's read-only desktop check confirmed the left-to-right portrait pop, final `1280x720` bounds of about `x=78..1206 / y=97..631`, and the `6.8s` portrait background motion; Race pacing, overflow, console, and focus-click acceptance remain for the user's own browser pass.
- Race pacing follow-up: human testing still perceived the chase as fast-forwarded even after widening its normalized range. The remaining cause was the generic visual-progress catch-up cap: `0.000115` progress/ms across a `0.37` span drives the fixed `5.7s` clip at about `1.77x` while target progress is ahead. The seek queue, short-GOP asset, and one-in-flight/latest-target protocol remain unchanged. Inside the Race envelope, the RAF cap now derives from the actual video duration and limits catch-up to about `1.12x`; its response time eases from `68ms` toward `96ms`, and directional input gain rises smoothly to `1.85x` instead of `1.22x`. This slows both forward and reverse traversal without restoring direct per-frame seeking. `git diff --check`, `npm test` (`21/21`), and `npm run build` (60 pages, 61-page Pagefind index) pass; final tactile acceptance remains with the user.
- Pinch-loop follow-up: the original `pinch-loop.mp4` is a `1.2s / 36-frame` one-way action, so native looping exposed a hard composition jump at the boundary. `pinch-pingpong.mp4` contains the forward action, the reversed release, and the relaxed-start hold. The latest cleaned derivative removes source frames `35..37`, which were three abrupt zoomed-out park frames embedded between the pinch close-up and its reverse; it is now `1600x900`, 30fps, 73 frames, `2.433s`, and no-B-frame. The original remains untouched for rollback.
- Pinch-loop gap follow-up: the earlier blank/decode-reset diagnosis is superseded. The visible gap was caused by those three zoomed-out source frames, not by the browser decoder or the double-buffer handoff. The pinch panel still mounts two copies of the cleaned derivative, removes native looping, preloads the inactive copy, and crossfades to its matching first frame during the last `160ms`; the generic embrace loop and scrub-video paths remain unchanged. `npm test` passes `21/21`; final real-browser visual acceptance remains with the user.
- Lovebrain audio one-shot follow-up: `lovebrain-ed` is now consumable after one natural playback or manual skip. It returns to the saved normal 11-track position instead of advancing into another hidden track, removes itself from the in-memory secret list, and can only be added again by clicking the Home heart entry. The player remains same-tab only and does not persist the unlock through refresh. `npm test` passes `22/22`, and `npm run build` produces 60 pages with a 61-page Pagefind index.
- Lovebrain stage-consumption follow-up: the stage previously kept `lovebrainActive` true until the damped scroll reached 001, so a missed completion callback or an upward wheel during the final transport could resurrect the already-left secret stage. Forward departure now commits consumption before scrolling, while the visual keeps its existing dissolve. A shared idempotent finalizer clears Home classes, restores the normal gate renderers, and directly finalizes the scene runtime; reversing during transport therefore returns only to the default Home gate. `npm test` passes `22/22`, and `npm run build` produces 60 pages with a 61-page Pagefind index.

### 2026-08-13 Kisara Home 001 continuous hint strikes and 002 timed inventory drop

- Each 001 hidden-route sentence now owns one `kisara-opening-easter-hint-fragment`, so the existing two-layer Pulse strike crosses the comma and spans the entire sentence instead of drawing two disconnected segments. The hint ids, ledger, achievement animation, and reduced-motion fallback are unchanged.
- The 002 refrigerator footage is 41 frames at 23.976fps (`1.710042s`). Contact-sheet review places the fully opened-door frame at roughly `1.00s`; the three inventory bodies now begin their original staggered physics launch from the video `timeupdate` at that point, while the video continues to its original final poster/status. Completion retains an idempotent launch fallback and reset/replay/lifecycle paths clear the temporary media listener before a fresh attempt.
- Added static regression coverage for both one-fragment-per-hint markup and time-based fridge release. `git diff --check`, `npm test` (`24/24`), and `npm run build` pass; Astro builds 62 pages and Pagefind indexes 63 pages. Final visual timing and line feel remain for direct human acceptance.

### 2026-08-13 Kisara Works title cut face

- The Works hero title no longer resolves to two zero-gap text halves. Each `data-cut-copy` title line now contains an aria-hidden `kisara-works-title-cut-face` text layer clipped to the existing diagonal knife route, with a warm highlight-to-pink/burgundy material gradient and shallow high-light/shadow treatment that reads as a cut surface.
- The entrance blade pass now precedes a bounded separation/rebound: the upper and lower glyph halves settle into opposite small diagonal offsets around the persistent face. Cutting a flying ingredient reuses the `is-knife-active` path to briefly widen that separation, brighten the face, and replay the two blade streaks before returning to the stable gap. Reduced-motion keeps the static cut surface and legible gap but disables blade/transform animation.
- Static validation added to the existing Works test file. `git diff --check`, `npm test` (`25/25`), and `npm run build` pass; Astro builds 62 pages and Pagefind indexes 63 pages. Browser-agent visual QA did not return usable evidence and was stopped, so final page-scale motion/texture acceptance remains for direct human review.

### 2026-08-13 Kisara Works title cut-face alignment repair

- Screenshot review found the first cut-face implementation was structurally misaligned: the face text lived as ordinary inline content inside an absolute wrapper, while the top/bottom glyph copies were absolute pseudo-elements. The face therefore established a separate line box and appeared as a horizontal text band beneath or across the title instead of occupying the same glyph coordinates.
- The face wrapper remains the clipped absolute layer, but its `::before` now renders `data-cut-face` as an absolute `inset: 0` same-font/same-line-height copy. The gradient, highlight, and shadow moved onto that pseudo-element; all three title layers now share the identical positioned box. The temporary interaction brightness selector follows the moved paint layer. Added static assertions for the face pseudo-element's absolute/inset alignment.
- `git diff --check`, `npm test` (`25/25`), and `npm run build` pass; Astro builds 62 pages and Pagefind indexes 63 pages. Two bounded browser-agent attempts did not return usable local screenshots and were stopped, so direct human visual confirmation remains the only pending acceptance.

### 2026-08-13 Kisara Works title cut single-layer repair

- The alignment repair remained visually invalid. The user screenshot showed the actual root cause: the title was simultaneously rendered as its original text, two clipped full-text pseudo-element halves, and a cut-face full-text copy. Even when sharing `inset: 0`, those independent glyph paints produced broken horizontal bands and per-glyph seams.
- The title now renders exactly one glyph layer per line. All `data-cut-copy`, `data-cut-face`, `kisara-works-title-cut-face`, upper/lower copied-text rules, and their keyframes were removed. A `173deg` text background gradient paints the narrow ivory-to-pink/burgundy diagonal material band inside the original glyphs; the existing non-text blade streak remains the only separate cut overlay and still replays for `is-knife-active` fruit cuts. This removes the entire class of duplicate-text alignment defects.
- The Works static test now asserts the removed duplicate-text markers remain absent while the single-layer text clipping and replayable blade feedback remain. `git diff --check`, `npm test` (`25/25`), and `npm run build` pass; Astro builds 62 pages and Pagefind indexes 63 pages. The direct screenshot sent by the user was the acceptance evidence for removing the prior architecture; final aesthetic acceptance of the simplified material cut remains with the user.

### 2026-08-15 Kisara Works transient Canvas split-and-merge title

- The user clarified that the desired effect is real separation and recombination, not a static material strip. The prior DOM-copy split and the later static text-gradient are both superseded for this effect: copied DOM text caused visible bands/seams, while a permanent gradient cannot physically open and close.
- `ProjectsPage.astro` now adds one aria-hidden Canvas inside the Works `h1`. It measures the two native title lines after `document.fonts.ready`, generates one offscreen text bitmap and one alpha-derived face bitmap per line, and only during an intro/re-cut animation clips/translates those shared bitmaps into upper half, lower half, and diagonal face. This keeps every animated fragment tied to one source image rather than independently rasterized text. After the bounded intro (`1160ms`) or re-cut (`540ms`) completes, the Canvas clears, `is-title-canvas-active` is removed, and the normal single-layer DOM title resumes.
- The Canvas remains disabled for reduced-motion, mobile, and lite performance profiles. A `ResizeObserver` coalesces title resize recalculation into RAF; Astro cleanup cancels both Canvas RAFs, disconnects the observer, and restores the native title. Fruit slicing now triggers the same Canvas re-cut path as the independent blade streak. Static tests assert the Canvas model, font readiness, ResizeObserver, cleanup finalizer, and both triggers. `git diff --check`, `npm test` (`25/25`), and `npm run build` pass with 62 generated pages and a 63-page Pagefind index. A local dev server is running at `http://127.0.0.1:4321`; browser-agent visual QA was attempted but did not return usable results and was stopped, so direct human visual acceptance remains pending.

### 2026-08-15 Kisara Home 002 earlier drop and pig capsule

- The fridge video remains `1.710042s`; its inventory trigger now begins at `0.88s` rather than `1.00s`, so the original staggered physics starts just before the door is completely open instead of waiting for the full-open frame or clip completion.
- `KisaraFridgeScene.astro` now contains four bodies. The new fourth body is a translucent capsule that reuses the existing transparent Works pig asset at `public/themes/fuyukawa-kagari/assets/mini-pig-scroll.webp` (`1899x1625`, about `284KB`), with its own spawn position, resting angle, lateral velocity, and spin. It remains part of the existing launch, reset, reduced-motion, collision, drag, and cleanup paths rather than adding a second physics system.
- Static coverage now asserts the `0.88s` release, the four-body invariant, and the pig-capsule markup. `git diff --check`, `npm test` (`25/25`), and `npm run build` (62 pages, 63 Pagefind pages) pass. Local desktop browser verification at `1280x720` observed all four launched bodies within the physics stage, no horizontal overflow, and no warnings/errors; the capsule and pig were visibly rendered. A mobile automation reload did not resolve the 002 selector before its tool deadline and was restored to the default viewport, so mobile visual confirmation remains pending.

### 2026-08-20 Kisara Works title print-and-registration rewrite

- The user rejected further refinement of the title-cut treatment. The Canvas split/recombine model, knife-streak overlay, and fruit-slice re-cut trigger are superseded for the Works hero title: the visual had accumulated too many incompatible mechanisms and was no longer a useful base for incremental adjustment.
- `ProjectsPage.astro` now keeps one semantic DOM layer for each title line: `WORKS,` is the primary logotype and `one layer at a time.` is the secondary line. All Canvas measurement/offscreen compositing, `ResizeObserver`, title cleanup, and title-specific slice callbacks were removed. Fruit slicing itself is unchanged.
- `projects.css` replaces the cut motif with a CSS-only print-and-registration entrance: the primary line rises from a blurred lower mask into a graphite/coral/gold/teal press gradient, a three-color registration rule draws beneath it, and a small rotated registration mark settles in. The secondary line arrives later with a separate lift, color sweep, and double-rule marker. Reduced motion resolves directly to the readable static title.
- Static tests assert the new title classes/keyframes and reject Canvas/cut remnants. Scoped `git diff --check`, `npm test` (`25/25`), and `npm run build` (62 pages, 63 Pagefind pages) pass. Per the user's instruction, no browser validation was run; visual acceptance remains with the user. An unrelated trailing whitespace warning in the user's modified `src/content/blog/codex-app-usage-notes.md` remains untouched.

### 2026-08-20 Kisara Works hero lifecycle recovery

- The first title-rewrite commit removed the Canvas measuring function but left `window.addEventListener("resize", scheduleWorksTitleMeasure, ...)` inside the hero bootstrap. Resolving that removed identifier threw before `playHeroIntro()`, leaving the hero image in its pale idle presentation and preventing the slice-field physics plus all entry state transitions from starting.
- The orphaned resize listener is removed. Static regression coverage now rejects `scheduleWorksTitleMeasure` alongside all superseded Canvas/cut symbols. Direct inline-script parsing with `new Function(...)`, scoped `git diff --check`, `npm test` (`25/25`), and `npm run build` (62 pages, 63 Pagefind pages) pass. Per user instruction, browser validation remains intentionally unperformed.

### 2026-08-20 Kisara Lovebrain and Game first-use media recovery

- Both reported failures came from cold first-use media paths. Lovebrain injected the initial media only at activation and later stage media only when progress approached each threshold; video panels had no stable poster while metadata and the first decoded frame were pending. Game's shake video stayed `preload="none"` until the interaction was already more than halfway armed, then tried to enter the visual state immediately without waiting for `HAVE_CURRENT_DATA`.
- Lovebrain now keeps the deferred `data-src` ownership model for ordinary Home visits, but uses a staged warm queue: entry hover/focus begins the initial orbit/portrait/race group, activation immediately hydrates the opening group and queues the remaining stages at `260ms` intervals. Every deferred image/video retains its source descriptor for a one-time retry after an `error`; cleanup cancels queued/retry timers. Orbit, race, pinch, and embrace videos have small matching existing WebP posters so no panel begins as an empty rectangle while video data arrives.
- Game's shake video now starts with metadata preload, receives a new `1280x720` first-content WebP poster derived from the existing `game-shake-easter.mp4` at `2.2s` (`44,292` bytes, SHA-256 prefix `100B0F393C99`), and is switched to `preload="auto"` after the ordinary hero becomes ready. On trigger it waits for a current video frame, retries one media reload on failure, and only then begins playback/commits the visual hint and secret-audio grant. An active post-start video error still exits cleanly.
- Direct inline-script parsing for both components, scoped `git diff --check`, `npm test` (`26/26`), and `npm run build` (62 pages, 63 Pagefind pages) pass. Per user direction, no browser validation was performed; real-network acceptance remains with the user.

### 2026-08-18 README rewrite and documentation screenshots

- Root `README.md` was rewritten from the minimal project setup note into a Chinese project overview: independent multi-theme architecture, an extended Kisara introduction, a dedicated Fuyukawa Kagari section, article authoring, local development, deployment, and repository structure. The presentation follows a compact theme-project README rhythm without treating the two primary themes as cosmetic variants.
- Added four local documentation screenshots under `public/readme/`: Kisara Home, Works, Friends, and Fuyukawa Kagari Home. Each is `1440x875`; together they are `494,584` bytes. A browser subagent did not complete the planned Fuyukawa Blog capture, so its incomplete image reference was removed rather than leaving a broken README asset.
- Before writing, the existing user worktree diff was preserved at `C:\Users\a1234\Desktop\codex-backups\personal-blog-before-readme-20260818-README.patch`. Focused `git diff --check` passed and `npm test` passed `25/25`. The temporary local Astro dev server used for screenshots was stopped after capture; direct human review of README prose and screenshots remains separate from static verification.
- Published checkpoint: `391d06f docs: rewrite themed project README` was pushed to `origin/main` after remote `c334f90`; no pre-existing user edits or untracked source materials were included in that push.

### 2026-08-18 README Kisara motion-media refresh

- The previous four static README screenshots were removed from both `README.md` and `public/readme/`. The title now places the existing Kisara `favicon.ico` and Fuyukawa Kagari `pig-favicon.png` tab icons on either side of `Yuimi Lab`.
- Five user-supplied source clips under the untracked `kisara/README/` directory were retained without modification. Local ffmpeg conversion created `public/readme/kisara-{home-stage,find003,chibi,lovebrain,me}.gif`, each `360x202` at about `8fps`; the full original clip durations are retained within GIF timing precision and their total size is `8,071,521` bytes. The first `480px/12fps` pass exceeded the 12 MiB README-media budget and was discarded.
- The Kisara README section now pairs the five clips with Home, Event 003, Chibi, Lovebrain, and Profile/Friends narrative text. The source-worktree patch before this edit is `C:\Users\a1234\Desktop\codex-backups\personal-blog-before-readme-kisara-media-20260818.patch`; focused diff checks passed and `npm test` passed `25/25`. Direct visual acceptance of the GIF compression remains for human review.
- Published checkpoint: `bdf967f docs: refresh Kisara README media` was pushed to `origin/main` after remote `391d06f`; no pre-existing user edits or untracked source materials were included in that push.

### 2026-08-20 README Fuyukawa motion-media addition

- Two user-supplied recordings under the untracked `fuyukawa/` directory were retained without modification. Local ffmpeg conversion created `public/readme/fuyukawa-home.gif` and `public/readme/fuyukawa-notes.gif`, each `360x202` at `8fps`, preserving the source clips' roughly `6.00s` and `6.55s` durations within GIF timing precision. Together they are `5,323,884` bytes.
- `README.md` now inserts the first clip after the Fuyukawa Kagari overview and the second after its Blog/Projects/About description. The existing Kisara motion media and both tab icons remain unchanged. The source-worktree patch before this edit is `C:\Users\a1234\Desktop\codex-backups\personal-blog-before-readme-fuyukawa-media-20260820.patch`; focused diff checks passed and `npm test` passed `27/27`. Direct visual acceptance of the GIF compression remains for human review.
- Published checkpoint: `f5d5408 docs: add Fuyukawa README media` was pushed to `origin/main` after remote `ed84b97`; no pre-existing user edits or untracked source recordings were included in that push.

### 2026-08-20 README theme reuse guidance

- `README.md` now includes concise second-development guidance. Kisara is documented as a source of isolated motion-effect reference/reimplementation, not a generic conversion template, because its visual narrative, hidden routes, audio state and runtime are deeply coupled to Engage Kiss-related settings and assets. Fuyukawa Kagari is documented as the more suitable personal-site adaptation base because its presentation effects generally remain useful after ordinary asset and copy replacement.
- The README also records practical boundaries: preserve independent theme implementations, centralize replaceable assets/content, limit each viewport to a primary action plus restrained supporting motion, and verify mobile/reduced-motion/theme-switch/page-cleanup behavior after interaction changes. The earlier generic `Kisara 适合什么` card was removed after review, so the precise reuse boundary now lives only in this section. Focused diff checks passed and `npm test` passed `27/27`; user did not request publication in this pass.
- Published checkpoint: `04fcc24 docs: add theme reuse guidance` was pushed to `origin/main` after remote `f5d5408`; no pre-existing user edits or untracked source materials were included in that push.

### 2026-08-20 Development notes publication

- The accumulated recovery record was deliberately made public at the user's request. `20a55cd docs: publish development notes` added the then-current `95` verified lines to `DEVELOPMENT_NOTES.md` and was pushed to `origin/main`; no unrelated article, image, generated asset, or untracked workshop material was included.

### 2026-08-20 Blog Kisara direct donor replacement

- The user rejected further local blending and explicitly selected the complete `tmp/kisara-rematte-20260813/candidates/vitmatte-narrow-runtime.webp` donor after reviewing the three-column comparison. Per that instruction, it directly replaced `public/themes/kisara/assets/blog/kisara-front-solo-v1.webp`; no additional alpha merge, image-model run, page capture, build, or visual verification was performed in this replacement step.
- The immediately previous production WebP (SHA-256 `7FF459F2A7C7834416F9A4FADA90AF07B11968133C1C66F6C1A48D5DE37330FD`) is preserved at `C:/Users/a1234/Desktop/codex-backups/personal-blog-kisara-hair-repair-20260820/kisara-front-solo-v1-before-hair-repair.webp`. This direct selection remains uncommitted and unpushed pending user acceptance.

### 2026-08-20 Kisara Home 001 edge transparency and Lovebrain handoff polish

- The lingering mist at the top of 001 was three compositing ramps arriving at different heights: the video/media mask, the section grain mask, and the live gate-to-001 container background. Their former low-opacity stops began near the top edge, so even an otherwise transparent first row still read as a foggy horizontal band. All three now hold at full transparency through `72px` before their controlled fade begins; the tiny `0.025` bridge stop is removed.
- Lovebrain's completed downward handoff no longer uses its separate blur-and-enlarge retreat. Its transport state now matches the Found Self exit timing and geometry: fade over `760ms` plus a `900ms` `translateY(-2.5%) scale(1.012)` motion, leaving the normal page scroll to carry the scene onward.
- The unlocked Lovebrain entry button now has a two-phase visibility lifecycle. Eligibility first removes `hidden`, then a RAF applies `is-entry-visible`; loss of eligibility removes that class and only re-applies `hidden` after the exit transition. Cleanup cancels both pending handles. This removes the prior pop-in/pop-out while keeping the button unfocusable during its departure.
- Added static guards for the 001 transparent edge and for the Lovebrain entry/transport lifecycle. The inline Lovebrain script parses with `new Function(...)`; scoped diff checks, `npm test` (`27/27`), and `npm run build` (62 pages, 63 Pagefind pages) pass. Per the user's direction, no browser verification was run; direct visual acceptance remains pending.

### 2026-08-20 Kisara Home 001 bridge-gradient correction

- Screenshot review clarified that the deep navy at the top of 001 is the intended underlying bridge image, not an unwanted layer. The visible issue was the overlying `.kisara-opening.is-opening-bridge-live` paper gradient beginning with a comparatively dense `0.32` alpha stop, which made the image read through a gray-white haze.
- The bridge now begins with explicit paper-colored zero alpha through `116px`, retains only `0.06` alpha at `146px`, then continues into the existing opaque paper handoff. This preserves the dark image behind it while making the upper end of the pale gradient fully transparent.
- Updated the 001 static assertion. Scoped diff check, `npm test` (`27/27`), and `npm run build` (62 pages, 63 Pagefind pages) pass. No browser validation was run at the user's request; visual acceptance remains with the user.

### 2026-08-20 Kisara Lovebrain-to-001 bridge handoff recovery

- The Lovebrain completion path used a direct `smoothScrollTo()` rather than the shared `enterNextPage()` bridge. It kept `is-lovebrain-active` and `is-lovebrain-home-active` through the entire scroll, which intentionally hides the Gate and 001 bridge layers. Once the fixed Lovebrain scene faded, the hidden page underneath appeared black; the hidden 001 bridge then reappeared only at arrival as a solid block.
- `enterNextPage()` now accepts an optional arrival callback. Lovebrain clears its two hiding classes and restarts the normal Gate visual resources before starting that shared bridge; its final consumption callback runs only after the standard 001 arrival state is established. If the handoff cannot start, the old Lovebrain classes and resource shutdown are restored before returning failure.
- Static regression coverage asserts the shared arrival hook, early removal of the Lovebrain hiding classes, and rejection of a second direct smooth-scroll path. `HomePage.astro` inline script parses with `new Function(...)`; targeted tests, `npm test` (`27/27`), and `npm run build` (62 pages, 63 Pagefind pages) pass. Per user direction, browser verification remains intentionally unperformed.

### 2026-08-20 Kisara Lovebrain transport ownership correction

- The shared-bridge recovery above is superseded after direct user testing: restoring the suspended Gate before Lovebrain had finished exposed stale scene state and left the background visually pinned to the secret stage. Lovebrain must retain ownership of its backdrop until its own dissolve is well underway.
- The completion path is back to its dedicated scroll route and does not restart or reveal Gate resources early. It now publishes normalized scroll progress to the Lovebrain component. The component keeps the secret stage fully visible for the first `22%` of the 001 journey, then drives its opacity, upward drift, and scale continuously through `99%`; arrival dispatches `1` before the ordinary finalizer removes the scene.
- The base 001 section background now shares the bridge's paper-colored zero-alpha top ramp, so its upper container can reveal the departing stage rather than becoming a solid white panel during this specific overlap. The later opaque paper body remains unchanged.
- Scoped diff checks and focused tests pass; `npm test` passes `27/27`, and `npm run build` produces 62 pages with 63 Pagefind pages. The Lovebrain component inline script parses with `new Function(...)`. `HomePage.astro` includes TypeScript generics and is validated through the successful Astro build rather than bare `new Function`. No browser validation was run at the user's request; human visual acceptance remains pending.

### 2026-08-20 Kisara Lovebrain direct-to-initial-stage dissolve

- The scroll-synchronized dissolve restored continuity but still revealed black because the ordinary Home gate remained hidden until the final Lovebrain cleanup. The intended recipient is the initial Home stage, not a black intermediary and not the stale pre-secret gate state.
- At the start of Lovebrain departure, while the secret scene is still fully opaque, Home now runs `resetGateState()`, rebuilds the normal space/title renderers, and only then removes the two Lovebrain hiding classes. The secret scene continues to own the visible top layer and fades with scroll progress, so the initial gate is already painted underneath every dissolve frame.
- If the smooth scroll cannot start, the hidden classes and resource shutdown are restored before returning to the Lovebrain scene. Static coverage asserts reset, renderer startup, and reveal order. Scoped checks, `npm test` (`27/27`), and `npm run build` (62 pages, 63 Pagefind pages) pass. Browser validation remains intentionally unperformed per user instruction.
- Published checkpoint: `ed84b97 fix(kisara): dissolve lovebrain into initial stage` was pushed to `origin/main`; pre-existing uncommitted assets and documentation edits were not included.
